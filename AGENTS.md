# SEC 仓库开发入口

本文件只负责启动和行为路由，不拥有产品架构、测试矩阵、当前状态或完整执行状态机。

0. **最高行为入口**：每个新任务、续跑、压缩恢复、委派和实现动作都先从当前用户的终局结果出发，再允许 Skill、Work Package、控制面或现有实现参与。先明确用户可观察终态、必须保持/明确不要的结果、已知生命周期和全生命周期成本；再对准备读取、保留、新增、修改或验证的每个对象做“彻底删除它，终态或正确变更成本是否变差”的反事实，并归类为`required | derivable | duplicate-owner | dominated | orphan | unknown`。只有`required`可直接保留或实现；其余必须派生、合并、删除或保持bounded unknown。该裁决强制沿最小因果图闭合identity/唯一owner、producer/consumer、state/Effect、failure/recovery、安全/权限、并发、资源/性能、外部能力/重复轮子、版本/兼容/迁移/退役、test/Evidence与可维护性；这些是同一裁决的必查投影，不是等待用户逐项提醒的可选清单。具体语义分别路由到`docs/product.md`、`docs/system-architecture.md`、`docs/change-management.md`、`docs/external-provider-policy.md`和`docs/verification-governance.md`的唯一owner。Skill、Work Package、代码、测试、文档、版本、路径、状态和绿色Evidence都只是手段，不能产生业务目标或否决更上游裁决；任何用户纠正或新反例推翻前提时，依赖它的计划、实现与Evidence立即失效并从终局结果重算，禁止在旧表示上继续补丁。
1. 以最新 `main`、开放 PR/Issue、CI、Review 和真实 diff 建立事实；branch、PR body、Issue、聊天和报告不能证明完成。
2. 用 `bun run dev:status -- --json` 绑定受信 latest default/base resolver、当前 Work Package 与 intended workspace；这是确定性 preflight，不要求预读任何 Skill。结果为 `unresolved` 或 `invalid` 时停止写入。**同一 active candidate 的跨会话续跑例外**：当上游已经从真实 Git/GitHub snapshot 冻结一个 digest-bound `sec-local-continuation-checkpoint-v1`，且当前用户/maintainer明确保证该快照后没有外部 GitHub/main/PR/Review 状态变化时，第一次只运行 `bun run dev:continue -- --handoff <external.json> --json`。它先对 exact branch/base-tree/head/tree/单父 parent/clean-state/manifest/scope 做一次零远程 admission，再把不可变 snapshot 作为 content-addressed object 写入仓库外 canonical SEC Runtime State，并建立 workspace locator 与 active pointer；之后同一工作区只运行 `bun run dev:continue -- --json`，不再要求手工传 JSON。candidate/self-generated checkpoint、本地重新填写同一事实或没有上游 snapshot 的 checkpoint不能证明远端未变化。本地 candidate 后续改变只形成 `local-candidate-diverged`，不因此机械重做 GitHub orientation；Review/MainHealth/authorization/merge/closeout 分别用 `--boundary` 只刷新对应 live owner，明确外部变化用 `--external-changed` 使旧 snapshot stale，session/work 完成用 `--terminal` 退役 active pointer并触发 GC。整个 continuation surface 永远只有 `context-compression-only` 权限，不能产生 Scope、Verification PASS、Review、MainHealth、IntegrationAuthorization、status 或 merge authority。
3. `src/control/agent/task-capsule.ts` 只拥有 deterministic `sec-task-capsule-v1` **unbound planning content**：强制 `authorityStatus=unbound-planning-content`、`effectAuthority=none`、`scopeGrantId=null`。candidate manifest、pointer、journal、Capsule digest、本地Git ref/文件或同用户self-digest都不是issuer credential。production positive path只消费trusted default-branch GitHub Actions/App producer为同一draft PR发布的PRE/FINAL：PRE在实现前驱动Task Capsule、Read Plan与Skill，FINAL在exact实现head上重验同一scope/control/owner closure；canonical payload在immutable Actions artifact，App comment只作locator。consumer必须重验App、artifact digest、repository、workflow SHA、run/attempt、job/steps、maintainer permission、WorkDecision、PR、manifest和whole-delta scope。当前head既无有效PRE也无有效FINAL时统一返回bounded typed activation reason及detail digest。不得用raw Capsule/envelope、caller fields、测试seam、手工JSON或candidate workflow绕过。新Agent默认只读`AGENTS.md`、active pointer、唯一manifest及其`authorityRefs`经registry派生的owner closure，不靠聊天、旧PR/Issue评论、全Issue census或全部Skill正文建立“熟悉度”。
4. 在 trusted issuer cutover 前，只在当前有效 effect grant 与 frozen Work Package 的 owned/forbidden paths、acceptance、tests、capability 和资源交集内工作；unbound Capsule 不能扩大、收窄或替代该交集。issuer cutover 后才加入 live-bound Capsule/ScopeGrant/CandidateScopeAttestation 交集，并在 new-main readback 后删除旧 owner，不保留双写或兼容旁路。任何 prose 都不能扩大 machine authority。
5. `OperationReadPlanCompiler` 与 Skill applicability decision只证明pure content、canonical digest、最小owner closure的一次性read receipt和zero-or-one算法；Work Package的`authorityRefs`把source scope映射到`docs/authority.json`中的canonical domain owner，changed registered document再自动加入自身owner或projection target，禁止固定四文件、全仓预读或candidate自造owner。选择面仅为`.agents/skills/**`。#275已作为第一个真实PRE→FINAL canary完成该路径；`none-required`时不加载，`ambiguous`/`conflict`/`stale`/`unresolved`时停止reconcile，Skill body数不超过`maxSkillBodies`。delegation的收益与隔离判断仍不可纯计算，因此`sec-task-delegation`继续作为第八个bounded Skill，直到真实production owner能同时推导delegate与no-delegation。#346直到三个真实operation和一次真实maintainer mutation observation完成前保持开放，implementation PR只能发布progress。
6. 产品与架构只读取 active Work Package 指定且由 `docs/authority.json` 定位的唯一领域 owner；issuer-bound Read Plan 上线后再由其 `requiredRefs`/`conditionalRefs` 收窄，任何阶段都不得靠全仓预读建立“熟悉度”。
7. 影响分析先使用 capability ledger 中当前 operation authority 已批准且可调用的能力；不可用时降级到 exact imports、consumer 与 test-impact census。执行集合是 `RequiredClosure ∩ MissingOrStale`；fresh PASS、fresh failure 与 authenticated in-flight 分别复用、停止该无效执行路径或 join，禁止临时安装工具、重建索引和机械全跑。
   MainHealth 先由同一 fresh exact-main ledger互斥投影为 `ordinary-only | repair-only | locked`：只有ordinary调用WorkDecision，repair只消费内容寻址的MainHealthRepairDecision，locked停止；禁止为启动repair先跑全Issue/catalog census。repair只是routing，不是scope、Review、provider或merge authority。
8. Worker 不自授权扩大 scope、触发 trusted-provider Gate 或 merge；A0 负责 DAG、integration、Gate custody、merge、readback 与本任务可证明安全的清理。GitHub Actions只是可选provider，不是Gate authority或完成前提。一个 logical run 只保留一个 mutable worktree 和一个 active candidate ref；finding 在原 worktree 修复，禁止 v2/v3/v4 舰队。
9. Squash merge 后按新 `main` tree、实际实现和 Evidence 判断结果，不按旧 commit ancestry、分支名或 ahead/behind 判断。信任变化必须封存旧 operation epoch 及其 effect grant、Review、Evidence 和缓存事实，但仍在用户授权中的长期 A0 任务必须自动从 exact new main 重新执行 canonical status、重载入口/manifest/authority closure并进入新 epoch；不得仅因代际变化返回历史性的 `TASK_RESTART_REQUIRED` 或要求用户再次说“继续”。只有控制面 `unresolved/invalid`、缺少外部authority、独立Review不可用、必须由用户选择或其他typed外部阻塞才停止整个任务。`check:full` 只用于 release/nightly、selector calibration 或 unresolved impact backstop，不属于普通 edit/finding loop。
10. 失败先定位 root cause、owner、invariant 与失效 Evidence；输入和 failure tail 未变时复用失败，只重跑被 delta 影响的最小验证。外部 maintainer/user 改动使旧 observation stale，当前物理状态优先；恢复 claim 只能由本层分类为“需要独立 verified executor”，不得从裸枚举签发 effect authority。显式用户恢复、Agent 自身越权 rollback receipt 与 operation-owned CAS 分别由其 authority owner复核 principal/resource/preimage/current/expiry 后执行；否则禁止用 `git fsck`/dangling object/checkout 复活旧状态，冲突返回 typed `external-maintainer-mutation`。机器可观察规则必须由类型、Schema、parser、validator、test、Hook 或 CI 拒绝；哲学与原理放在 canonical owner，AGENTS 只保留启动路由。
11. 遇到可重复问题或接受新的计划裁决时，按 `sec-heuristic-governance` 先解析已有 work identity、current spec 与 canonical owner；命中则同步既有 owner，确认无身份才建立 focused work。下一步由 #221 `WorkDecision` 对 normalized machine facts裁决，live adapter切换前A0按同policy显式reconciliation、切换后只消费receipt；#349只编译order/conflict，rolling plan只作当前加二至五候选的投影。聊天、评论时间、AI评分或新建第二计划不能成为选择authority。
   受控PR/merge只能由IssueDisposition-aware唯一写者生成并在effect前后读回zero closing authority；provider无条件写能力时，意外关闭只能返回typed maintainer action，禁止伪装CAS自动reopen。Git worktree closeout必须先持久化target外authorization，再以registry+physical双读回判定completed/residue；`git worktree remove`退出码不是终态，#313只能消费#186 completed receipt后处理ref。
12. 外部成熟机制的调用与是否新增 wrapper/Adapter 由 `sec-external-capability-governance` 和 `docs/external-provider-policy.md` 裁决：普通离散开发操作默认直接消费最窄稳定 machine interface，不为 Git/GitHub/compiler/search/test/container 等成熟能力建立一对一 SEC 镜像 API；只有 protocol/platform/Effect/credential/version/Evidence/performance/compatibility/security 边界确有新增价值时才允许薄 Adapter。存在 JSON、porcelain、NUL-safe records 或 explicit format 时禁止解析面向人的 presentation。shell 只作 bounded process transport，不能签发 semantic/effect/completion authority；跨多个成熟工具形成真实 SEC invariant/state transition 时才建立上层 semantic operation。具体 route/version/standing capability 只从 external capability ledger 和对应 toolchain authority 读取。
13. 工程北极星是最小化“正确变更”的全生命周期成本，而不是局部代码量、Gate 数量或单次命令时长。业务与架构事实只在唯一 semantic owner 写一次；依赖闭包、公共投影、TCB、影响测试与 Evidence 必须从该事实机器派生，禁止用源码句子、路径清单或第二份手工期望镜像实现。优先用类型、Schema、模块与 capability 边界让非法状态不可表示；测试观察公共行为、持久状态和真实 Effect。编辑期只运行受 delta 影响的廉价哨兵，昂贵 Evidence 只对 frozen exact tree/environment 生产一次并按 `ActionKey` 复用。具体机制由对应 architecture/verification machine contract 拥有，本文件只负责路由。
14. 发现可跨任务复用的原则时，按`docs/development-governance.md`的“跨任务原则闭包与Work Package边界”进入既有canonical owner；本文件只保留路由，Work Package不得成为全局原则的第二owner。
15. 发现边界清晰且并行收益真实的bounded subtask时，按`docs/development-governance.md`的delegation decision evidence路由委派；主线程保留授权、canonical决策、single writer、集成、验证和收口。
16. 上下文压缩、Agent/进程切换或工具宿主重建后，先运行既有continuation/live-provider reconciliation；summary只作probe hint，不能授权spawn、exec、download、build、外部写、cleanup或merge。
17. 治理自纠是长期有效的仓库内行为权限，不是某一轮对话的临时扩权。当前事实或maintainer纠错一旦证明Skill、Work Package、AGENTS、计划、projection或其他治理对象制造/放大了错误，或阻止同一根因的修复，立即把依赖该错误前提的计划、实现与Evidence标为stale，并按`governance-self-correction`路由审查和修正该对象的唯一owner、必要投影、manifest ownership与验证；有缺陷的治理对象不能用自己的scope、forbidden path或停止规则否决自身修复。恢复实现前必须持久化maintainer-correction reconciliation，至少包含失败前提、通用不变量、唯一owner、受影响闭包、确定性拒绝点、启发式Skill owner、负测/回扫、被替代路径和恢复条件；只回复“明白/对/已记录”不构成完成。该常设权限只覆盖导致当前失败的最小治理闭包，不授权无关产品修改、外部写、安装、发布、删除、清理或merge。
