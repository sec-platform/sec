name: compiler-fast-probe-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired probe notice
        run: echo "Temporary fast probe retired. Use compiler-pr-validation. Full release validation is exercised separately."
