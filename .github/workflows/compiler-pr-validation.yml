name: compiler-pr-validation
run-name: verify PR #${{ github.event.pull_request.number }} ${{ github.event.action }} @ ${{ github.event.pull_request.head.sha }}

on:
  pull_request:
    types:
      - labeled
      - synchronize

permissions:
  contents: read
  statuses: write

concurrency:
  group: ${{ github.workflow }}-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  compiler-pr-verification:
    if: >-
      (github.event.action == 'labeled' && (github.event.label.name == 'run-quick' || github.event.label.name == 'run-full')) ||
      (github.event.action == 'synchronize' && contains(github.event.pull_request.labels.*.name, 'run-full'))
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - name: Resolve exact head, current base, profile, and dedupe key
        id: verification
        uses: actions/github-script@v7
        env:
          VERIFY_LABEL: ${{ github.event.label.name }}
          VERIFY_ACTION: ${{ github.event.action }}
          VERIFY_HEAD: ${{ github.event.pull_request.head.sha }}
        with:
          script: |
            const label = process.env.VERIFY_LABEL;
            const action = process.env.VERIFY_ACTION;
            const labels = context.payload.pull_request.labels.map((entry) => entry.name);
            const profile = action === 'synchronize'
              ? labels.includes('run-full') ? 'full' : null
              : label === 'run-full' ? 'full' : label === 'run-quick' ? 'quick' : null;
            if (!profile) {
              core.setFailed(`Unsupported verification label: ${label}`);
              return;
            }
            const headSha = process.env.VERIFY_HEAD;
            const { data: baseBranch } = await github.rest.repos.getBranch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              branch: context.payload.pull_request.base.ref
            });
            const baseSha = baseBranch.commit.sha;
            const { data: comparison } = await github.rest.repos.compareCommitsWithBasehead({
              owner: context.repo.owner,
              repo: context.repo.repo,
              basehead: `${baseSha}...${headSha}`
            });
            if (comparison.behind_by !== 0) {
              core.setFailed(`PR head ${headSha} is behind current base ${baseSha} by ${comparison.behind_by} commit(s); update the branch before verification.`);
              return;
            }
            const statusContext = `sec-verification/${profile}/ci-verification-v3/base-${baseSha}`;
            const { data: statuses } = await github.rest.repos.listCommitStatusesForRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: headSha,
              per_page: 100
            });
            const trustedSuccess = statuses.some((status) => (
              status.context === statusContext && status.state === 'success'
            ));
            core.setOutput('profile', profile);
            core.setOutput('head', headSha);
            core.setOutput('base', baseSha);
            core.setOutput('skip', trustedSuccess ? 'true' : 'false');
            if (trustedSuccess) {
              core.notice(`Trusted ${statusContext} success already exists for ${headSha}; expensive verification skipped.`);
            }

      - name: Checkout exact PR head
        if: steps.verification.outputs.skip != 'true'
        uses: actions/checkout@v5
        with:
          ref: ${{ steps.verification.outputs.head }}
          fetch-depth: 0

      - name: Setup Bun
        if: steps.verification.outputs.skip != 'true'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.6

      - name: Install dependencies once
        if: steps.verification.outputs.skip != 'true'
        run: bun install --frozen-lockfile

      - name: Run exact-head verification
        if: steps.verification.outputs.skip != 'true'
        id: verify
        shell: bash
        run: bun scripts/ci-verification.ts --profile "$profile" --expected-head "$SEC_EXPECTED_HEAD_SHA"
        env:
          profile: ${{ steps.verification.outputs.profile }}
          SEC_CHANGED_BASE: ${{ steps.verification.outputs.base }}
          SEC_AFFECTED_TESTS_BASE: ${{ steps.verification.outputs.base }}
          SEC_EXPECTED_HEAD_SHA: ${{ steps.verification.outputs.head }}

      - name: Upload compact verification evidence
        id: evidence
        if: always() && steps.verification.outputs.skip != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: sec-verification-${{ steps.verification.outputs.profile }}-${{ github.event.pull_request.head.sha }}
          path: .tmp/ci-verification-evidence.json
          if-no-files-found: error
          retention-days: 7

      - name: Record exact-head verification status
        if: always() && steps.verification.outputs.skip != 'true'
        uses: actions/github-script@v7
        env:
          VERIFY_PROFILE: ${{ steps.verification.outputs.profile }}
          VERIFY_OUTCOME: ${{ steps.verify.outcome }}
          EVIDENCE_OUTCOME: ${{ steps.evidence.outcome }}
          VERIFY_HEAD: ${{ steps.verification.outputs.head }}
          VERIFY_BASE: ${{ steps.verification.outputs.base }}
        with:
          script: |
            const profile = process.env.VERIFY_PROFILE;
            const state = process.env.VERIFY_OUTCOME === 'success' && process.env.EVIDENCE_OUTCOME === 'success'
              ? 'success'
              : 'failure';
            const statusContext = `sec-verification/${profile}/ci-verification-v3/base-${process.env.VERIFY_BASE}`;
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: process.env.VERIFY_HEAD,
              state,
              context: statusContext,
              description: `${profile} ${state} for exact frozen head and current base`
            });
