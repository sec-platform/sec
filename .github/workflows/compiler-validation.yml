name: compiler-validation-legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired workflow notice
        run: echo "Retired. Use compiler-pr-validation or compiler-release-validation."
