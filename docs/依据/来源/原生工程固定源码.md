# 原生工程固定源码

> **阅读范围**：外部依据；保留原核查版本与证据边界。


本篇按实际机制保存来源或理论论证，保留各条自己的版本、前提及采用边界；它不是另一个现行产品合同。

## 大型现有工程的固定源码证据

### Code-OSS搜索、替换预览、模型编辑与保存

**记录日期：** 2026-09-10；固定提交 `3e078a39dc95d262123da38f916225a126fb1ccf`，提交时间 2026-09-09 16:47:47 UTC。来自 `microsoft/vscode` 的源码快照，不作为最新发行号或本地安装版本。

**原始定位与实际范围：** [S1—S10来源表](../../../examples/原生维护/Code-OSS搜索替换.md#source-evidence)给出十份文件的精确提交永久链接及实际读取范围：SearchModel、SearchService、FileMatch、Match、ReplaceService、BulkTextEdits、ripgrep提供者、SearchResult、TextSearchHeading、Remove/Replace动作。整仓、全工作区/其他视图入口、完整构建/测试闭包没有因这些阅读而被视为已取得。

**支持命题：** 打开模型的零命中覆盖也有意义；替换计算不能只使用截断展示串；预览是独立模型；真实模型编辑与保存为分立步骤；当前 ReplaceService 的编辑构造没有填入可选 versionId，不能以 BulkTextEdits 有可选验证推导该入口已有无条件版本保证；单项/文件/文件夹动作存在共同选择/替换/刷新与焦点路径；取消请求与实际进程停止不同。

**采用条件与排除推论：** 复用真实搜索、模型、差异、撤销与保存服务，新增准确计划、版本/范围和分项恢复接缝。源码观察不证明已发生缺陷、整产品安全性、跨文件事务、任意宿主等价、断电耐久性或 SEC 实现完成。冻结计划及强模式为 SEC 范本的设计选择，不误归为上游已承诺能力；换快照、修改提供者/保存参与者或新增消费者时重核相交合同。

**使用与维护位置：** [Code-OSS锚点](../../../examples/原生维护/Code-OSS搜索替换.md)拥有具体目标合同；[物理职责](../../架构/装配/模块与运行实例.md)、[恢复](../../运行/持久化提交与恢复.md)、[保证](../../运行/保证/要求证据与裁决.md)保留共同责任；剩余归原 [U主题接缝](../../状态/前沿与登记规则.md#codeoss-design-frontier)。没有因此引入新的公共工具或统一替换平台。
