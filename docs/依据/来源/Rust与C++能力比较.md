# Rust 与 C++：能力差额、替代路线与采用条件

> 来源与比较记录；不是独立语言规格、实现完成表或“Rust 缺少 64 项”的结论。

本篇按真实要求连接来源、替代方案与条件合同。A 编号只保留已有比较条目的对应关系，X 编号保留后来发现的独立分支；条目相交不重复计成底层能力。原早期的“全部实装”与“七构件已证明最小”均不采用。正常设计先复用已有类型、库、静态展开及精确原生供给；只有存在不能消去的观察/接口/资源差额才启用附加合同。

**资料范围。** 比较字段来自 2026-09-22 研究材料的有日期记录；保留当时引用的版本与不确定性，不把历史测试重新标为本次已运行。工具链 API 的 stable/nightly、目标支持与库可用性是可变事实，生产选择始终绑定实际取得的版本。本文不凭保存资料宣称全部来源在本次重新访问。当前复核记录置于本篇末尾。

## 逐项需求、替代与设计去向

<a id="a01"></a>
### 实现继承

**需要什么：** 复用状态与行为，向共同接口传递不同实现。

**可保留的语义区别：** 存在真实基子对象、继承实现、确定的初始化／析构与替代关系；实现复用和可替代关系可以独立约束。

**Rust 已有替代（研究记录）：** 组合字段＋trait 默认方法／泛型／dyn 分派；转发可用宏生成，组合本身不必堆分配。

**剩余差额：** 没有原生基类子对象与继承式覆写规则；转发和保持同一对象身份可能需要额外设计。

**成为硬边界的条件：** 既存框架要求由外部派生类覆写、基指针代表同一完整对象，或指定 C++ 类 ABI。

**当前取舍：** 新代码先用组合和接口；需要原生继承协议时做适配，不给普通类型默认加继承图。

**条件设计：** [子对象、继承与动态视图](../../领域/对象与控制/子对象视图与兼容构造.md)；[place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)。

**选择额外机制后可用的目标路线：** Rust：生成组合存储、基投影与必要 dispatch；不把它冒充 Rust 原生类继承。 TypeScript：生成受管对象形状与方法；prototype 只在其语义足够时使用。

**不能越过的边界：** 精确 C++ 类 ABI 另查 M14；不得隐藏额外分配。

**核验出口：** 经基视图修改状态后，派生视图观察到同一个基子对象；动态析构顺序符合合同。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [C01](#research-c01) [R06](#research-r06) [I01](#research-i01)

<a id="a02"></a>
### 多重实现继承

**需要什么：** 一个对象拥有多个有状态行为部分，并暴露多个接口。

**可保留的语义区别：** 多个基子对象分别有身份、路径、实现与访问规则；重名使用限定路径或明确 override。

**Rust 已有替代（研究记录）：** 组合多个状态字段，实现多个 trait；固定集合也可用 enum。

**剩余差额：** 多 trait 约束不是多基类子对象；基视图间转换、同名方法、整体对象借用要显式处理。

**成为硬边界的条件：** 开放插件按多个基类 ABI 操作同一对象，且禁止额外间接层或改变原生接口。

**当前取舍：** 保留多接口/组合需求；多基类对象模型只在兼容需求成立时启用。

**条件设计：** [子对象、继承与动态视图](../../领域/对象与控制/子对象视图与兼容构造.md)；[名称、访问、成员投影与 opaque 类型](../../作者/组合/访问投影与不完整类型.md)。

**选择额外机制后可用的目标路线：** Rust：独立字段存储加有类型投影；虚调用按 owner 和效果足迹借用。 TypeScript：受管多子对象形状，而不是只链一个 prototype。

**不能越过的边界：** 同类型重复基不自动合并；不能从一个 mutable 基视图重建与其他借用冲突的完整对象引用。

**核验出口：** A、B 的状态独立；二者同名方法未消歧时报错，不按声明顺序猜测。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [C01](#research-c01) [R06](#research-r06) [I01](#research-i01)

<a id="a03"></a>
### 虚继承／菱形共享基

**需要什么：** 两条访问路径必须共享一份根状态，而不是复制两份。

**可保留的语义区别：** 同一完整对象中标明共享的基节点只有一份状态和一次初始化／销毁。

**Rust 已有替代（研究记录）：** 唯一 owner 保存 Root，两条路径借用/投影到它；需要动态共享时才选择适当句柄。

**剩余差额：** 简单复制两个 base 字段不等价；共享路径的借用冲突、动态分派及布局可能增加实现负担。

**成为硬边界的条件：** 要求 C++ 虚基转换和精确子对象布局，或开放扩展下的透明跨接口访问。

**当前取舍：** 保留共享身份与别名规则；不为所有 Gift 对象默认支持虚继承。

**条件设计：** [子对象、继承与动态视图](../../领域/对象与控制/子对象视图与兼容构造.md)；[place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)。

**选择额外机制后可用的目标路线：** Rust：共享基放在唯一 owner 位置，两条投影指向同一节点。 TypeScript：逻辑共享对象节点及受管视图。

**不能越过的边界：** 共享路径的独占借用互相冲突；必须保留完整对象内共享而非跨实例共享。

**核验出口：** 左右路径读写同一个计数；构造失败与正常析构中共享基均不会销毁两次。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [C01](#research-c01) [R08](#research-r08) [R20](#research-r20)

<a id="a04"></a>
### 继承的 public/protected/private

**需要什么：** 限制基类转换、接口暴露和实现细节。

**可保留的语义区别：** 继承边控制基转换的合法调用主体及是否再导出，不混同为字段本身的公开性。

**Rust 已有替代（研究记录）：** 模块可见性、私有组合、显式公开的转换/委托入口。

**剩余差额：** 不能原样表达按继承边传播的访问关系；可能改变 API 的组织方式。

**成为硬边界的条件：** 必须源兼容既有继承家族的访问规则，而不能重构模块或包装。

**当前取舍：** 默认模块与导出规则；原生继承访问属于源适配器，不是通用新增权限系统。

**条件设计：** [子对象、继承与动态视图](../../领域/对象与控制/子对象视图与兼容构造.md)；[名称、访问、成员投影与 opaque 类型](../../作者/组合/访问投影与不完整类型.md)。

**选择额外机制后可用的目标路线：** Rust：SEC 访问检查后生成私有投影／合法导出入口。 TypeScript：SEC 编译域检查与导出封装；不宣称对恶意同进程 JS 形成安全隔离。

**不能越过的边界：** 外部原生接口无法隐藏的关系不能伪装成已封装；foreign 兼容规则另行固定。

**核验出口：** 同一基转换在派生内部可用、无权外部调用被拒绝；公开包装不能越权扩大关系。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [C01](#research-c01) [R18](#research-r18)

<a id="a05"></a>
### protected 派生家族权限

**需要什么：** 准许外部子类访问扩展点，却不向任意调用者公开。

**可保留的语义区别：** 授权依赖派生关系、访问位置和 receiver 限制，不等于同模块可见。

**Rust 已有替代（研究记录）：** 受控扩展 trait、模块重组、私有实现包装与公开的有限能力方法。

**剩余差额：** 继承家族和 module/crate 是不同授权主体；外部可扩展家族不总能由一次改可见性替代。

**成为硬边界的条件：** 库要求任意下游派生者访问受限成员，但普通用户不得获得同样入口。

**当前取舍：** 承认细粒度封装差异，按插件/扩展用例设计有限接口；不把 protected 当安全沙箱。

**条件设计：** [名称、访问、成员投影与 opaque 类型](../../作者/组合/访问投影与不完整类型.md)；[子对象、继承与动态视图](../../领域/对象与控制/子对象视图与兼容构造.md)。

**选择额外机制后可用的目标路线：** Rust：前端进行派生家族和 receiver 检查，后端只接收合法投影。 TypeScript：同一前端规则，必要封装内部表示。

**不能越过的边界：** 继承一个类不自动得到对所有任意基对象的 unrestricted 私有访问。

**核验出口：** 合法派生 receiver 访问通过；用不合格 receiver 绕过 protected 的程序被拒绝。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [C01](#research-c01) [R18](#research-r18)

<a id="a06"></a>
### friend

**需要什么：** 精确授权某函数/类型操作私有表示。

**可保留的语义区别：** 授权精确函数／类型／模板主体访问指定私有内容；授权身份和可否再委派有明确边界。

**Rust 已有替代（研究记录）：** 模块内协调、封装代理、由拥有者签发受限操作句柄/凭证；生成必要转发。

**剩余差额：** 可能要重组代码或新增受控入口；Rust 没有普遍的任意声明 friend 授权。

**成为硬边界的条件：** 固定模块边界、公开 API 与单个特权主体都不可改，且确需绕过普通私有性。

**当前取舍：** 普通封装先用已有规则；有实际授权主体差异再加局部机制，不能从 friend 推出全局 capability 平台。

**条件设计：** [名称、访问、成员投影与 opaque 类型](../../作者/组合/访问投影与不完整类型.md)；[阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)。

**选择额外机制后可用的目标路线：** Rust：编译期检查后生成受控内部入口，不向整个 crate 额外开放。 TypeScript：编译期封装与仅需的桥接入口。

**不能越过的边界：** 名称相同或反射生成不获得 friend 权限；授权不是 OS 沙箱或跨不可信代码安全保证。

**核验出口：** 被授予的 serializer 能访问，另一个同名声明与未获权的生成代码不能访问。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R18](#research-r18) [C01](#research-c01) [C14](#research-c14)

<a id="a07"></a>
### 虚函数协变返回

**需要什么：** 具体实现返回更具体对象，仍可经共同接口调用。

**可保留的语义区别：** 返回类型可在真实替代关系内收窄，保留借用寿命、所有权、动态析构器和接口效果。

**Rust 已有替代（研究记录）：** 关联类型处理静态情形；dyn 接口使用统一返回 trait object，具体入口另返回具体类型。

**剩余差额：** 单个 dyn 签名不能自动提供 C++ 类层级式协变；可能多一个适配入口或擦除具体类型。

**成为硬边界的条件：** 原生虚函数 ABI 或同一 override 的精确返回接口不可改。

**当前取舍：** 一般化接口和拥有权正确即可；源语言协变由适配保留，不扩展到可写容器的任意协变。

**条件设计：** [子对象、继承与动态视图](../../领域/对象与控制/子对象视图与兼容构造.md)；[效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**选择额外机制后可用的目标路线：** Rust：生成适配 thunk 与合格 view／拥有句柄转换。 TypeScript：视图转换并保留动态对象身份。

**不能越过的边界：** 不扩展到可写容器的无条件协变；override 不能增加未允许效果。

**核验出口：** 基调用得到合法基视图，具体调用保留派生能力；释放拥有结果调用正确析构／allocator。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [C01](#research-c01) [R06](#research-r06)

<a id="a08"></a>
### dynamic_cast 下转与横转

**需要什么：** 运行时从某接口找到同一对象的具体实现或其他接口。

**可保留的语义区别：** 基于受信完整对象、类型图、可访问和唯一子对象路径做 downcast／sibling cast。

**Rust 已有替代（研究记录）：** Any 精确 downcast；enum；显式 query-interface/投影表；可在统一对象包装中提供。

**剩余差额：** Any 不自动拥有继承图，横转和开放接口查询须登记；还要承担生命周期与表的维护。

**成为硬边界的条件：** 未知插件仍需按既定 C++ RTTI 图横转，或原生地址调整/ABI 固定。

**当前取舍：** 保留明确的接口查询协议；不要求全部对象携带继承 RTTI。

**条件设计：** [子对象、继承与动态视图](../../领域/对象与控制/子对象视图与兼容构造.md)；[foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)。

**选择额外机制后可用的目标路线：** Rust：按实例类型描述与 projection thunk 实现；不是把 Any 精确类型匹配当成完整图。 TypeScript：受管类型图与运行世代检查。

**不能越过的边界：** 不存在返回失败，路径不唯一返回歧义；不能从任意裸地址猜类型。

**核验出口：** 菱形可达 sibling cast 成功，重复歧义基和不相容类型分别给出规定结果。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R19](#research-r19) [C01](#research-c01) [I01](#research-i01)

<a id="a09"></a>
### typeid 动态类型识别

**需要什么：** 取得动态类型身份以实现诊断、分派和受检转换。

**可保留的语义区别：** 区分声明身份、实例化类型、动态完整对象和模块世代；保留层级查询需求，而非声称 Rust 没有 RTTI。

**Rust 已有替代（研究记录）：** Any/TypeId 已提供常见用途；借用型对象可用受约束标记或静态多态。

**剩余差额：** Any 的 static 约束不涵盖任意非静态借用类型；类型名称/标签也不是自动安全 downcast 证据。

**成为硬边界的条件：** 要求任意借用对象的通用原生 RTTI、继承图转换或跨 ABI 身份一致。

**当前取舍：** 撤回“Rust 没有 RTTI”；仅在真实跨模块协议处区分类型身份、生命周期与 ABI。

**条件设计：** [子对象、继承与动态视图](../../领域/对象与控制/子对象视图与兼容构造.md)；[foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)。

**选择额外机制后可用的目标路线：** Rust：按需要生成身份与类型描述；原生 Any 可用于确实匹配的子问题。 TypeScript：受管身份，不以显示名称或 JS constructor 名称作为完整类型身份。

**不能越过的边界：** 跨插件共享身份需协议；ABI 相容不能仅靠 TypeId 相等推导。

**核验出口：** 同名不同模块类型不误认，旧世代元数据不会调用已卸载代码。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R19](#research-r19) [C01](#research-c01)

<a id="a10"></a>
### 成员指针

**需要什么：** 把字段/方法选择作为值，延迟应用到不同对象。

**可保留的语义区别：** 成员数据／方法句柄保存声明和投影语义，可包含基路径调整或动态 thunk。

**Rust 已有替代（研究记录）：** 类型化 getter/setter、函数指针、闭包、trait 投影；固定字段可生成访问函数。

**剩余差额：** 没有同一套 member-pointer 类型/运算；捕获、基调整、表示大小和可组合泛型 API 可能不同。

**成为硬边界的条件：** 要求精确 C++ pointer-to-member 表示，或既存泛型接口必须原生接收这种值。

**当前取舍：** 常规访问器足够时不新造原语；确需字段投影时保留 owner/借用/不可寻址字段约束。

**条件设计：** [名称、访问、成员投影与 opaque 类型](../../作者/组合/访问投影与不完整类型.md)；[子对象、继承与动态视图](../../领域/对象与控制/子对象视图与兼容构造.md)；[布局、位域、padding 与 volatile](../../编译/目标/布局与设备访问.md)。

**选择额外机制后可用的目标路线：** Rust：类型化 projection、函数表／闭包；不承诺一律等于裸指针大小。 TypeScript：成员标识和受管访问器。

**不能越过的边界：** 非字节字段、打包字段返回 proxy；不能伪造普通对齐引用。

**核验出口：** 同一成员句柄用于合法派生对象时定位正确；不合格 owner 拒绝，位域不能直接取普通地址。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [C01](#research-c01) [R11](#research-r11) [R05](#research-r05)

<a id="a11"></a>
### 不完整类与前置声明

**需要什么：** 隐藏表示、减少依赖、递归指针关系及外部不透明对象。

**可保留的语义区别：** 先建立唯一名义身份，允许句柄／指针／引用，按值布局仅在定义完整后成立。

**Rust 已有替代（研究记录）：** Rust 通常不需源内前置声明；私有具体类型、模块封装、FFI opaque 句柄与工厂/析构入口。

**剩余差额：** 真正未知大小的 foreign 类型仍有 stable 表达边界；不能按占位零尺寸进行真实对象分配或步进。

**成为硬边界的条件：** 原生接口要求同一外来名义类型的精确按值布局，但定义并未提供。

**当前取舍：** 复用现有 opaque/模块依赖；不是为了模仿 C++ 头文件而增加前置声明系统。

**条件设计：** [名称、访问、成员投影与 opaque 类型](../../作者/组合/访问投影与不完整类型.md)；[foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)。

**选择额外机制后可用的目标路线：** Rust：不透明句柄与由真实定义方拥有的构造／析构入口。 TypeScript：不透明受管句柄；不是虚构具有某个 native size 的空对象。

**不能越过的边界：** 禁止假零尺寸外来类型及无限按值递归；无法提供布局时不生成按值 ABI。

**核验出口：** 相互指针引用可用，缺定义的按值成员被拒绝，释放由真正 owner 完成。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R21](#research-r21) [R07](#research-r07)

<a id="a12"></a>
### 函数重载集合

**需要什么：** 同名入口接受多种输入并选择具体实现。

**可保留的语义区别：** 候选域固定；自有画像按其参数、receiver、访问、约束和转换规则固定绑定。精确 C++ 输入先完成其规定的最佳可行选择，再检查所选函数的可访问性，不能先滤 private 再退选。

**Rust 已有替代（研究记录）：** 泛型函数＋trait（含 tuple 参数）、Into、不同入口名；nightly 有实验，不默认采用。

**剩余差额：** 同名多签名和 C++ 候选排序不原生；API 名称/推导/第三方参与扩展方式不同。

**成为硬边界的条件：** 原生调用语法必须不变，或库要求无需合作即可按任意输入参与自动重载。

**当前取舍：** 作者便利可在解析期降为确定调用；是否提供重载按可用性测试决定，非系统完备性必需。

**条件设计：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[特化、结构探测与静态分支](../../编译/特化与结构查询.md)。

**选择额外机制后可用的目标路线：** Rust：生成唯一符号；导出原生 Rust API 时提供实际可表达的入口，而非假同名重载。 TypeScript：明确调用绑定／运行分派；TS overload 声明本身不产生多个 runtime 实现。

**不能越过的边界：** 不可比较最优候选是歧义；后端不能再次选择，不能只依赖返回类型消歧。

**核验出口：** 同参数的不同约束只有在所选调用画像能建立严格特化关系时才唯一选择；否则保留歧义。两个分别在不同参数更优的候选也报歧义。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R17](#research-r17) [R04](#research-r04) [C16](#research-c16)

<a id="a13"></a>
### receiver 的 cv/ref 重载

**需要什么：** 按只读/可变/消费上下文选择借用或转移结果。

**可保留的语义区别：** 根据共享／独占／消费 receiver 及 place 属性选择；选择同时决定借用和效果。

**Rust 已有替代（研究记录）：** &self/&mut self/self 和明确命名的方法；trait/辅助函数携带 receiver 类型。

**剩余差额：** 不具备相同的同名 overload set；同一调用拼写下自动选择借用类别的 API 可能改变。

**成为硬边界的条件：** 必须保持既有 fluent API 或由通用表达式精确选择值类别。

**当前取舍：** 保持 receiver 拥有权语义；同名便利可局部语法降低，不必复制全部 C++ cv/ref 体系。

**条件设计：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)。

**选择额外机制后可用的目标路线：** Rust：不同内部方法名，保留 &、&mut、按值要求。 TypeScript：生成不同受管调用，消费式操作更新线性状态。

**不能越过的边界：** 不能为了匹配候选复制不可复制对象或让消费后的对象继续可用。

**核验出口：** 共享 receiver 返回只读视图，独占 receiver 返回可变视图，消费 receiver 转交所有权。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R05](#research-r05) [R17](#research-r17)

<a id="a14"></a>
### explicit object parameter / deducing this

**需要什么：** 避免重复定义，并以 receiver 的实际类型和类别参与推导。

**可保留的语义区别：** receiver 是可参与泛型推导的显式参数；支持指针／视图等已定义投影协议。

**Rust 已有替代（研究记录）：** trait Self、普通泛型函数；稳定 receiver 已包括 Box/Rc/Arc/Pin 等，绝不只限三种 self。

**剩余差额：** 任意自定义 receiver 与 C++ 式统一推导仍非同一个表面；函数形式可能不同。

**成为硬边界的条件：** 下游原生方法语法或精确 receiver 转换协议被固定。

**当前取舍：** 先用显式参数与目标已有 receiver；撤回把 deducing-this 等同 arbitrary-self-types 的粗糙比较。

**条件设计：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)。

**选择额外机制后可用的目标路线：** Rust：降为普通泛型／具体函数，不依赖 arbitrary-self-types 才能完成内部调用。 TypeScript：显式 receiver 参数和受管投影。

**不能越过的边界：** receiver 适配必须保留寿命和拥有权；原生目标方法语法要求单独检查。

**核验出口：** 同一泛型对象函数适用于拥有值、共享视图与合格智能句柄，不制造悬空返回。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R05](#research-r05) [R06](#research-r06)

<a id="a15"></a>
### 默认参数

**需要什么：** 减少常用调用的重复配置，并定义缺省值。

**可保留的语义区别：** 省略与显式空值不同；默认表达式按定义域和固定版本解释，只在参数省略时执行。

**Rust 已有替代（研究记录）：** builder、配置 Default、额外入口、宏或显式 Option 策略。

**剩余差额：** 入口数/调用字数改变；省略和显式空值不可混淆；默认表达式副作用和 API 版本需保留。

**成为硬边界的条件：** 目标原生调用语法不能改变；否则通常是设计便利而非底层控制缺失。

**当前取舍：** 可作为作者糖或库 API；不因为 C++ 有它就创建新语义子系统。

**条件设计：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**选择额外机制后可用的目标路线：** Rust：调用点补齐或生成 wrapper，保留求值顺序与效果。 TypeScript：按规定插入默认求值，不直接借宿主 undefined 规则冒充全部语义。

**不能越过的边界：** 省略判断、作用域、早期参数引用及默认效果参与检查；源 C++ 兼容顺序按对应 profile。

**核验出口：** 显式传参不触发默认分配；省略时只执行一次；null／None 不被当作省略。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R17](#research-r17) [C15](#research-c15)

<a id="a16"></a>
### 用户定义隐式转换

**需要什么：** 适配输入类型，减少样板代码。

**可保留的语义区别：** 默认仅纯、无失败且已证明无损的转换；局部兼容模式可允许声明效果的用户转换。

**Rust 已有替代（研究记录）：** From/Into、AsRef、泛型形参，以及语言已有的 deref/coercion；不是 Rust 完全没有隐式转换。

**剩余差额：** 任意用户转换不会像 C++ 那样自动加入全局重载决议；需调整函数入口/显式调用。

**成为硬边界的条件：** 保持原生源调用或既有重载匹配行为是硬要求。

**当前取舍：** 默认显式资源/失败语义；是否允许受限转换由使用性决定，不自动复制有隐式成本的规则。

**条件设计：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**选择额外机制后可用的目标路线：** Rust：在已绑定调用中显式生成转换，效果向调用者传播。 TypeScript：同样显式生成，不依赖 JS 隐式 coercion。

**不能越过的边界：** 限定候选域和转换深度；歧义拒绝；不能丢失 volatile、精度或借用约束。

**核验出口：** 合格转换自动采用；两个不可比较转换报错；可能抛出／分配的转换出现在效果合同中。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R05](#research-r05) [R17](#research-r17) [C16](#research-c16)

<a id="a17"></a>
### 自定义赋值 operator=

**需要什么：** 对已有对象赋值时复用资源、更新代理或维护不变量。

**可保留的语义区别：** 赋值可绑定 assign_from，但保持 replace、复制赋值、转移赋值语义可区分。

**Rust 已有替代（研究记录）：** clone_from、自定义 assign/replace 方法；一般值替换仍由 Rust 原生赋值和 Drop 完成。

**剩余差额：** 只缺把任意协议挂在 = 上；不能说 Rust 无法复用旧资源。

**成为硬边界的条件：** 外部泛型算法必须以 = 调用你的自定义协议，且不能改为 trait 方法。

**当前取舍：** 保留替换与内容更新的差别；显式操作默认足够。与45合并计成本，不重复累计缺口。

**条件设计：** [place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)。

**选择额外机制后可用的目标路线：** Rust：生成显式赋值协议调用，不声称 Rust 原生 = 可重载。 TypeScript：同一受管操作，不用普通 JS 引用赋值替代内容赋值。

**不能越过的边界：** 自赋值、别名、源失效和强／基本失败保证明确；外部原生语法是另一要求。

**核验出口：** 资源赋值调用指定 hook；自赋值不双重释放；失败后对象状态符合承诺。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R10](#research-r10) [R11](#research-r11)

<a id="a18"></a>
### 任意用户类型 operator()

**需要什么：** 对象携带状态，同时可交给接受 callable 的接口。

**可保留的语义区别：** 任意定义了合法 call 协议的值可在 SEC 中用调用形式，区分共享、可变、一次性调用。

**Rust 已有替代（研究记录）：** 闭包包装、显式 call 方法或自定义 Call trait；原生闭包可满足 Fn/FnMut/FnOnce。

**剩余差额：** stable 不能普遍手写 Fn 系列 impl；包装后的类型身份、自动 trait、借用及泛型接口可能不同。

**成为硬边界的条件：** 要求原对象直接满足外部 Fn 接口且不能增加适配层/改变暴露类型。

**当前取舍：** call 语法可以下降为方法；保留导出原生 callable 的边界，不为普通闭包重造调用框架。

**条件设计：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**选择额外机制后可用的目标路线：** Rust：内部降为方法；需要 Fn API 时导出合格闭包适配器，不假称原类型已实现 stable 自定义 Fn。 TypeScript：输出函数对象／显式调用适配器并保持捕获状态。

**不能越过的边界：** 一次性 callable 消费后不得再调；捕获借用不能逃逸，外部 API 类型身份变化明确。

**核验出口：** 有状态 callable 多次累积正确；FnOnce 式值第二次调用被拒绝，任意外部 JS 调用者也不能绕过公开边界的必要运行检查。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R12](#research-r12) [R05](#research-r05)

<a id="a19"></a>
### 额外运算符与代理下标

**需要什么：** 自定义数值、容器视图、位引用、位置代理及等待操作。

**可保留的语义区别：** 涵盖赋值、调用、索引、箭头／成员投影、逗号、取址、前后增减、new/delete、co_await、逻辑及算术操作。

**Rust 已有替代（研究记录）：** Add 等原生 traits、get/set、借用代理、显式状态操作、Future/awaiter 适配。

**剩余差额：** 尤其 Index 返回引用，不能普遍返回临时 value/lending proxy；packed bit 的 b[i]=v 不是普通 &mut bool。++/逗号/箭头等多属拼写，不能混成一项不可替代控制。

**成为硬边界的条件：** 算法固定以原生下标/赋值处理代理结果，或需保留特殊求值顺序、eager逻辑等源语义。

**当前取舍：** 保留精确代理/借用/求值需求；先显式方法或局部糖，别把每个运算符升级为核心原语。

**条件设计：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[分配器、资源域与 TLS](../../运行/分配域与退出责任.md)；[协程、取消与 CFG](../../领域/对象与控制/协程帧与控制流降低.md)。

**选择额外机制后可用的目标路线：** Rust：各自降为已绑定函数／投影／分配／协程操作。 TypeScript：同样语义展开，不假设 JS 可重载全部运算符。

**不能越过的边界：** 短路逻辑与 C++ eager overload 分开；前后增量、index proxy、箭头递归终止和取址权限单独定义。

**核验出口：** 短路分支不求值，eager 兼容操作求值；后增量保留旧值语义；proxy 写入命中实际存储。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R11](#research-r11) [R12](#research-r12) [C07](#research-c07)

<a id="a20"></a>
### 用户字面量后缀

**需要什么：** 单位、编译期解析、精确大数/DSL 常量。

**可保留的语义区别：** 字面量扩展在局部命名域中消费原始 token／字节，编译期产生有类型结果。

**Rust 已有替代（研究记录）：** 声明宏/过程宏、const 构造函数、类型化单位库。

**剩余差额：** 不同拼写；过程宏对任意外部类型无通用语义查询。原始大数 token 可保留，不必先变 f64。

**成为硬边界的条件：** 必须兼容既有后缀语法或无宏入口的第三方常量协议。

**当前取舍：** 字面量/DSL 属作者层可选便利；保留解析错误与数值精度，不是新系统控制缺口。

**条件设计：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)。

**选择额外机制后可用的目标路线：** Rust：预计算常量或生成普通构造；不依赖 Rust 原生支持新后缀。 TypeScript：同一路线，整数原 token 不先转换成有损 Number。

**不能越过的边界：** 解析错误定位原 literal；无授权元程序不能联网读文件；名称冲突诊断。

**核验出口：** 超大整数 literal 无精度丢失；非法单位／字符串给出编译期位置；副作用不在后台发生。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R13](#research-r13) [R14](#research-r14)

<a id="a21"></a>
### ADL

**需要什么：** 泛型算法找到参数类型拥有者提供的定制行为。

**可保留的语义区别：** 参数类型可以公开有版本的定制点候选；候选来源显式且有界，不扫描全生态。

**Rust 已有替代（研究记录）：** trait 的显式定制点、本地新类型、传入策略/witness、拥有者公开接口。

**剩余差额：** 名义参与和孤儿规则约束第三方扩展；不能按参数 namespace 自动吸收同名自由函数。

**成为硬边界的条件：** 需要对不合作的外部类型与外部算法做全局透明扩展，不能包装/传策略。

**当前取舍：** 设计明确的定制入口；不移植整套 C++ 查找算法作为默认。也不声称 C++ 开放扩展不受 ODR/可见性约束。

**条件设计：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[特化、结构探测与静态分支](../../编译/特化与结构查询.md)。

**选择额外机制后可用的目标路线：** Rust：前端决定最终目标，生成固定符号。 TypeScript：同样固定绑定，而非运行时按同名属性猜方法。

**不能越过的边界：** 新导入不悄悄改变旧绑定；要求精确 C++ ADL／hidden friend 时由其 compatibility profile 解释。

**核验出口：** 显式启用的 swap 定制被找到；无关模块同名函数不抢占调用。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R04](#research-r04) [C04](#research-c04) [C16](#research-c16)

<a id="a22"></a>
### 类模板推导指南

**需要什么：** 由构造输入推导经过变换的结果类型。

**可保留的语义区别：** 用户定义构造输入到模板实参的类型函数和约束，与普通值构造分离。

**Rust 已有替代（研究记录）：** 泛型工厂、关联类型、From 实现、返回 impl Trait，配合正常类型推导。

**剩余差额：** 不能给任意目标泛型类型注册同样的原生 deduction-guide；API 入口或类型可命名性可能变。

**成为硬边界的条件：** 既存构造语法/推导结果身份不可改。

**当前取舍：** 先工厂和推导规则；这不是运行时缺失，也不必另建全局推导指南注册表。

**条件设计：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)。

**选择额外机制后可用的目标路线：** Rust：SEC 推导后发出显式类型参数或具体实例。 TypeScript：生成已确定的类型和构造函数。

**不能越过的边界：** guide 不得伪造不满足类型约束的实例；多条不可比较 guide 报歧义。

**核验出口：** 构造输入被映射到变换后的泛型类型；冲突 guide 拒绝而非任取一个。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R05](#research-r05) [C04](#research-c04)

<a id="a23"></a>
### decltype / decltype(auto)

**需要什么：** 精确取未求值表达式的类型，并在类型计算/返回中继续使用。

**可保留的语义区别：** 分别提供 type_of(expression) 与 place_info(expression)；后者保留借用／可变性／消费类别。

**Rust 已有替代（研究记录）：** 局部推导、关联 Output、泛型辅助函数、impl Trait；已有 trait 明确暴露的结果类型可组合。

**剩余差额：** stable 没有普遍表达式 typeof；任意匿名类型精确可命名、引用类别传播/别名定义不完全可替代。

**成为硬边界的条件：** 编写通用库必须在类型位置复用任意表达式类型，且不允许改写为新 trait/辅助入口。

**当前取舍：** 编译器内部类型查询本来必要；是否向作者开放为语法/API，是独立决定。

**条件设计：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)；[阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)。

**选择额外机制后可用的目标路线：** Rust：先在 SEC 中求得类型信息，再发出目标类型或推导友好代码。 TypeScript：同样在编译期完成，不把 typeof runtime 字符串当成精确类型。

**不能越过的边界：** 未求值查询不执行表达式；不能把值类型等同于引用种类并丢失寿命。

**核验出口：** 查询带副作用调用的类型不会真的调用；按值返回与借用返回保持区别。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R05](#research-r05) [R14](#research-r14)

<a id="a24"></a>
### 类型参数包

**需要什么：** 泛型处理任意实际有限长度的异构序列。

**可保留的语义区别：** 类型 pack 是 kind 明确的编译期序列，支持任意实际有限长度及受控展开。

**Rust 已有替代（研究记录）：** HCons/HNil 递归异构列表与递归 traits；宏改善写法。不是只能手工生成到16项。

**剩余差额：** native tuple/function 参数包接口不同；推导、诊断、trait 深度、编译时间可能更差，需实际测量。

**成为硬边界的条件：** 外部库要求原生任意 arity 函数/tuple 接口；或编码的编译负担超出真实预算。

**当前取舍：** 有反复用例可统一为 pack；否则复用列表与生成。不给所有程序强加变参泛型设计。

**条件设计：** [kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)；[实例化、分发与链接](../../编译/目标/泛型分发与实例归属.md)。

**选择额外机制后可用的目标路线：** Rust：展开为具体字段／参数／实例，不依赖固定 16 元 tuple 宏上限。 TypeScript：生成真实所需成员与签名。

**不能越过的边界：** 没有语义性固定长度上限，但编译资源不足返回预算结果；不是无限算力承诺。

**核验出口：** 空 pack、单元素及超过既定宏常见上限的 pack 都有正确生成／资源诊断。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [L01](#research-l01) [R04](#research-r04)

<a id="a25"></a>
### 值参数包

**需要什么：** 把编译期值序列用于类型/算法生成。

**可保留的语义区别：** 值 pack 的元素类型、常量身份和序列顺序参与实例化。

**Rust 已有替代（研究记录）：** 类型级数字/标记组成 HList，关联常量，宏/代码生成；同质运行数据另用数组。

**剩余差额：** 原生 const 值域受限，数组/运行循环不能自动保留每项参与类型身份的语义。

**成为硬边界的条件：** 任意结构值/符号序列必须原生成为公开模板身份，或需要未合作外部泛型接口。

**当前取舍：** 区分值序列计算与类型身份；只在实际输出依赖时保留编译期参数，不整套复制 NTTP。

**条件设计：** [kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)；[阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)。

**选择额外机制后可用的目标路线：** Rust：SEC 求值后生成常量和具体代码。 TypeScript：同一路线，精度按 SEC 格式保持。

**不能越过的边界：** 运行期未知值不能伪装成常量；无限／超预算展开不回退 runtime。

**核验出口：** 值序列不同导致必要实例差别；空 pack 和非法常量分别得到规定结果。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R03](#research-r03) [L01](#research-l01) [L02](#research-l02)

<a id="a26"></a>
### template-template 参数和包

**需要什么：** 抽象容器/类型构造器本身，再应用到不同类型。

**可保留的语义区别：** 类型构造器以 Type→Type 等 kind 参与传参与 pack；参数形状在应用时检查。

**Rust 已有替代（研究记录）：** GAT witness，例如 Family::Of<T>；显式策略类型组合，也可递归成包。

**剩余差额：** 需要适配标记/关联类型协议；不是任意现存泛型名字可直接作为模板参数传入。

**成为硬边界的条件：** 下游通用库必须无适配地接受任意外部类型构造器，或要求原生签名完全一致。

**当前取舍：** GAT 式设计足够则复用；作者高阶类型便利不自动意味着再造完备 kind calculus。

**条件设计：** [kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)；[实例化、分发与链接](../../编译/目标/泛型分发与实例归属.md)。

**选择额外机制后可用的目标路线：** Rust：具体应用在 SEC 实例化，或在合格场景降为 witness／关联类型。 TypeScript：生成具体类型／构造应用。

**不能越过的边界：** 错误 arity／kind 立即诊断；公开无限泛型 API 不能只分发若干已生成实例。

**核验出口：** 把两种容器构造器应用于同一元素类型得到不同合格实例；错 kind 被拒绝。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R05](#research-r05) [L01](#research-l01)

<a id="a27"></a>
### fold 表达式

**需要什么：** 对异构包做有定义顺序的归约。

**可保留的语义区别：** 左／右 fold、运算符、初始值、求值顺序与短路行为均显式定义。

**Rust 已有替代（研究记录）：** HList 递归 fold、宏展开；同质数据直接 Iterator::fold。

**剩余差额：** 异构包接口有编码成本；不能把同质循环冒充类型变化的 fold；求值/短路/结合顺序须保持。

**成为硬边界的条件：** 必须原生包语法，或类型级归约规模使替代方案失去可用性。

**当前取舍：** 通常复用泛型序列机制；不是在24之外又一个独立机器能力。

**条件设计：** [kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)；[重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)。

**选择额外机制后可用的目标路线：** Rust：生成相应嵌套表达式或等价循环，保持效果顺序。 TypeScript：同样展开，不让宿主 reduce 默认改变结合方向。

**不能越过的边界：** 空 pack 无合法单位元时报错；浮点／带效果操作不得任意重结合。

**核验出口：** 减法左折叠和右折叠产生各自预期结果；逻辑 fold 保持短路。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [L01](#research-l01) [R04](#research-r04)

<a id="a28"></a>
### pack indexing

**需要什么：** 从编译期异构序列中按位置取类型/值。

**可保留的语义区别：** 对 pack 进行静态索引、切片、zip 和映射；结果的类型／值由实例化确定。

**Rust 已有替代（研究记录）：** 递归索引 trait、HList selector/pluck、已知 tuple 字段或代码生成。

**剩余差额：** 没有同一原生静态索引表达式；类型级索引协议和诊断更复杂。

**成为硬边界的条件：** 要求对外暴露原生通用 pack 索引，且不能接受另一套表示。

**当前取舍：** 作为包/反射的派生操作；C++26支持按实际编译器核验，不混入已成熟C++23比较。

**条件设计：** [kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)；[阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)。

**选择额外机制后可用的目标路线：** Rust：生成直接成员访问或具体类型引用。 TypeScript：同样生成静态成员，必要保留源位置。

**不能越过的边界：** 索引越界、负长度、zip 长度冲突报具体错误；不偷偷补默认类型。

**核验出口：** 选取异构序列第 k 项得到正确类型；越界在编译期拒绝。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [L01](#research-l01) [T01](#research-t01)

<a id="a29"></a>
### 广义非类型模板参数

**需要什么：** 以常量内容/符号身份区分类型实例、进行静态选择。

**可保留的语义区别：** 整型、枚举、结构值、浮点位表示、符号／成员引用等可按明确规范形成常量身份。

**Rust 已有替代（研究记录）：** const 泛型受支持标量；类型标记＋关联常量、宏或具体实例生成覆盖更多用例。

**剩余差额：** 任意结构/浮点/指针类参数缺少同一原生身份与自动推导规则；标记相等性未必等于值相等性。

**成为硬边界的条件：** 开放原生 API 必须按这种值身份自动匹配，或类型计算无法被有限提前展开。

**当前取舍：** 保留真正需要的形状/值身份；不要默认纳入全部浮点NaN与符号指针模板规则。

**条件设计：** [kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)；[阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)；[foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)。

**选择额外机制后可用的目标路线：** Rust：超出原生 const-generic 值域时在 SEC 实例化，不把它直接交给 rustc。 TypeScript：同样生成唯一实例与必要常量。

**不能越过的边界：** 不得用构建宿主地址作可复用身份；NaN、带符号零和符号引用有明确定义。

**核验出口：** 结构常量可稳定重建；不同符号引用不混同；NaN bit policy 导致的实例身份符合规则。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R03](#research-r03) [C02](#research-c02)

<a id="a30"></a>
### 泛型常量表达式进入类型

**需要什么：** 矩阵/数组/缓冲区大小随 N 计算且仍静态内联。

**可保留的语义区别：** 允许数组尺寸等位置依赖 N*2+1 等合格表达式，具体化后检查整数／布局约束。

**Rust 已有替代（研究记录）：** generic-array/typenum＋关联存储；较简单嵌套数组也能表达部分需求；已知N先生成具体数组。

**剩余差额：** 原生 [T;2*N+1] 等 stable 受限；替代签名、推导和类型运算负担不同。不能谎称只能Vec/堆。

**成为硬边界的条件：** 原生 [T;expr] 公开 API 被固定，或编码复杂度/编译时间实际不合格。

**当前取舍：** 静态尺寸有真实价值；先常量求值/库映射，在高频需求下再扩展作者表达。

**条件设计：** [kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)；[阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)；[布局、位域、padding 与 volatile](../../编译/目标/布局与设备访问.md)。

**选择额外机制后可用的目标路线：** Rust：实例化得到具体数组长度，或采用目标确实可表达的泛型形式。 TypeScript：生成实际容量和相应检查，不以类型擦除替代尺寸要求。

**不能越过的边界：** 未知参数保留未决；溢出、负尺寸、不满足目标上限拒绝；不把未知改为零。

**核验出口：** N=3 产生 7 项结构；溢出实例诊断；未具体化的公开定义仍保持泛型义务。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R03](#research-r03) [R16](#research-r16) [L02](#research-l02)

<a id="a31"></a>
### 完全特化

**需要什么：** 为具体类型/值采用不同实现，甚至不同关联表示。

**可保留的语义区别：** 特定参数可选择不同类型定义；实现优化特化则必须保持公共合同，两种用途分开。

**Rust 已有替代（研究记录）：** 互不重叠的具体 impl、关联类型和显式策略；不是“任何具体类型特例都不支持”。

**剩余差额：** 有通用 blanket impl 后再覆盖特例是稳定限制；结构体自身字段集也不能直接重定义。

**成为硬边界的条件：** 必须保留通用默认＋任意具体例外的开放接口或不同实体定义。

**当前取舍：** 区分不重叠实现、语义类型族与等价优化；只有后两者确需时才引入相应规则。

**条件设计：** [特化、结构探测与静态分支](../../编译/特化与结构查询.md)；[kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)。

**选择额外机制后可用的目标路线：** Rust：已选分支生成独立目标类型／函数，不依赖重叠 trait impl。 TypeScript：同样具体生成。

**不能越过的边界：** 跨实例布局可不同但不自动 ABI 相容；调用者不能假定统一字段集合。

**核验出口：** 一般 X<T> 与专门 X<int> 可有不同字段；错误共用布局的访问被拒绝。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R04](#research-r04) [R15](#research-r15) [C03](#research-c03)

<a id="a32"></a>
### 部分特化

**需要什么：** 按指针/容器/数组等参数结构选择定义。

**可保留的语义区别：** 按参数结构和约束进行模式匹配，保留偏序与唯一最具体实例。

**Rust 已有替代（研究记录）：** impl<T> Trait for Vec<T> 等模式实现完全可行，只要不重叠；关联存储可改变内部表示。

**剩余差额：** 不能直接等同于 C++ 的重叠候选加更专门者优先。C++ 函数模板本身也不能部分特化。

**成为硬边界的条件：** 需要重叠结构模式自动排序、公开字段/类型族变化，且不能分离策略。

**当前取舍：** 撤回“Rust 没有部分模式实现”；真正审查重叠与定义变化，不按关键词裁决。

**条件设计：** [特化、结构探测与静态分支](../../编译/特化与结构查询.md)；[kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)。

**选择额外机制后可用的目标路线：** Rust：SEC 先解析再输出无冲突实例／显式 witness。 TypeScript：同样处理，不依赖动态 typeof 替代结构模式。

**不能越过的边界：** 模式重叠但无法证明更具体则歧义；不能把求解超时当作不匹配。

**核验出口：** 指针／数组等结构匹配正确；不可比较的交叉模式报错。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R04](#research-r04) [R15](#research-r15) [C03](#research-c03)

<a id="a33"></a>
### 重叠候选按专门程度选择

**需要什么：** 默认实现加高性能/特定语义例外，并允许组合扩展。

**可保留的语义区别：** 在有界 family／owner 或显式 witness 下允许重叠，通过已证明蕴含关系找唯一候选。

**Rust 已有替代（研究记录）：** 显式策略参数、sealed 分类、确定入口、作者生成选择；nightly specialization 有限制。

**剩余差额：** 策略显式化改变开放泛型 API；autoref 等技巧不能普遍在泛型单态化时重新发现所有更具体实现。

**成为硬边界的条件：** 要求不改调用点、任意新类型可合作/不合作地加入选择，且全局唯一性必须维持。

**当前取舍：** 优化分支可放优化器；语义差异必须在明示候选域/witness绑定中表达。不能默认全局开放特化。

**条件设计：** [特化、结构探测与静态分支](../../编译/特化与结构查询.md)；[重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[实例化、分发与链接](../../编译/目标/泛型分发与实例归属.md)。

**选择额外机制后可用的目标路线：** Rust：不直接生成 Rust 禁止的重叠 impl；用已绑定实例或字典参数。 TypeScript：同样绑定，导入／插件加载不重新解释旧调用。

**不能越过的边界：** 开放世界缺少实现不构成负证明；不能依赖被擦除的寿命分派后又合并代码。

**核验出口：** 有限重叠可消歧；开放插件不改变已绑定行为；无法证明的选择报告 unknown／ambiguous。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R04](#research-r04) [R15](#research-r15) [C03](#research-c03)

<a id="a34"></a>
### SFINAE/requires 结构式探测

**需要什么：** 询问任意类型是否有某表达式/成员，从而选择实现。

**可保留的语义区别：** 在受控上下文探测表达式、关联类型、调用及效果是否成立，无须先有显式名义 trait。

**Rust 已有替代（研究记录）：** 显式trait约束/适配、derive、已知类型的过程生成；有类型就可以在约定接口内调用。

**剩余差额：** 无法普遍对不参与协议的未知外部T做结构探测和自动fallback；token宏不是类型查询器。

**成为硬边界的条件：** 公共通用库必须接受未来未适配类型并按成员存在性分派。

**当前取舍：** 这是比没有默认参数更实质的抽象限制；Gift有需要时用已有语义查询，不必因此照搬SFINAE所有历史规则。

**条件设计：** [特化、结构探测与静态分支](../../编译/特化与结构查询.md)；[阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)。

**选择额外机制后可用的目标路线：** Rust：SEC 探测后生成实际选定代码。 TypeScript：同样在编译期进行，而不是运行期试错调用。

**不能越过的边界：** 不适用、未知、歧义、预算耗尽和编译器故障分开；不能吞掉内部错误选择 fallback。

**核验出口：** 缺 foo 的类型进入合法 fallback；探测超预算不给 false；私有 foo 不绕过权限。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R04](#research-r04) [R06](#research-r06) [R13](#research-r13) [C04](#research-c04)

<a id="a35"></a>
### if constexpr 丢弃依赖分支

**需要什么：** 不同T选不同合法代码，不要求每个T满足两边操作。

**可保留的语义区别：** 静态条件选定后，仅实例化选中分支的依赖语义；语法与非依赖前提仍须合格。

**Rust 已有替代（研究记录）：** 互不重叠trait实现、宏/生成、明确的类型策略；cfg只解决构建配置而非任意T。

**剩余差额：** 普通if两边要类型正确；复杂重叠条件需要额外策略，不能靠优化器删掉类型错误。

**成为硬边界的条件：** 一个开放泛型定义必须按未知T的结构探测自动选依赖分支。

**当前取舍：** 可在实例化/生成阶段解决；先复用分派，避免把它误称CPU控制能力。

**条件设计：** [特化、结构探测与静态分支](../../编译/特化与结构查询.md)；[阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)。

**选择额外机制后可用的目标路线：** Rust：只生成选中分支，避免让 rustc 检查不属于该实例的代码。 TypeScript：同样生成，不输出两侧并依赖 TS any 掩盖错误。

**不能越过的边界：** 未知条件不能自动成为 runtime if；未选分支不是任意损坏语法的避难所。

**核验出口：** 针对 int 的分支无需具备 string-only 方法；无法解析的分支仍诊断。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R04](#research-r04) [R16](#research-r16) [C04](#research-c04)

<a id="a36"></a>
### 显式模板实例化

**需要什么：** 决定某实例的构建/导出归属，控制重复工作。

**可保留的语义区别：** 作者／库拥有者可以固定某组实例的产生位置、参数、语义版本和导出身份。

**Rust 已有替代（研究记录）：** 具体包装函数、crate边界、真实引用和导出符号、代码生成选项。

**剩余差额：** 源作者没有同级任意模板实体实例化指令；机器码布局/内联保留仍由工具链决定。

**成为硬边界的条件：** 库发布要求精确的实例提供者/符号/构建归属，或编译资源有测得瓶颈。

**当前取舍：** 交给构建/导出策略，不把编译组织变成运行时语言机制。

**条件设计：** [实例化、分发与链接](../../编译/目标/泛型分发与实例归属.md)；[kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)。

**选择额外机制后可用的目标路线：** Rust：生成专门目标成员与真实引用；不声称可任意控制 rustc 每个内部代码生成细节。 TypeScript：生成固定函数／模块成员。

**不能越过的边界：** 同身份的冲突定义拒绝；要求精确 native 符号／链接策略时由工具能力验证。

**核验出口：** 指定实例有唯一合格 owner，重复相同请求复用，冲突定义不被任意覆盖。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R33](#research-r33) [C03](#research-c03)

<a id="a37"></a>
### extern template

**需要什么：** 多个消费者共用已产生实例，避免重复实例化。

**可保留的语义区别：** 下游明确消费外部提供的已固定实例，检查提供者、符号、类型、语义与 ABI。

**Rust 已有替代（研究记录）：** 导出非泛型具体入口，消费者链接/调用；跨crate单态化和LTO另有工具策略。

**剩余差额：** 非泛型包装不等于保留同一公开泛型API且只抑制代码生成；可能改变可内联性和分发形式。

**成为硬边界的条件：** 必须直接保留原生泛型接口、外部实例所有权和稳定构建行为。

**当前取舍：** 明确有限实例、可继续实例化定义和原生泛型三类交付；按真实构建成本采用。

**条件设计：** [实例化、分发与链接](../../编译/目标/泛型分发与实例归属.md)；[foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)。

**选择额外机制后可用的目标路线：** Rust：生成真实外部引用或合格公共模块调用。 TypeScript：导入固定生成成员，不重跑同一生成器。

**不能越过的边界：** 缺失实例、版本不匹配或仅有声明不能被当成功；仍需真实链接／装载证据。

**核验出口：** 消费者不重复生成实例；缺少 provider 给准确缺项，错误 ABI 被拒绝。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R33](#research-r33) [C03](#research-c03)

<a id="a38"></a>
### consteval

**需要什么：** 接口保证调用只能发生在编译期。

**可保留的语义区别：** compile_only 函数的合格调用必须在编译阶段完成，不允许无声明 runtime fallback。

**Rust 已有替代（研究记录）：** const上下文/inline const、宏和隐藏实现的编译期入口；const fn本身允许运行时调用。

**剩余差额：** 没有完全同样的函数级 immediate 调用协议；调用形式和可组合函数值不同。

**成为硬边界的条件：** 要求函数名在任意正常调用处强制编译期，并参与开放泛型组合。

**当前取舍：** 编译期执行合同有用，但只对确实需要的调用生效；不要为简单常量计算复制整套历史constexpr机制。

**条件设计：** [阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)；[效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**选择额外机制后可用的目标路线：** Rust：将结果生成成常量／代码，不留下普通 runtime 调用。 TypeScript：同样预计算并保持目标数值规则。

**不能越过的边界：** 运行期实参、禁止效果、资源耗尽或非终止都阻断该调用的生成。

**核验出口：** 字面量解析在编译时完成；传入运行期字符串编译失败而非偷偷延后执行。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R14](#research-r14) [C02](#research-c02)

<a id="a39"></a>
### 更广泛泛型 constexpr

**需要什么：** 同一泛型算法和库行为能在编译期执行，包括合格临时容器。

**可保留的语义区别：** witness 同时带类型、效果与可执行阶段，泛型行为可在满足条件时参与 const-eval。

**Rust 已有替代（研究记录）：** Rust const fn/const上下文；已知输入通过生成器预计算；部分const友好数据结构。

**剩余差额：** trait函数const能力/可用库操作仍有边界；外部build脚本不是目标类型上下文中的透明const-eval。

**成为硬边界的条件：** 需要通用库操作在目标语义下静态运行、类型检查一致，不能另写host脚本/复制算法。

**当前取舍：** 保留阶段和目标数值输入；先做真实所需操作，不默认开放无界元程序/整个编译期堆。

**条件设计：** [阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)；[特化、结构探测与静态分支](../../编译/特化与结构查询.md)；[效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**选择额外机制后可用的目标路线：** Rust：SEC 执行已知实例，避免依赖尚不稳定的目标 const trait 能力。 TypeScript：同样执行并生成常量；运行实例仍可拥有独立实现。

**不能越过的边界：** runtime-only witness 不能进入编译期；const 分配不能携带宿主地址／句柄逃逸。

**核验出口：** 同一算法在 compile-capable witness 下预计算，runtime-only witness 下明确拒绝常量调用。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R14](#research-r14) [R06](#research-r06) [C02](#research-c02)

<a id="a40"></a>
### 语义静态反射

**需要什么：** 按解析后的类型/声明生成通用序列化、绑定、检查等。

**可保留的语义区别：** 反射受权语义快照中的类型、成员、函数签名、模板等；正文／源码可见性另外声明。

**Rust 已有替代（研究记录）：** derive/proc宏处理已提供声明；预先生成metadata、显式trait、外部语义工具。

**剩余差额：** 任意外部T的完整已解析语义不可直接从TokenStream获得；metadata需合作/同步，私有信息有边界。

**成为硬边界的条件：** 需要无额外opt-in地检查未知类型语义，并把结果参与泛型实例化。

**当前取舍：** 编译器内部查询与用户开放反射分开；用例足够才稳定后者，C++26实现成熟度单列。

**条件设计：** [阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)；[名称、访问、成员投影与 opaque 类型](../../作者/组合/访问投影与不完整类型.md)；[实例化、分发与链接](../../编译/目标/泛型分发与实例归属.md)。

**选择额外机制后可用的目标路线：** Rust：SEC 持有自己的已解析语义并产生代码，不把 proc-macro token 等同完整语义图。 TypeScript：同样使用 SEC 语义，不依赖 TS runtime 类型保留。

**不能越过的边界：** 未分发定义、opaque 内容、无权限或跨阶段循环必须报告；集合查询变化参与失效。

**核验出口：** 按字段生成序列化器；新增字段触发重生成；未授权私有成员不可获得。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R13](#research-r13) [C12](#research-c12) [T01](#research-t01)

<a id="a41"></a>
### reflection splice

**需要什么：** 将反射到的类型/实体安全拼回程序。

**可保留的语义区别：** 反射值可以经类型化构造生成引用／声明／表达式，名称卫生、访问和阶段义务继续成立。

**Rust 已有替代（研究记录）：** 过程宏生成token、derive及类型化辅助trait；已有编译器内部节点生成。

**剩余差额：** 缺少同一“语义反射值→原生实体引用”的stableAPI；宏生成必须重解析/解析名字。

**成为硬边界的条件：** 通用反射结果必须无损保持语义实体绑定，不能依赖字符串/显式metadata协议。

**当前取舍：** hygiene和绑定正确性必须保留；公共splice不是编译器项目天然必须暴露的语言功能。

**条件设计：** [阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)；[名称、访问、成员投影与 opaque 类型](../../作者/组合/访问投影与不完整类型.md)；[特化、结构探测与静态分支](../../编译/特化与结构查询.md)。

**选择额外机制后可用的目标路线：** Rust：生成 SEC 合格节点后再 lowering，不直接拼目标源码绕过检查。 TypeScript：同样流程，原生源码片段必须是明确的受限边界。

**不能越过的边界：** 错误 kind、悬空符号、越权、循环生成以及部分失败不能发布半个语义图。

**核验出口：** 生成一个调用精确绑定到被反射声明；局部同名不捕获；中途失败旧版本仍完整。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R13](#research-r13) [C12](#research-c12) [T01](#research-t01)

<a id="a42"></a>
### template for

**需要什么：** 按异构静态序列逐项实例化不同类型的代码。

**可保留的语义区别：** 对常量异构序列逐项实例化主体，每项有独立类型绑定、源映射和效果检查。

**Rust 已有替代（研究记录）：** HList map/fold、宏展开、生成具体函数/字段。

**剩余差额：** 原生作者语法和对任意反射序列的组合不同；编译资源/错误位置可能更难管理。

**成为硬边界的条件：** 无适配开放的反射/参数包迭代是公共API硬要求。

**当前取舍：** 归入真正需要的静态展开；不与24/27/40再重复累计成多个底层控制缺口。

**条件设计：** [阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)；[kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)。

**选择额外机制后可用的目标路线：** Rust：输出具体循环体／成员序列，不依赖运行期同质迭代器。 TypeScript：同样输出已展开结构。

**不能越过的边界：** 明确顺序与资源预算；失败定位到原主体与该元素，不能只报生成文件错误。

**核验出口：** int、string 等不同类型元素分别选择合法操作，某一项失败可定位到该项。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [L01](#research-l01) [R13](#research-r13) [C12](#research-c12) [T01](#research-t01)

<a id="a43"></a>
### 自定义复制构造

**需要什么：** 复制可拥有资源的对象，执行资源增加/深复制等协议。

**可保留的语义区别：** clone_into 与结构复制分开；可局部启用用户可观察的隐式复制，其效果明确。

**Rust 已有替代（研究记录）：** Clone显式自定义；Copy保持非用户hook的结构复制。

**剩余差额：** 隐式值复制的调用时机和泛型语法不同；clone不能自动出现在所有赋值/传参位置。

**成为硬边界的条件：** 外部泛型接口依赖隐式copy事件或固定构造协议且不可改。

**当前取舍：** 默认显式复制；保留复制成本/失败/副作用即可，不为覆盖C++而添加隐式copy hook。

**条件设计：** [place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**选择额外机制后可用的目标路线：** Rust：生成显式 clone／构造操作，不把 Copy trait 改成可执行任意代码。 TypeScript：生成真正的对象复制，不以普通引用共享冒充。

**不能越过的边界：** 复制可能分配／失败；已构造部分逆序释放；不能随便删除 hook。

**核验出口：** 带计数／资源的复制执行一次 hook；第二字段构造失败只释放已成功部分。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R10](#research-r10) [R08](#research-r08)

<a id="a44"></a>
### 自定义移动构造

**需要什么：** 转移到新对象并修复内部地址/资源状态，同时留下合法源对象。

**可保留的语义区别：** transfer_construct 可执行用户代码并留下规定 moved-from 状态；不同于 ownership take 和字节重定位。

**Rust 已有替代（研究记录）：** 所有权move、显式转移函数、偏移表示、稳定地址句柄或定制容器。

**剩余差额：** 普通Rust move不执行用户hook；Pin禁止相关移动而不是允许移动时修复；可能改变内联性/容器兼容。

**成为硬边界的条件：** 对象需内联存储、可被普通容器迁移、迁移时自动修复内部指针，且不能改表示/间接存储。

**当前取舍：** 这是有条件的真控制差异；先偏移或原位设计，确需hook再定义显式迁移协议，不照搬所有特殊成员函数。

**条件设计：** [place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**选择额外机制后可用的目标路线：** Rust：生成显式转移函数，源和目标状态由 SEC 跟踪。 TypeScript：显式转移协议，不借 JS 引用赋值隐式完成资源修改。

**不能越过的边界：** 指针修复只能覆盖已声明关系；外部观察旧地址时必须 pin 或注册重定位协议。

**核验出口：** 内部指针修复及源有效状态正确；有外部未更新引用时拒绝普通重定位。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R08](#research-r08) [R20](#research-r20) [C05](#research-c05)

<a id="a45"></a>
### 复制/移动赋值

**需要什么：** 更新已有对象，复用容量并妥善处理旧值及分配域。

**可保留的语义区别：** 复制赋值保留源，转移赋值按协议更新源；目标旧资源、allocator 及失败保证单独规定。

**Rust 已有替代（研究记录）：** clone_from可覆写复用资源；显式assign/replace/mem::take等进行转移。

**剩余差额：** =自身不能进入用户协议；C++源对象仍存活而Rustownership转交后源绑定不可用。

**成为硬边界的条件：** 必须与固定原生赋值语法/第三方generic接口配合，或必须保持源对象post-move状态。

**当前取舍：** 默认显式更新和所有权转移；保留allocator/失败保证；与17共用需求，不重复计数。

**条件设计：** [place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[分配器、资源域与 TLS](../../运行/分配域与退出责任.md)；[效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**选择额外机制后可用的目标路线：** Rust：生成显式赋值协议和必要准备—提交步骤。 TypeScript：同样生成内容更新与资源清理。

**不能越过的边界：** 自赋值、重叠存储、allocator 不相容、失败不能统一成一次裸覆盖。

**核验出口：** 跨分配域赋值正确重分配或拒绝；失败保证与源／目标实际状态吻合。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R10](#research-r10) [R08](#research-r08)

<a id="a46"></a>
### 不可移动值的直接目的地构造

**需要什么：** 对象从建立起地址固定，又能以值工厂或栈存储使用。

**可保留的语义区别：** 固定对象先选最终 slot 再建立引用，可 caller-provided destination 返回或使用稳定拥有句柄。

**Rust 已有替代（研究记录）：** Pin含栈pin，pin-init/初始化器、caller提供slot；无需一概Box。

**剩余差额：** stable普通返回T不普遍保证不经过临时位置；库初始化协议改变API，普通容器也不自动接纳。

**成为硬边界的条件：** 必须原生按值工厂+不可移动类型+栈/内联存储，无新增间接层；C++同类型prvalue可满足特定情形。

**当前取舍：** 保留原位初始化和地址稳定合同；不要把Pin单独称无损平替，也别把NRVO当强保证。

**条件设计：** [place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[分配器、资源域与 TLS](../../运行/分配域与退出责任.md)；[协程、取消与 CFG](../../领域/对象与控制/协程帧与控制流降低.md)。

**选择额外机制后可用的目标路线：** Rust：合格 Pin／field-wise 初始化路线；不依赖可选 NRVO。 TypeScript：仅保证受管逻辑身份；需要原生稳定地址时另用合格宿主供给。

**不能越过的边界：** 严格栈地址不得隐式堆替代；容器增长不能搬走 pinned 对象。

**核验出口：** 返回后内部 self-reference 指向最终位置；禁止 relocate；named local 不凭优化期望通过。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R08](#research-r08) [R09](#research-r09) [L03](#research-l03) [C05](#research-c05) [C06](#research-c06)

<a id="a47"></a>
### placement new

**需要什么：** 在指定存储上开始生命周期、手工管理初始化与销毁。

**可保留的语义区别：** init_at 在已有存储逐部分建立 T；存储与对象生命周期分离，失败只清理成功部分。

**Rust 已有替代（研究记录）：** MaybeUninit、raw write、字段级初始化和合格init库都能做。

**剩余差额：** ptr::write(make_T())不保证make_T没有大临时值；普通new语法与构造协议不同。

**成为硬边界的条件：** 严格栈上限/固定地址/外来对象构造ABI要求无法由普通值临时满足。

**当前取舍：** 撤回“Rust无原位初始化”；保留真实slot合同及部分失败清理，优先成熟封装。

**条件设计：** [place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[布局、位域、padding 与 volatile](../../编译/目标/布局与设备访问.md)。

**选择额外机制后可用的目标路线：** Rust：MaybeUninit／raw write 的受控实现与有效性检查。 TypeScript：受管状态构造；精确机器地址初始化需要 native 供给。

**不能越过的边界：** 对齐、空间、未初始化状态必须成立；不得在无效对象上先构造引用。

**核验出口：** 字段初始化中途失败不析构未建立字段；重复初始化和已死对象访问拒绝。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R09](#research-r09) [L03](#research-l03) [R20](#research-r20)

<a id="a48"></a>
### 全局/类级 new/delete

**需要什么：** 选择分配域，按类型或操作控制分配和释放。

**可保留的语义区别：** 类型或已明确采用的全局政策选择分配／释放，构造失败、数组、对齐与 allocator owner 都绑定。

**Rust 已有替代（研究记录）：** 全局分配器、显式allocator/arena、自定义容器/工厂；构造和释放遵守owner。

**剩余差额：** 没有同样的类型级new语法hook；已有标准容器未必能接受局部分配器，改变可能传播到API。

**成为硬边界的条件：** 要求未修改第三方代码的每次new自动走该类型策略，或nativeallocatorABI不可改。

**当前取舍：** 优先显式资源政策；不要让导入库悄悄接管全局，也不重复自写全部容器。

**条件设计：** [分配器、资源域与 TLS](../../运行/分配域与退出责任.md)；[place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)。

**选择额外机制后可用的目标路线：** Rust：显式 allocation policy 和真实释放入口。 TypeScript：受管资源／buffer allocator；不能控制普通 JS GC 对象的底层分配器。

**不能越过的边界：** 库导入不隐式接管其他库；精确 foreign allocator ABI 需真实桥接。

**核验出口：** 构造失败回到原 owner；跨域释放拒绝；类型局部政策不污染无关类型。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R24](#research-r24) [L04](#research-l04)

<a id="a49"></a>
### 依赖泛型的对齐

**需要什么：** 容器随元素类型保持正确对齐，或按常量要求特定对齐。

**可保留的语义区别：** 对齐可依赖 T；进一步支持具体化后的合法尺寸／对齐表达式和硬布局要求。

**Rust 已有替代（研究记录）：** [T;0]和实际T字段可使align随T变化；固定repr align/具体生成类型覆盖常见强对齐。

**剩余差额：** 只有任意generic表达式驱动repr属性、精准外部布局仍有边界；之前一概说不能随T变化是错误。

**成为硬边界的条件：** 指定align表达式和原生导出类型必须同时成立，不能改表示/生成具体实例。

**当前取舍：** 撤销错误缺口；只对真实的精确布局要求建立检查。

**条件设计：** [布局、位域、padding 与 volatile](../../编译/目标/布局与设备访问.md)；[kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)。

**选择额外机制后可用的目标路线：** Rust：先用真实泛型字段／零长数组等可行机制；更强布局选合法具体类型／存储路线。 TypeScript：wire buffer 可控；普通对象 native 对齐不承诺。

**不能越过的边界：** 表示规则相同不等于实例布局相同；目标不支持的对齐和溢出尺寸不能忽略。

**核验出口：** 不同 T 得到对应对齐；不合法对齐值拒绝；精确 ABI 不用逻辑对象替代。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R07](#research-r07)

<a id="a50"></a>
### 原生位域

**需要什么：** 紧凑位编码、设备寄存器访问或指定外部布局。

**可保留的语义区别：** 固定宽度、符号、位序、字节序、保留位和读写副作用；访问经非字节 projection。

**Rust 已有替代（研究记录）：** mask/shift、bitfield宏/库、buffer视图或ABI生成绑定。

**剩余差额：** 无相同字段语法；C++位域本身布局实现定义，不自动是wire格式，更不自动满足MMIO副作用。

**成为硬边界的条件：** 必须精确匹配某编译器C++位域ABI且无可用布局信息/访问器；否则常规库足够。

**当前取舍：** 按wire、设备、nativeABI分开；设备W1C等操作不可被通用RMW掩盖。

**条件设计：** [布局、位域、padding 与 volatile](../../编译/目标/布局与设备访问.md)；[名称、访问、成员投影与 opaque 类型](../../作者/组合/访问投影与不完整类型.md)。

**选择额外机制后可用的目标路线：** Rust：掩码／移位／未对齐操作的合格实现，或精确 native ABI 桥接。 TypeScript：buffer 编码与合格数值操作。

**不能越过的边界：** C++ 位域不是自动可移植 wire format；设备 W1C／read-clear 禁止盲目读改写。

**核验出口：** 跨字节有符号字段编码正确；保留位政策正确；W1C 写入不误清其他位。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [C07](#research-c07) [R29](#research-r29)

<a id="a51"></a>
### no_unique_address / 尾部padding复用

**需要什么：** 压缩空策略对象或在合法条件下复用尾部空隙。

**可保留的语义区别：** 允许在不破坏有效字段、生命周期和地址观察的条件下共享地址或复用 padding。

**Rust 已有替代（研究记录）：** Rust ZST可零空间/共享地址；重新排列字段、不同存储表示、raw buffer专门实现。

**剩余差额：** 普通非ZST字段尾部padding不能靠同一属性重用；改表示可能丢失&Field/native字段API。

**成为硬边界的条件：** 精确紧凑布局、正常字段引用和ABI必须同时保留，且字节数确实决定缓存/包布局要求。

**当前取舍：** 保留可选布局需求，不保证所有目标缩小；不能以重叠普通Rust引用强行模拟。

**条件设计：** [布局、位域、padding 与 volatile](../../编译/目标/布局与设备访问.md)；[place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)。

**选择额外机制后可用的目标路线：** Rust：仅采用可证明合格的精确字节访问／native 供给；不生成重叠的普通独占引用。 TypeScript：仅 wire／显式 buffer 布局有此意义，普通对象大小不类比。

**不能越过的边界：** 允许优化不承诺每个目标缩小；宽写、析构、比较不能覆盖其他活跃成员。

**核验出口：** 复用尾部 padding 时邻接活跃字段在赋值后仍正确；要求唯一地址时拒绝共享。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R07](#research-r07) [C08](#research-c08) [R20](#research-r20)

<a id="a52"></a>
### volatile类型限定

**需要什么：** 确保经某类视图的访问使用volatile操作，传播设备访问政策。

**可保留的语义区别：** volatile view 保留操作宽度、地址空间及访问政策，最终生成规定的 volatile 操作。

**Rust 已有替代（研究记录）：** read_volatile/write_volatile加类型化寄存器访问器，禁用普通借用逃逸。

**剩余差额：** 不是语言级cv qualifier；generic访问必须通过正确接口，但可以得到更明确的只读/W1C政策。

**成为硬边界的条件：** 固定外部类型/overload/volatile调用约定无法改，或未知代码必须靠原生qualifier传播。

**当前取舍：** 常规设备访问可库化；volatile不等于atomic或CPU屏障，分别履行真实硬件合同。

**条件设计：** [布局、位域、padding 与 volatile](../../编译/目标/布局与设备访问.md)；[重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**选择额外机制后可用的目标路线：** Rust：使用真正 volatile load/store 的受控访问器，禁止变成普通引用访问。 TypeScript：仅在合格设备／native 接口存在时实现；普通属性读取不能冒充 MMIO。

**不能越过的边界：** volatile 不等于 atomic／硬件 fence；设备权限与地址有效性独立检查。

**核验出口：** 两次有意读取产生规定的两次访问；转换不丢属性；需要屏障时没有就拒绝。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R29](#research-r29) [C07](#research-c07)

<a id="a53"></a>
### long double / 扩展浮点

**需要什么：** 获得特定精度/范围，或传递本机long-double ABI。

**可保留的语义区别：** 数值格式、舍入、特殊值与 ABI 角色分开，可表达扩展精度和指定 long-double profile。

**Rust 已有替代（研究记录）：** 软件高精度、实际硬件intrinsic/汇编、外部数值内核；f128状态另查。

**剩余差额：** 数值可算不等于相同格式/ABI/指令/成本；binary128也不等于全部平台long double。

**成为硬边界的条件：** 要求x87扩展精度、double-double等原生ABI或严格性能/异常环境，不能用普通f64/软件替代。

**当前取舍：** 保留格式与ABI区分；只为实际数值域选择供给，不默认造全浮点系统。

**条件设计：** [数值、SIMD 与优化控制](../../领域/数值/格式环境与向量控制.md)；[foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)。

**选择额外机制后可用的目标路线：** Rust：实际目标原语、软件实现或明确 native bridge；不以 f128 名字自动解决 ABI。 TypeScript：合格软件数值或宿主模块，不用 Number 冒充更高精度。

**不能越过的边界：** 软件数值等价不等于硬件 ABI／时延等价；无法保持硬要求则不支持。

**核验出口：** 带符号零／NaN／舍入符合合同；ABI 按实际工具链校验，数值模式不同不误认。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R31](#research-r31) [R30](#research-r30) [T02](#research-t02)

<a id="a54"></a>
### 类型化栈展开异常

**需要什么：** 非局部错误传播、类型匹配处理器及确定的清理。

**可保留的语义区别：** 最近匹配处理器、类型／继承匹配、payload 寿命、清理顺序与传播效果均明确。

**Rust 已有替代（研究记录）：** 普通失败用Result/?；panic_any/catch_unwind有payload，但属panic协议；外部异常在匹配域转译。

**剩余差额：** 改Result会影响整个传播路径签名/控制流；固定无错误返回的回调无法透明改造。foreign catch非通用类型化catch。

**成为硬边界的条件：** 固定外部调用链要求实际C++异常展开、catch层级或错误表/正常路径成本，不允许更改ABI。

**当前取舍：** 新代码优先显式错误与统一清理；实际unwind属于有界互操作能力，不能一律说Result无损，也不默认新增完整异常语言。

**条件设计：** [效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)；[place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)。

**选择额外机制后可用的目标路线：** Rust：受管调用链降低为 Result＋cleanup；真实 native unwinding 要求另查异常域。 TypeScript：类型化受管 throw／handler 加明确清理；外来 throw 保留 foreign error。

**不能越过的边界：** panic 不是通用 typed exception 替代；错误不能越过不允许 unwind 的 FFI。

**核验出口：** 深层错误命中正确 handler，恰好清理已建立资源；foreign 边界按协议转译。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R21](#research-r21) [R22](#research-r22) [C11](#research-c11) [I03](#research-i03)

<a id="a55"></a>
### 通用可定制协程

**需要什么：** 可恢复计算、生成器、yield/resume参数和自定义调度。

**可保留的语义区别：** resume／yield／return／error 参数化 continuation，one-shot、状态、帧与取消责任明确。

**Rust 已有替代（研究记录）：** Iterator、Future、显式状态机、生成宏/库；不同场景选择不同接口。

**剩余差额：** 手写状态机跨挂起借用/帧初始化难度较高；一般Coroutine及自定义语言协议并非stable一等能力。

**成为硬边界的条件：** 库必须暴露原生通用resume/yield协议、保留地址/帧分配控制，且宏/手工状态机成本不可接受。

**当前取舍：** 先复用Future/Iterator或生成状态机；取消/清理必须正确，但不由此自动推出multi-shot或通用effect系统。

**条件设计：** [协程、取消与 CFG](../../领域/对象与控制/协程帧与控制流降低.md)；[place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[分配器、资源域与 TLS](../../运行/分配域与退出责任.md)；[foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)。

**选择额外机制后可用的目标路线：** Rust：显式状态机或合格 Future／generator 适配，不假定 arbitrary coroutine trait stable。 TypeScript：受管状态机／适当 async-generator adapter。

**不能越过的边界：** 并发 resume、完成后 resume、frame 提前销毁拒绝；异步清理必须有执行 owner。

**核验出口：** yield 后恢复数据正确；取消进入 Closing；注册唤醒竞争不丢通知；旧 waker 不访问已死帧。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R28](#research-r28) [R08](#research-r08)

<a id="a56"></a>
### 定义C变参函数

**需要什么：** 导出printf类真实C ellipsis入口。

**可保留的语义区别：** 导出实际 C 变参入口，执行 ABI 提升与 va_list 合同；不同于类型安全 pack。

**Rust 已有替代（研究记录）：** stable可调用已声明C变参；导出用C/C++shim或明确支持的实验工具链。

**剩余差额：** slice/tuple/JS rest不是C ABI；要承担原生入口、提升与va_list处理。

**成为硬边界的条件：** 禁止任何额外原生对象且要求pure stable Rust导出真实变参符号。

**当前取舍：** 只在FFI适配器中处理，不让每个Gift函数都背C变参模型。

**条件设计：** [foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)；[kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)。

**选择额外机制后可用的目标路线：** Rust：合格预置原生入口／明确允许的工具链；纯 stable Rust 无对应能力时拒绝该导出。 TypeScript：需要真实 native 入口；JS rest parameter 不是 C ellipsis ABI。

**不能越过的边界：** 不能以可调用 foreign variadic 证明可定义；无格式／标签不能安全猜参数。

**核验出口：** float／小整数提升正确，va_list 生命周期正确；缺少原生供给时给精确缺项。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R23](#research-r23) [R21](#research-r21)

<a id="a57"></a>
### goto

**需要什么：** 表达任意合法函数内控制流，减少某些状态机额外dispatch。

**可保留的语义区别：** 以有类型基本块／jump 表达任意合法 CFG，边携带拥有权、初始化及清理义务。

**Rust 已有替代（研究记录）：** loop/break/continue/match、CFG结构化、状态机生成。

**剩余差额：** 算法可实现不保证指令形状或额外dispatch消失；computed goto又是GNU扩展而非同一能力。

**成为硬边界的条件：** 不可约CFG的分支结构/速度已证明重要，生成Rust结构化代码无法满足且不能调用专门后端。

**当前取舍：** 高层不必暴露goto；低层需保留CFG和清理事实，严格分支结构另查目标。

**条件设计：** [协程、取消与 CFG](../../领域/对象与控制/协程帧与控制流降低.md)；[place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)。

**选择额外机制后可用的目标路线：** Rust：结构化变换或 loop＋state match；精确 target 分支结构单独约束。 TypeScript：循环和状态分派。

**不能越过的边界：** 禁止跳入未初始化作用域、漏掉 cleanup 或重用 moved 值；不保证状态机性能等同原生 goto。

**核验出口：** 错误清理分支释放一次；非法跳过构造被拒绝；不可约图行为被保持。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R33](#research-r33) [T04](#research-t04)

<a id="a58"></a>
### 原生C++ ABI与模板生态

**需要什么：** 逐步接入既存C++库/SDK/对象、回调及二进制接口。

**可保留的语义区别：** 固定类布局、符号、构造析构、错误、allocator、回调与实际工具链域。

**Rust 已有替代（研究记录）：** CXX/Crubit等桥接、C接口、少量手写shim；可复用C++而不重写全部。

**剩余差额：** 支持边界依桥接器/工具链；不能普遍保留所有虚继承、模板开放API、异常和布局；构建依赖真实存在。

**成为硬边界的条件：** 不可加shim、不能改变现成API、没有可用兼容toolchain，或要求任意C++源码逐文件替换。

**当前取舍：** 明确支持的ABI边界并复用成熟前端/编译器；不默认让Gift成为完整C++实现。

**条件设计：** [foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)；[子对象、继承与动态视图](../../领域/对象与控制/子对象视图与兼容构造.md)；[place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**选择额外机制后可用的目标路线：** Rust：优先复用合格 CXX／shim／native 供给；不声称 rustc 通用支持完整 C++ ABI。 TypeScript：通过明确宿主 native bridge，普通 TS 类型声明不够。

**不能越过的边界：** 目标仅 TS＋Rust 不隐含批准 C++ 构建；匹配 ABI 之前不能传按值类／异常。

**核验出口：** 对象由约定方释放；异常在正确域捕获；动态库有在途对象时拒绝卸载。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [I01](#research-i01) [I02](#research-i02) [R21](#research-r21)

<a id="a59"></a>
### 泛型 atomic<T>

**需要什么：** 原子读取/交换复合简单状态。

**可保留的语义区别：** 合格 T 上的原子 load/store/CAS 独立声明表示、访问域和进展政策。

**Rust 已有替代（研究记录）：** atomic等泛型crate、原生整数编码、portable-atomic的宽原子、允许时锁协议。

**剩余差额：** 类型有效表示/NoUninit/对齐范围有区别；锁回退不是无锁；泛型std名称仍不等于任意T稳定支持。

**成为硬边界的条件：** 禁止锁/分配、进程共享/ISR安全或精确复合ABI，目标原语和库不足。

**当前取舍：** 按真实进展、表示和访问域选择供给；不因std少一个模板就自己重造atomic系统。

**条件设计：** [原子值、原子视图与内存模型](../../领域/计算基础/原子存储与进展条件.md)；[布局、位域、padding 与 volatile](../../编译/目标/布局与设备访问.md)；[分配器、资源域与 TLS](../../运行/分配域与退出责任.md)。

**选择额外机制后可用的目标路线：** Rust：实际原子原语／库；仅被允许时使用锁，不能用实验 Generic Atomic 名字扩大类型范围。 TypeScript：共享 buffer 的合格编码／原生模块；任意 JS 对象不直接原子化。

**不能越过的边界：** 原子性、无锁、wait-free、跨进程及崩溃耐久分开；不能随意原子复制独占资源。

**核验出口：** 复合状态不可撕裂；硬性 lock-free 时拒绝锁回退；指针 payload 不丢 provenance。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R25](#research-r25) [L05](#research-l05) [L06](#research-l06) [C09](#research-c09)

<a id="a60"></a>
### atomic_ref<T>

**需要什么：** 对既有存储临时提供原子访问而不改变原始声明。

**可保留的语义区别：** 在已有合格存储上建立有寿命的原子访问租约，约束全部并发访问者及访问粒度。

**Rust 已有替代（研究记录）：** 相关primitive from_mut/from_ptr路径；1.98稳定新增视图；复合值用专门封装/库。

**剩余差额：** 任意复合T的原生通用视图不同；借用生命周期不能自动约束所有外部线程/设备。

**成为硬边界的条件：** 既有共享内存布局/复合类型/无锁要求不可改，且没有匹配原语和访问协议。

**当前取舍：** 保留租约、对齐及混合访问规则；撤回Rust一概没有原子视图。

**条件设计：** [原子值、原子视图与内存模型](../../领域/计算基础/原子存储与进展条件.md)；[布局、位域、padding 与 volatile](../../编译/目标/布局与设备访问.md)。

**选择额外机制后可用的目标路线：** Rust：primitive 使用真实已稳定视图能力，复合类型采用经检查的专门路线。 TypeScript：仅实际共享存储／宿主支持的范围适用。

**不能越过的边界：** 对齐、类型、寿命、混合非原子访问、lease 结束前在途使用均需检查。

**核验出口：** 合格普通原始存储可转原子视图；活跃租约中的冲突非原子访问被拒绝。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R02](#research-r02) [R25](#research-r25) [C09](#research-c09)

<a id="a61"></a>
### allocator参数标准容器/pmr

**需要什么：** 每容器/每区域选择分配策略并控制OOM/释放。

**可保留的语义区别：** 容器可静态／动态选择 allocator，明确 region 寿命、OOM、增长与传播政策。

**Rust 已有替代（研究记录）：** allocator-api2等独立容器、arena、定制库；全局allocator解决另一层问题。

**剩余差额：** 不是可直接替换现有std::Vec类型；第三方固定std容器接口可能阻断局部allocator传播。

**成为硬边界的条件：** 无法改依赖签名，却要求其内部容器逐实例使用不同allocator；或资质只允许指定标准库。

**当前取舍：** 这是实际生态可组合性差异；先检查依赖接受什么容器/策略，不能说随便用全局分配器就解决。

**条件设计：** [分配器、资源域与 TLS](../../运行/分配域与退出责任.md)；[place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)。

**选择额外机制后可用的目标路线：** Rust：采用实际稳定可用的合格容器／适配器；不偷偷启用 allocator_api nightly。 TypeScript：显式 buffer／外部资源容器；普通 JS 堆对象分配不可伪控制。

**不能越过的边界：** 跨 allocator move／swap 需匹配所有权或重分配；arena 逃逸拒绝。

**核验出口：** 增长溢出和 OOM 可按合同恢复；跨域 swap 不发生错误释放；区域过期引用拒绝。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R24](#research-r24) [L04](#research-l04)

<a id="a62"></a>
### 原生thread_local变量

**需要什么：** 按物理线程独立状态，必要时导出原生TLS符号。

**可保留的语义区别：** 物理线程局部存储与任务局部存储分开；初始化、访问寿命、退出与原生符号单独约定。

**Rust 已有替代（研究记录）：** thread_local!/LocalKey；const初始化可避免部分lazy/Drop追踪开销；任务局部另有模型。

**剩余差额：** 正常TLS用途已覆盖；原生TLS符号、取址和外部链接接口仍不同，不能一概宣称每次有包装开销。

**成为硬边界的条件：** 现成二进制要直接引用TLS符号，或指定TLS模型/初始化顺序。

**当前取舍：** 通常复用标准库；线程亲和跨await需要正确处理，但不把TLS便利升级为新语言核心。

**条件设计：** [分配器、资源域与 TLS](../../运行/分配域与退出责任.md)；[协程、取消与 CFG](../../领域/对象与控制/协程帧与控制流降低.md)；[foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)。

**选择额外机制后可用的目标路线：** Rust：LocalKey 受管访问可满足行为；原生 TLS 符号／地址需真实 ABI 路线。 TypeScript：worker-local 仅满足匹配宿主语义；不冒充 OS TLS 导出。

**不能越过的边界：** 任务迁移不得继续用原线程专属引用；初始化循环／退出后访问明确失败。

**核验出口：** 不同线程实例隔离；跨 await 迁移受约束；递归初始化报循环而非死锁。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R26](#research-r26) [R21](#research-r21)

<a id="a63"></a>
### 标准portable SIMD

**需要什么：** 可移植向量运算、mask和归约，并选择合格ISA。

**可保留的语义区别：** 向量／mask 的 lane、内存访问、转换、归约及数值规则是稳定 SEC 合同。

**Rust 已有替代（研究记录）：** 稳定架构intrinsics、实际SIMD库、编译器向量化；portable std::simd实验中。

**剩余差额：** 标准统一API与覆盖成熟度不同；自动向量化不保证指令，标量/锁等不能冒充性能硬保证。

**成为硬边界的条件：** 固定标准库依赖、ISA、mask异常行为或确定性归约合同，现有库/目标无法满足。

**当前取舍：** 按实际数值与ISA合同复用供给；C++26特性成熟度单列，不说Rust没有SIMD。

**条件设计：** [数值、SIMD 与优化控制](../../领域/数值/格式环境与向量控制.md)；[使用点保证、目标画像与准入结果](../../编译/目标/能力履行与接口差额.md)。

**选择额外机制后可用的目标路线：** Rust：合格架构 intrinsics／库／供给；不默认把实验 std::simd 当 stable。 TypeScript：允许时采用已取得原生／Wasm 路线或标量；各自资源与数值义务分开。

**不能越过的边界：** 屏蔽 lane 的访问副作用、ISA 探测、浮点重结合和性能硬要求不能忽略。

**核验出口：** inactive lane 不产生被禁止访问；缺 ISA 使用合格基线或拒绝；固定归约结果可重现。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [R27](#research-r27) [T01](#research-t01) [T02](#research-t02)

<a id="a64"></a>
### 语言级contracts

**需要什么：** 写前/后置条件并统一检查/诊断，可能帮助工具分析。

**可保留的语义区别：** pre／post／invariant／old 具有阶段、纯性、成本和失败政策，并与效果／override 合同组合。

**Rust 已有替代（研究记录）：** assert、验证构造器、类型状态、过程宏、测试/外部验证器。

**剩余差额：** 原生契约语法/模式/工具集成不同；但C++contracts也不是一般refinement证明系统。

**成为硬边界的条件：** 必须兼容C++契约语义/诊断模式，或公共API工具必须消费标准contract实体。

**当前取舍：** 保留真实安全前提与检查，是否提供通用契约语法按需求决定；不能因此宣布Gift必须有全套effect/refinement系统。

**条件设计：** [效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)；[阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)；[使用点保证、目标画像与准入结果](../../编译/目标/能力履行与接口差额.md)。

**选择额外机制后可用的目标路线：** Rust：可证明则静态履行，否则保留合格检查与错误政策。 TypeScript：同样生成检查／快照，不假定类型擦除后自动成立。

**不能越过的边界：** ignore／observe 不得撤掉唯一内存安全前提；unknown 不等于证明成立。

**核验出口：** 子类不得强化前置或弱化后置；删检查后 unsafe 无依据的 lowering 被拒绝。 这描述条件验收，不表示本次执行过该目标程序。

**依据：** [C10](#research-c10) [R13](#research-r13) [R20](#research-r20)

## 新发现的独立分支

<a id="x01"></a>
### 动态浮点环境：真正漏掉的控制边界

**研究记录：** Rust x86 MXCSR 文档明确：在普通 Rust 执行域修改 masking、rounding、DAZ 等被假定为默认的状态会导致未定义行为；即使源码中未写浮点表达式，重排仍会影响。

**替代与对照：** 特殊运算全部放入一个合格 asm 块并恢复状态，或使用明确隔离的数值内核/软件定向舍入。C++/Clang 也必须使用支持动态环境的严格模式，不能用默认优化编译假设代替。

**条件与影响：** 区间算法、严格定向舍入、异常环境等需要普通表达式在动态环境下运行时，Rust 原生表达式不是透明替代；软件/外部内核的ABI、性能和可内联性需核验。

**采用决定：** 数值合同应标明格式/舍入/环境；这比添加 friend 关键字更可能改变真实结果，但仍只在相关数值操作中生效。

**归属：** [数值、SIMD 与优化控制](../../领域/数值/格式环境与向量控制.md)。

**依据：** [R30](#research-r30) [T02](#research-t02)

<a id="x02"></a>
### 快速浮点也不能按旧印象比较

**研究记录：** Rust 1.98 稳定了 f32/f64 的 algebraic_* 操作，允许按代数性质优化；它们不因这类优化产生UB，但可能产生不同、未指定的数值结果。

**替代与对照：** 在允许误差/重排的区域使用明确操作；普通严格计算保留原顺序和特殊值合同。

**条件与影响：** 不能再声称Rust完全没有类似fast-math的控制；也不能说这等于Clang全部全局fast-math开关及浮点环境语义。

**采用决定：** 把容许的数值变化写在真正需要处，区分快速代数操作、固定ISA和动态舍入。

**归属：** [数值、SIMD 与优化控制](../../领域/数值/格式环境与向量控制.md)。

**依据：** [R02](#research-r02) [T02](#research-t02)

<a id="x03"></a>
### 带捕获的泛型闭包

**研究记录：** C++ 泛型 lambda 的同一个 closure 对象可对不同参数类型实例化调用；本次测试同一个捕获对象用于 int 与 double。

**替代与对照：** Rust 可定义带泛型方法的具名状态类型、GAT/trait策略或宏生成多态callable；普通闭包的参数类型不是任意类型量化的C++泛型lambda接口。

**条件与影响：** 交给固定原生Fn接口、跨泛型高阶函数传递且不能换协议时，需额外适配；不是“Rust不能写泛型函数”。

**采用决定：** 有高阶泛型库需求再提供相应作者抽象，降低为明确实例/策略，不默认复制全部闭包模板语法。

**归属：** [kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)；[重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)。

**依据：** [C17](#research-c17) [R36](#research-r36) [R12](#research-r12) [L01](#research-l01)

<a id="x04"></a>
### 表示族与条件行为

**研究记录：** C++ 类型特化可改变成员、布局和可用操作。Rust 的不重叠 impl、关联存储和条件 trait 实现已能覆盖不少按类型选择需求。

**替代与对照：** 将表示抽到关联类型，把不同策略显式编码；可按bounds条件实现Copy/Clone等允许的traits。

**条件与影响：** 修改已有泛型struct本身的字段集、默认实现再被例外覆盖、条件析构等并不是任意一致支持的统一能力；每种操作需单独检查，不能从能特化一个方法推出全部。

**采用决定：** 将真实不同表示保留为类型差异；纯优化优先交后端，不建立一个名称宽泛的万能specialization开关。

**归属：** [特化、结构探测与静态分支](../../编译/特化与结构查询.md)；[kind、参数包与常量身份](../../作者/组合/参数包与类型级值.md)。

**依据：** [R04](#research-r04) [R05](#research-r05) [R15](#research-r15) [C03](#research-c03)

<a id="x05"></a>
### 编译期临时容器与编译宿主

**研究记录：** 本次C++测试验证了合格constexpr函数可临时使用vector并在常量求值结束前释放。Rust常量求值可用的库协议/分配操作需逐项核验。

**替代与对照：** 数据表生成可以移到受控build步骤；小型常量计算用const fn；已知类型先生成。

**条件与影响：** host脚本不自动等于目标类型系统内的const运算；目标字长、浮点、环境依赖、错误时机和共享泛型实现可能不同。

**采用决定：** 有实际编译期算法时保留阶段和目标输入；不能仅因C++有constexpr就默认实现任意元程序堆。

**归属：** [阶段化执行与语义反射](../../编译/内核/阶段求值与反射生成.md)。

**依据：** [R14](#research-r14) [R06](#research-r06) [C02](#research-c02)

<a id="x06"></a>
### unsafe 是证明责任边界，不是无限权限

**研究记录：** Rust unsafe 仍须遵守有效值、引用/别名和生命周期等条件；合法C++程序也不包括借UB得到的虚假能力。

**替代与对照：** 对借用检查器无法证明但可证明安全的图结构/重入/内存操作，可用raw pointer、UnsafeCell及小型安全封装；不必一律Rc/RefCell/锁。

**条件与影响：** 替代若把每次访问变成动态检查或把大量用户代码变成unsafe审计，静态保证/维护成本就发生实质变化；单靠API看起来安全不证明实现健全。

**采用决定：** 优先小而可审核的边界；不把“用unsafe就能写”当作已解决所有接口、证明和维护问题。

**归属：** [place、初始化、复制／转移／赋值](../../领域/对象与控制/位置初始化与转移.md)；[使用点保证、目标画像与准入结果](../../编译/目标/能力履行与接口差额.md)。

**依据：** [R20](#research-r20) [R08](#research-r08)

<a id="x07"></a>
### 原生索引代理与借用型结果

**研究记录：** Index 的固定引用结果不能普遍表达C++按值proxy，packed-bit引用只是一个例子；矩阵视图、多索引、延迟写回也可能相交。

**替代与对照：** get/set、显式借用proxy、自定义投影trait，或生成专用操作。

**条件与影响：** 公共算法必须使用原生[]/=协议时，库方案需要改接口；存储原子性、提交时机和代理寿命不能因语法糖丢掉。

**采用决定：** 作为具体容器接口判断，不把所有operator缺失一起无条件忽略。

**归属：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)；[布局、位域、padding 与 volatile](../../编译/目标/布局与设备访问.md)。

**依据：** [R11](#research-r11) [C07](#research-c07)

<a id="x08"></a>
### noexcept、无分配和有界执行不是同一保证

**研究记录：** C++ noexcept 属异常规格；函数内部仍可能执行会失败的操作，异常逃出边界会终止。它不是无分配、必终止或静态证明纯性的统一保证。

**替代与对照：** Rust可显式Result、限定可用API、panic策略/外部检查；不能拿全局panic=abort冒充相同逐函数类型协议。

**条件与影响：** 优化/泛型库可能按nothrow性质选择复制或移动；固定源接口及失败策略可能需要适配。

**采用决定：** 先保留真实错误和清理要求；不能从noexcept/contracts推导Gift必须有完整代数效果系统或一般refinement求解器。

**归属：** [效果、异常与契约](../../领域/计算基础/错误传播与契约保持.md)。

**依据：** [C11](#research-c11) [C10](#research-c10) [R22](#research-r22)

<a id="x09"></a>
### GNU computed goto、尾调用与厂商扩展

**研究记录：** GNU labels-as-values 是超出ISO C++的独立能力；Rust显式尾调用处于实验方向。各编译器对调用约定和属性的支持也有版本/目标限制。

**替代与对照：** 结构化循环、显式状态机、汇编/独立原生内核、实际后端支持的tailcall；必须看最终目标证据。

**条件与影响：** 解释器dispatch、保证栈不增长、特定ABI入口有硬性指标时，等价算法可能不等于等价分支/栈行为。GNU computed goto另有跨作用域析构风险。

**采用决定：** 先明确要求标准语言还是指定编译器；不要拿GCC扩展无标记扩大C++标准优势，也别凭优化希望宣称Rust已无损覆盖。

**归属：** [协程、取消与 CFG](../../领域/对象与控制/协程帧与控制流降低.md)；[实例化、分发与链接](../../编译/目标/泛型分发与实例归属.md)。

**依据：** [T04](#research-t04) [R34](#research-r34) [T03](#research-t03) [R33](#research-r33)

<a id="x10"></a>
### 平台、GPU、SDK、认证/资质与工具支持

**研究记录：** Rust存在明确的目标分级；C/C++厂商工具链及GPU等扩展的可用性属于外部供给，不由语言计算完备性决定。

**替代与对照：** Rust调原生内核、复用供应商库、分阶段迁移或保留兼容岛。

**条件与影响：** 某项目实际指定的芯片、驱动接口、调试器或获准工具链不支持所需Rust路线时，这可以成为硬阻碍；本次未逐一调查所有芯片/资质，不能宣称某个未查型号不支持。

**采用决定：** 把目标/工具版本和支持证据绑定到使用点。Rust宿主能启动一个C++内核，不等于已能用纯Rust编写同一内核。

**归属：** [foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)；[使用点保证、目标画像与准入结果](../../编译/目标/能力履行与接口差额.md)。

**依据：** [R32](#research-r32) [T03](#research-t03) [I02](#research-i02)

<a id="x11"></a>
### 标准库之外的工程可用性

**研究记录：** 类似atomic wait/wake等能力可由独立Rust库提供；atomic-wait的支持对象、平台和超时范围需按其文档理解。

**替代与对照：** 复用合格crate/系统API，而不是为了标准库命名对称重新写轮子。

**条件与影响：** 团队只允许特定依赖、no_std目标缺系统服务、外部API固定容器、审计/许可证不接受依赖时，库方案可能不合格。

**采用决定：** 把依赖成本、支持矩阵和API可组合性当真实条件；不枚举每个C++库函数当新语言缺口。

**归属：** [分配器、资源域与 TLS](../../运行/分配域与退出责任.md)；[原子值、原子视图与内存模型](../../领域/计算基础/原子存储与进展条件.md)。

**依据：** [L07](#research-l07) [L04](#research-l04) [L06](#research-l06) [R32](#research-r32)

<a id="x12"></a>
### 构建、下游泛型和动态插件的开放世界

**研究记录：** 具体实例提前生成，可以绕开目标语言没有的抽象语法；但无法据此承诺未来所有未知原生类型都能直接调用。

**替代与对照：** 分发可继续编译的Gift语义定义、目标原生可表达的泛型、有限实例，或明确的动态字典/接口。

**条件与影响：** 三者的编译工具依赖、二进制体积、内联机会、ABI和动态元数据要求不同；运行时未知插件不能靠构建时无限猜测解决。

**采用决定：** 保留实例化/字典选择和导出边界；若需要额外编译器/JIT必须明示，不把“编译器会lowering”当万能答案。

**归属：** [实例化、分发与链接](../../编译/目标/泛型分发与实例归属.md)；[特化、结构探测与静态分支](../../编译/特化与结构查询.md)。

**依据：** [R33](#research-r33) [R06](#research-r06) [I01](#research-i01)

<a id="x13"></a>
### 较简单规则不等于源语言兼容

**研究记录：** C++先做最佳重载选择，再检查选中函数是否可访问；private精确匹配不会被提前删掉，以便退选public的较差匹配。本次GCC与Clang均验证拒绝该程序。

**替代与对照：** Gift可自主选择不同规则；导入C++时由真实C++前端解释，或建立明确覆盖的兼容规则。

**条件与影响：** 把旧设计的“先筛访问权限”直接用于精确C++输入，会接受本来非法的程序。旧规则作为Gift自有语义可以存在，但不能被当作C++等价规则。

**采用决定：** 已解析调用可以进入中立IR；不必把完整重载决议留到每个后端。默认值、临时对象寿命、析构时机同样需要源语义归属。

**归属：** [重载、receiver、转换与运算符](../../领域/计算基础/调用定制与源语言选择.md)。

**依据：** [C16](#research-c16) [C15](#research-c15) [R21](#research-r21)

<a id="x14"></a>
### 全局初始化、TLS、资源退出和动态卸载

**研究记录：** 运行期资源生命周期不只等于局部Drop。原生TLS导出、外部模块初始化/退出、在途回调和旧对象析构都可能跨越模块边界。

**替代与对照：** 普通新代码用显式初始化/关闭、受控局部状态与已选运行时；特定loader协议通过适配器处理。

**条件与影响：** 本次未对所有平台的loader协议作全面调查，因此不把“C++有全局构造、Rust没有同名语法”直接算新硬缺口；真正约束是指定时机和在用代码不能先卸载。

**采用决定：** 保留旧设计的初始化环、回调注销和卸载安全案例，作为选择相应运行域时才启用的要求。

**归属：** [分配器、资源域与 TLS](../../运行/分配域与退出责任.md)；[foreign ABI、C 变参、回调与卸载](../../运行/原生边界与模块驻留.md)。

**依据：** [R26](#research-r26) [R21](#research-r21) [I01](#research-i01)

## 一手来源及其原记录

同一 URL 的多条引用不是多个独立证据；保留原命题角色，不按重复数量提高置信度。仓库与运行完成事实应另取准确修订回读，不能用外部规范代签。

<a id="baseline-s01"></a>
**Rust 1.98.0 release notes；atomic 视图接口稳定情况** — [原始材料](https://blog.rust-lang.org/2026/08/20/Rust-1.98.0/)。原记录日期：2026-09-22。

<a id="baseline-s02"></a>
**Rust Reference：Generic parameters** — [原始材料](https://doc.rust-lang.org/stable/reference/items/generics.html)。原记录日期：2026-09-22。

<a id="baseline-s03"></a>
**Rust Reference：Type layout** — [原始材料](https://doc.rust-lang.org/stable/reference/type-layout.html)。原记录日期：2026-09-22。

<a id="baseline-s04"></a>
**Rust Reference：Implementations／coherence** — [原始材料](https://doc.rust-lang.org/reference/items/implementations.html)。原记录日期：2026-09-22。

<a id="baseline-s05"></a>
**Rust Unstable Book：fn_traits** — [原始材料](https://doc.rust-lang.org/unstable-book/library-features/fn-traits.html)。原记录日期：2026-09-22。

<a id="baseline-s06"></a>
**Rust Unstable Book：specialization** — [原始材料](https://doc.rust-lang.org/unstable-book/language-features/specialization.html)。原记录日期：2026-09-22。

<a id="baseline-s07"></a>
**Rust Reference：Constant evaluation** — [原始材料](https://doc.rust-lang.org/reference/const_eval.html)。原记录日期：2026-09-22。

<a id="baseline-s08"></a>
**Rust std::pin：地址敏感值与 Pin 合同** — [原始材料](https://doc.rust-lang.org/std/pin/index.html)。原记录日期：2026-09-22。

<a id="baseline-s09"></a>
**Rust Unstable Book：C variadic definitions** — [原始材料](https://doc.rust-lang.org/unstable-book/language-features/c-variadic.html)。原记录日期：2026-09-22。

<a id="baseline-s10"></a>
**Rust Nomicon：FFI 及 unwinding 边界** — [原始材料](https://doc.rust-lang.org/nomicon/ffi.html)。原记录日期：2026-09-22。

<a id="baseline-s11"></a>
**Rust std::alloc::Allocator 接口状态** — [原始材料](https://doc.rust-lang.org/std/alloc/trait.Allocator.html)。原记录日期：2026-09-22。

<a id="baseline-s12"></a>
**Rust std::simd 接口状态** — [原始材料](https://doc.rust-lang.org/std/simd/index.html)。原记录日期：2026-09-22。

<a id="baseline-s13"></a>
**Rust std::sync::atomic::Atomic：generic_atomic／AtomicPrimitive 与视图 API** — [原始材料](https://doc.rust-lang.org/stable/std/sync/atomic/struct.Atomic.html)。原记录日期：2026-09-22。

<a id="baseline-s14"></a>
**Rust Function Overloading — Call for Experimentation（2026-08-19）** — [原始材料](https://blog.rust-lang.org/inside-rust/2026/08/19/overloading-experiment/)。原记录日期：2026-09-22。

<a id="baseline-s15"></a>
**WG21 P4197R0：Design questions for trivial relocation in C++29** — [原始材料](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p4197r0.html)。原记录日期：2026-09-22。

<a id="baseline-s16"></a>
**C++ working draft：Derived classes** — [原始材料](https://eel.is/c++draft/class.derived)。原记录日期：2026-09-22。

<a id="baseline-s17"></a>
**C++ working draft：Template parameters** — [原始材料](https://eel.is/c++draft/temp.param)。原记录日期：2026-09-22。

<a id="baseline-s18"></a>
**C++ working draft：Initialization；结合 S18B 区分 prvalue 与 NRVO** — [原始材料](https://eel.is/c++draft/dcl.init)。原记录日期：2026-09-22。

<a id="baseline-s18b"></a>
**C++ working draft：Copy elision** — [原始材料](https://eel.is/c++draft/class.copy.elision)。原记录日期：2026-09-22。

<a id="baseline-s19"></a>
**WG21 P2996R13：Reflection for C++26（提案正文）** — [原始材料](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2025/p2996r13.html)。原记录日期：2026-09-22。

<a id="baseline-s20"></a>
**C++ working draft：atomic_ref generic** — [原始材料](https://eel.is/c++draft/atomics.ref.generic)。原记录日期：2026-09-22。

<a id="baseline-s21"></a>
**C++ working draft：Coroutines** — [原始材料](https://eel.is/c++draft/dcl.fct.def.coroutine)。原记录日期：2026-09-22。

<a id="baseline-s22"></a>
**C++ working draft：Bit-fields** — [原始材料](https://eel.is/c++draft/class.bit)。原记录日期：2026-09-22。

<a id="baseline-s23"></a>
**C++ working draft：no_unique_address** — [原始材料](https://eel.is/c++draft/dcl.attr.nouniqueaddr)。原记录日期：2026-09-22。

<a id="baseline-s24"></a>
**C++ working draft：Contracts** — [原始材料](https://eel.is/c++draft/basic.contract)。原记录日期：2026-09-22。

<a id="baseline-s25"></a>
**C++ working draft：SIMD** — [原始材料](https://eel.is/c++draft/simd)。原记录日期：2026-09-22。

<a id="baseline-s26"></a>
**CXX 官方文档：Rust／C++ bridge** — [原始材料](https://cxx.rs/)。原记录日期：2026-09-22。

<a id="baseline-s27"></a>
**ECMAScript working specification：Structured Data／Atomics** — [原始材料](https://tc39.es/ecma262/multipage/structured-data.html)。原记录日期：2026-09-22。

<a id="baseline-s28"></a>
**ECMAScript working specification：Memory Model** — [原始材料](https://tc39.es/ecma262/multipage/memory-model.html)。原记录日期：2026-09-22。

<a id="baseline-s29"></a>
**Rust f128 primitive 接口状态** — [原始材料](https://doc.rust-lang.org/std/primitive.f128.html)。原记录日期：2026-09-22。

<a id="baseline-s30"></a>
**TypeScript Handbook：类型检查与类型注解擦除** — [原始材料](https://www.typescriptlang.org/docs/handbook/2/basic-types.html)。原记录日期：2026-09-22。

<a id="baseline-r1"></a>
**SEC《语言能力映射与最小控制》；经 GitHub connector 读取** — [原始材料](https://github.com/sec-platform/sec/blob/main/docs/作者/需求关系与能力边界.md)。原记录日期：2026-09-22。

<a id="baseline-r2"></a>
**SEC《总体架构》；经 GitHub connector 读取** — [原始材料](https://github.com/sec-platform/sec/blob/main/docs/架构/总体设计.md)。原记录日期：2026-09-22。

<a id="baseline-r3"></a>
**SEC《产品要求与工作约束》；经 GitHub connector 读取** — [原始材料](https://github.com/sec-platform/sec/blob/main/docs/产品/产品要求与工作约束.md)。原记录日期：2026-09-22。

<a id="baseline-r4"></a>
**SEC《原则总纲与归属》；经 GitHub connector 读取** — [原始材料](https://github.com/sec-platform/sec/blob/main/docs/产品/原则总纲与归属.md)。原记录日期：2026-09-22。

<a id="research-r01"></a>
**Rust 1.98.1 发布公告** — [原始材料](https://blog.rust-lang.org/2026/09/03/Rust-1.98.1/)。原记录日期：2026-09-22。

<a id="research-r02"></a>
**Rust 1.98.0：原子视图与 algebraic 浮点操作** — [原始材料](https://blog.rust-lang.org/2026/08/20/Rust-1.98.0/)。原记录日期：2026-09-22。

<a id="research-r03"></a>
**Rust 泛型参数与 const 参数限制** — [原始材料](https://doc.rust-lang.org/stable/reference/items/generics.html)。原记录日期：2026-09-22。

<a id="research-r04"></a>
**Rust impl、孤儿规则与重叠检查** — [原始材料](https://doc.rust-lang.org/stable/reference/items/implementations.html)。原记录日期：2026-09-22。

<a id="research-r05"></a>
**Rust 关联类型、GAT 与 receiver** — [原始材料](https://doc.rust-lang.org/stable/reference/items/associated-items.html)。原记录日期：2026-09-22。

<a id="research-r06"></a>
**Rust traits、泛型方法与 dyn compatibility** — [原始材料](https://doc.rust-lang.org/stable/reference/items/traits.html)。原记录日期：2026-09-22。

<a id="research-r07"></a>
**Rust 布局与数组对齐** — [原始材料](https://doc.rust-lang.org/stable/reference/type-layout.html)。原记录日期：2026-09-22。

<a id="research-r08"></a>
**Rust Pin 合同** — [原始材料](https://doc.rust-lang.org/stable/std/pin/index.html)。原记录日期：2026-09-22。

<a id="research-r09"></a>
**Rust MaybeUninit** — [原始材料](https://doc.rust-lang.org/stable/std/mem/union.MaybeUninit.html)。原记录日期：2026-09-22。

<a id="research-r10"></a>
**Rust Clone 与 clone_from** — [原始材料](https://doc.rust-lang.org/stable/std/clone/trait.Clone.html)。原记录日期：2026-09-22。

<a id="research-r11"></a>
**Rust Index 的引用返回接口** — [原始材料](https://doc.rust-lang.org/stable/std/ops/trait.Index.html)。原记录日期：2026-09-22。

<a id="research-r12"></a>
**Rust Fn 与手写实现边界** — [原始材料](https://doc.rust-lang.org/stable/std/ops/trait.Fn.html)。原记录日期：2026-09-22。

<a id="research-r13"></a>
**Rust 过程宏的 TokenStream 接口** — [原始材料](https://doc.rust-lang.org/stable/reference/procedural-macros.html)。原记录日期：2026-09-22。

<a id="research-r14"></a>
**Rust 常量求值** — [原始材料](https://doc.rust-lang.org/stable/reference/const_eval.html)。原记录日期：2026-09-22。

<a id="research-r15"></a>
**Rust specialization 实验能力** — [原始材料](https://doc.rust-lang.org/unstable-book/language-features/specialization.html)。原记录日期：2026-09-22。

<a id="research-r16"></a>
**Rust generic_const_exprs 实验能力** — [原始材料](https://doc.rust-lang.org/unstable-book/language-features/generic-const-exprs.html)。原记录日期：2026-09-22。

<a id="research-r17"></a>
**Rust 重载实验公告，2026-08-19** — [原始材料](https://blog.rust-lang.org/inside-rust/2026/08/19/overloading-experiment/)。原记录日期：2026-09-22。 实验不是 stable 承诺，也不是 Gift 必须采纳的设计。

<a id="research-r18"></a>
**Rust 可见性与隐私** — [原始材料](https://doc.rust-lang.org/stable/reference/visibility-and-privacy.html)。原记录日期：2026-09-22。

<a id="research-r19"></a>
**Rust Any 及 static 约束** — [原始材料](https://doc.rust-lang.org/stable/std/any/trait.Any.html)。原记录日期：2026-09-22。

<a id="research-r20"></a>
**Rust 未定义行为边界** — [原始材料](https://doc.rust-lang.org/stable/reference/behavior-considered-undefined.html)。原记录日期：2026-09-22。

<a id="research-r21"></a>
**Rust FFI、opaque 类型与 unwind** — [原始材料](https://doc.rust-lang.org/nomicon/ffi.html)。原记录日期：2026-09-22。 foreign catch 的具体结论优先依当前标准库 R22；Nomicon 与 API 文档存在表述差异。

<a id="research-r22"></a>
**catch_unwind：foreign exception 行为** — [原始材料](https://doc.rust-lang.org/stable/std/panic/fn.catch_unwind.html)。原记录日期：2026-09-22。

<a id="research-r23"></a>
**Rust C variadic 定义仍属实验能力** — [原始材料](https://doc.rust-lang.org/unstable-book/language-features/c-variadic.html)。原记录日期：2026-09-22。

<a id="research-r24"></a>
**Rust 泛型 Allocator API** — [原始材料](https://doc.rust-lang.org/stable/std/alloc/trait.Allocator.html)。原记录日期：2026-09-22。

<a id="research-r25"></a>
**Rust Atomic 泛型名称、primitive 范围和实验标记** — [原始材料](https://doc.rust-lang.org/stable/std/sync/atomic/struct.Atomic.html)。原记录日期：2026-09-22。 文档位于 stable 路径不表示页内全部项目均稳定。

<a id="research-r26"></a>
**Rust thread_local! 的 const 初始化优化** — [原始材料](https://doc.rust-lang.org/stable/std/macro.thread_local.html)。原记录日期：2026-09-22。

<a id="research-r27"></a>
**Rust portable std::simd** — [原始材料](https://doc.rust-lang.org/stable/std/simd/index.html)。原记录日期：2026-09-22。

<a id="research-r28"></a>
**Rust 一般 coroutine 实验能力** — [原始材料](https://doc.rust-lang.org/unstable-book/language-features/coroutines.html)。原记录日期：2026-09-22。

<a id="research-r29"></a>
**Rust volatile 操作** — [原始材料](https://doc.rust-lang.org/stable/std/ptr/fn.read_volatile.html)。原记录日期：2026-09-22。

<a id="research-r30"></a>
**Rust x86 MXCSR：默认环境假设及汇编隔离** — [原始材料](https://doc.rust-lang.org/stable/core/arch/x86_64/fn._mm_setcsr.html)。原记录日期：2026-09-22。

<a id="research-r31"></a>
**Rust f128 当前状态** — [原始材料](https://doc.rust-lang.org/stable/std/primitive.f128.html)。原记录日期：2026-09-22。

<a id="research-r32"></a>
**Rust 平台支持分级** — [原始材料](https://doc.rust-lang.org/stable/rustc/platform-support.html)。原记录日期：2026-09-22。

<a id="research-r33"></a>
**Rust 代码生成选项** — [原始材料](https://doc.rust-lang.org/stable/rustc/codegen-options/index.html)。原记录日期：2026-09-22。

<a id="research-r34"></a>
**Rust 显式尾调用实验** — [原始材料](https://doc.rust-lang.org/unstable-book/language-features/explicit-tail-calls.html)。原记录日期：2026-09-22。

<a id="research-r35"></a>
**Cargo SemVer：语言一致性不等于一切演进兼容** — [原始材料](https://doc.rust-lang.org/stable/cargo/reference/semver.html)。原记录日期：2026-09-22。

<a id="research-l01"></a>
**frunk 0.5.0 异构列表：map/fold/zip** — [原始材料](https://docs.rs/frunk/latest/frunk/hlist/index.html)。原记录日期：2026-09-22。

<a id="research-l02"></a>
**generic-array 1.4.5：关联长度的定长存储** — [原始材料](https://docs.rs/generic-array/latest/generic_array/)。原记录日期：2026-09-22。

<a id="research-l03"></a>
**pin-init 0.2.0：堆与栈原位初始化** — [原始材料](https://docs.rs/pin-init/latest/pin_init/)。原记录日期：2026-09-22。

<a id="research-l04"></a>
**allocator-api2 0.4.0：稳定工具链上的独立容器/API** — [原始材料](https://docs.rs/allocator-api2/latest/allocator_api2/)。原记录日期：2026-09-22。

<a id="research-l05"></a>
**atomic 0.6.1：泛型原子库及 NoUninit 约束** — [原始材料](https://docs.rs/atomic/latest/atomic/)。原记录日期：2026-09-22。

<a id="research-l06"></a>
**portable-atomic 1.15：目标宽度、检测和锁回退** — [原始材料](https://docs.rs/portable-atomic/latest/portable_atomic/)。原记录日期：2026-09-22。

<a id="research-l07"></a>
**atomic-wait 1.1.0：wait/wake 库** — [原始材料](https://docs.rs/atomic-wait/latest/atomic_wait/)。原记录日期：2026-09-22。

<a id="research-i01"></a>
**Crubit 支持状态** — [原始材料](https://crubit.rs/overview/status)。原记录日期：2026-09-22。 官方状态页自带可能滞后说明；某个桥接器的限制不代表全部 Rust 方案。

<a id="research-i02"></a>
**Rust 2026 interop 问题图** — [原始材料](https://goals.rust-lang.org/2026/interop-problem-map.html)。原记录日期：2026-09-22。 工作方向不等于能力已经发布。

<a id="research-c01"></a>
**C++ working draft：派生对象** — [原始材料](https://eel.is/c++draft/class.derived)。原记录日期：2026-09-22。 动态 working draft 不整体等于 C++23；只引用相应既有规则。

<a id="research-c02"></a>
**C++ working draft：模板参数** — [原始材料](https://eel.is/c++draft/temp.param)。原记录日期：2026-09-22。

<a id="research-c03"></a>
**C++ working draft：部分特化** — [原始材料](https://eel.is/c++draft/temp.spec.partial)。原记录日期：2026-09-22。

<a id="research-c04"></a>
**C++ working draft：模板推导** — [原始材料](https://eel.is/c++draft/temp.deduct)。原记录日期：2026-09-22。

<a id="research-c05"></a>
**C++ working draft：copy elision** — [原始材料](https://eel.is/c++draft/class.copy.elision)。原记录日期：2026-09-22。

<a id="research-c06"></a>
**C++ working draft：初始化** — [原始材料](https://eel.is/c++draft/dcl.init)。原记录日期：2026-09-22。

<a id="research-c07"></a>
**C++ working draft：bit-fields** — [原始材料](https://eel.is/c++draft/class.bit)。原记录日期：2026-09-22。

<a id="research-c08"></a>
**C++ working draft：no_unique_address** — [原始材料](https://eel.is/c++draft/dcl.attr.nouniqueaddr)。原记录日期：2026-09-22。

<a id="research-c09"></a>
**C++ working draft：atomic_ref** — [原始材料](https://eel.is/c++draft/atomics.ref.generic)。原记录日期：2026-09-22。

<a id="research-c10"></a>
**C++ working draft：contracts** — [原始材料](https://eel.is/c++draft/basic.contract)。原记录日期：2026-09-22。

<a id="research-c11"></a>
**C++ working draft：exception specifications** — [原始材料](https://eel.is/c++draft/except.spec)。原记录日期：2026-09-22。

<a id="research-c12"></a>
**WG21 P2996R13：C++26 reflection 设计** — [原始材料](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2025/p2996r13.html)。原记录日期：2026-09-22。 提案内容与具体编译器实施分别判断。

<a id="research-c13"></a>
**WG21 P4197R0：trivial relocation 的 C++29 重议** — [原始材料](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p4197r0.html)。原记录日期：2026-09-22。

<a id="research-t01"></a>
**GCC C++ 特性状态表** — [原始材料](https://gcc.gnu.org/projects/cxx-status.html)。原记录日期：2026-09-22。 核验到 C++26 reflection/contracts/expansion 的逐特性状态；不是本环境的已安装编译器状态。

<a id="research-t02"></a>
**Clang Users Manual：浮点环境与 strict 模式** — [原始材料](https://clang.llvm.org/docs/UsersManual.html)。原记录日期：2026-09-22。

<a id="research-t03"></a>
**Clang 扩展** — [原始材料](https://clang.llvm.org/docs/LanguageExtensions.html)。原记录日期：2026-09-22。

<a id="research-t04"></a>
**GCC Labels as Values：computed goto** — [原始材料](https://gcc.gnu.org/onlinedocs/gcc/Labels-as-Values.html)。原记录日期：2026-09-22。 GNU 扩展，不是 ISO C++ 普通 goto；还存在析构语义边界。

<a id="research-c14"></a>
**C++ friend 精确授权及非传递性** — [原始材料](https://eel.is/c++draft/class.friend)。原记录日期：2026-09-22。

<a id="research-c15"></a>
**C++ 默认参数：求值、作用域及静态选择** — [原始材料](https://eel.is/c++draft/dcl.fct.default)。原记录日期：2026-09-22。

<a id="research-c16"></a>
**C++ 重载选择与选中后的访问检查** — [原始材料](https://eel.is/c++draft/over.match)。原记录日期：2026-09-22。

<a id="research-c17"></a>
**C++ 泛型 lambda 的 closure 类型** — [原始材料](https://eel.is/c++draft/expr.prim.lambda.closure)。原记录日期：2026-09-22。

<a id="research-r36"></a>
**Rust closure 类型与调用 trait** — [原始材料](https://doc.rust-lang.org/stable/reference/types/closure.html)。原记录日期：2026-09-22。

<a id="research-i03"></a>
**CXX Result<T> 的异常转译** — [原始材料](https://cxx.rs/binding/result.html)。原记录日期：2026-09-22。

## 本次定向核验

2026-09-22 读取的 Rust 官方 catch_unwind 文档显示版本 1.98.1，并明确外来 unwind 的捕获结果不能被当作通用 C++ typed catch；原比较的这一限制保留。Rust Reference 的泛型规则继续区分可用常量参数类型及 standalone const 表达式，不把已知实例展开误认为开放原生 API。浮点环境与代数运算另以 _mm_setcsr 和 f64 官方文档校准。其余表中的版本状态属于原记录，没有因复制到本篇而取得新的“最新”确认。

<a id="current-fp-evidence"></a>
### 浮点环境与代数运算的定向复核

2026-09-22：Rust 官方 `_mm_setcsr` 文档明确环境改变与默认优化假设的风险，要求实际封闭相关指令与恢复；f64 文档在 1.98.0 起列出稳定的 `algebraic_add/sub/mul/div/rem`。前者约束方法合法性，后者只是可选择的不同运算语义，不提供性能或适合性证明。来源：[环境](https://doc.rust-lang.org/core/arch/x86/fn._mm_setcsr.html)、[f64](https://doc.rust-lang.org/std/primitive.f64.html)。

2026-09-22：C++ 工作草案 class.access.general 规定语言构造的解释先于访问控制；这支持本补充不提前剔除 private 最佳重载的决定。工作草案不是当前所有编译器均已实现的证明。来源：[访问控制](https://eel.is/c++draft/class.access.general)。
