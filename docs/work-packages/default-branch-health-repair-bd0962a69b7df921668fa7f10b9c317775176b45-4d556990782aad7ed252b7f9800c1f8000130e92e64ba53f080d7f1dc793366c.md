---
schema: codex-development-work-package-v1
id: default-branch-health-repair-bd0962a69b7df921668fa7f10b9c317775176b45-4d556990782aad7ed252b7f9800c1f8000130e92e64ba53f080d7f1dc793366c
tracking: none
base: bd0962a69b7df921668fa7f10b9c317775176b45
manifestState: frozen
requiredProfile: quick
ciRevision: ci-verification-v19
authorityRefs:
  - development-governance
  - verification-governance
tasks:
  - id: affected-git-observation-owner
    owner: verification-test-impact-owner
    ownedPaths:
      - platform/dev-runner/test-runner.ts
      - platform/shared/test-impact-rules/verification.ts
      - tests/contract/dev-runner-contract.test.ts
      - tests/contract/test-impact.test.ts
      - tests/helpers/dev-runner-authority-proof.ts
      - tests/unit/dev-runner-authority-proof.test.ts
      - tests/unit/test-runner.test.ts
  - id: selector-calibration-authority
    owner: verification-governance-owner
    ownedPaths:
      - docs/verification-governance.md
  - id: document-control-child-failure-envelope
    owner: document-control-a0-activation-owner
    ownedPaths:
      - tests/contract/document-control-plane-lifecycle.test.ts
      - tests/helpers/document-control-freeze-child-failure.ts
  - id: generated-trust-closure
    owner: trusted-verifier-tcb-owner
    ownedPaths:
      - platform/shared/tcb-closure-lock.ts
      - tests/contract/tcb-closure-lock.test.ts
  - id: durable-plan-and-control
    owner: development-governance-owner
    ownedPaths:
      - docs/work-packages/default-branch-health-repair-421d5544dc51820c4f05cf8011415cf4beada468-29f0234d8afcd2086d0763abcded409ed009d5704d63b24a9ea2a6c3d4313fd5.md
      - docs/work-packages/default-branch-health-repair-bd0962a69b7df921668fa7f10b9c317775176b45-4d556990782aad7ed252b7f9800c1f8000130e92e64ba53f080d7f1dc793366c.md
      - docs/work/active-work-package.md
      - docs/work/rolling-plan.md
forbiddenPaths:
  - .agents/skills/
  - .codex/
  - .githooks/
  - .github/workflows/
  - AGENTS.md
  - bun.lock
  - docs/authority.json
  - docs/development-governance.md
  - package.json
  - platform/compiler/
  - scripts/
  - source/
acceptance:
  - the repair binds exact live main bd0962a69b7df921668fa7f10b9c317775176b45 tree f90200cc6a6b3fddbdd0d587023b4fb95838f77a and reuses MainHealth run 31599440295 job 94122873749 failure fingerprint sha256:d45db4cba70f7c03331169a18b8b32ab984c97d4a4bcff626555b08b8c3c9205 without rerunning the unchanged failed input
  - imports exact-main TypeScript and documentation checks passed before the complete fast inventory exposed exactly two dev-runner contract failures so the failure is one selector-calibration and direct-consumer closure defect rather than a product regression environment transient or reason to enter ordinary Issue selection
  - the finite authority proof removes the historical exact-two-process-call magic number and instead requires every shared byte-process call to remain a direct literal-git use owned by the private changed-file observation closure with no module-initializer alias exposure or alternate process capability
  - changed-file discovery uses only canonical exact-revision diff untracked and path-blob argument builders and keeps path-blob execution directly inside the private observation owner rather than an anonymous nested authority seam
  - the dev-runner contract asserts canonical changed records exact base-head transition observation removed-blob provenance and transition-aware affected and risk consumers rather than stale name-only parsing or pre-transition function signatures
  - platform shared ci-git-changed-files has one explicit affected-test-selection owner edge to the dynamically reading dev-runner contract so future changes select that non-import consumer without broadening every CI source to the complete fast inventory
  - the selector regression proves the source selects the dev-runner contract and the existing focused CI transition unit closure while unknown sources remain unresolved and unrelated verification infrastructure does not acquire this extra test
  - Full and MainHealth remain calibration backstops rather than edit-loop defaults and the exact missed relation invalidates prior selector completeness evidence until this permanent edge and focused regression enter new main
  - the production repair renderer consumes only the fresh content-addressed MainHealthRepairDecision retires the byte-identical published active projection and preserves all unresolved candidates byte-for-byte and in order without ordinary WorkDecision or manual pointer staging
  - one logical run retains one mutable physical worktree and one active candidate ref; the two merged predecessor repair worktrees and local refs plus three stale remote-tracking projections are removed only after exact PR merge tree parity clean-state and live-remote absence readback
  - Windows worktree closeout failure from default long-path handling remains owned by roadmap Issue 186 and its canonical residue contract; this repair uses command-scoped core.longPaths only for the exact already-authorized residue and does not mutate global Git configuration or duplicate the future production owner
  - this repair tracks no Issue and closes or reopens no Issue; Issue completion remains controlled by post-main acceptance and remaining-work evidence rather than PR text or a repair manifest
  - the published predecessor repair manifest has zero remaining activation consumers and is deleted while the byte-exact Issue 346 predecessor required by delayed WorkDecision handoff remains present
  - exact TCB closure is regenerated once after the final source delta and every generated module blob and raw digest binds the frozen candidate head
  - hosted bootstrap run 31616348309 completed the 196-test focused trusted union then exposed only the affected-selected mechanism fixture's stale whole-scenario PASS oracle; the failed execution is reused and only that causal unit case is rerun after the oracle separation
  - a mechanism-only synthetic Program fixture intentionally omitting the canonical changed-file observation owner is classified rejected with only PROCESS_OWNER_CONTRACT while the complete production positive remains the sole zero-finding authority fixture
  - successor hosted bootstrap run 31617577084 reused the corrected unit evidence and exposed one platform-dependent child stack transport assertion after 93 of 94 document-control lifecycle cases passed; the child now emits one closed JSON failure envelope and the parent validates semantic fields without stack substring matching
  - the child failure boundary uses one shared pure compiler renderer and parser with exact safe tuples canonical single-line UTF-8 byte and code-point bounds and a fixed sanitized projection for unknown oversized path-bearing or arbitrary-code errors
  - verification runs the previously failing dev-runner contract once after the causal delta then only the selector-owned focused union TypeScript imports docs and TCB closure required by the final changed paths; same-input broad reruns are forbidden
  - integration requires exact-head independent Review authorized single-candidate merge candidate-tree parity remote main pointer and successful automatic MainHealth readback before any branch worktree artifact or Issue closeout is reported complete
tests:
  - tests/contract/dev-runner-contract.test.ts
  - tests/contract/document-control-plane-lifecycle.test.ts
  - tests/contract/test-impact.test.ts
  - tests/unit/dev-runner-authority-proof.test.ts
  - tests/unit/test-runner.test.ts
  - tests/contract/docs-doctor.test.ts
  - tests/contract/documentation-corpus-census.test.ts
  - tests/contract/documentation-authority.test.ts
  - tests/contract/tcb-closure-lock.test.ts
---

# Affected Git observation contract and selector calibration repair

This package repairs the deterministic complete-fast failure emitted by the first automatic MainHealth observation
of `main@bd0962a69b7df921668fa7f10b9c317775176b45`. The production changed-file implementation is valid; the
failure exposed two stale proof surfaces: a magic-number process-call invariant and a missing explicit edge to a
contract that reads the implementation dynamically rather than importing it.

The repair replaces count-based authority with capability containment, records the direct consumer edge in the
single test-impact owner, and updates only the affected contract vocabulary. It does not turn Full into an ordinary
development gate, create a second selector, enter the ordinary Issue catalog, or absorb the separately owned
physical worktree closeout program.
