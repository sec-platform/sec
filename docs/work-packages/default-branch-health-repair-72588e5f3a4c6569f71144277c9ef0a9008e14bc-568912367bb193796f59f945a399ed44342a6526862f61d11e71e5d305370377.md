---
schema: codex-development-work-package-v1
id: default-branch-health-repair-72588e5f3a4c6569f71144277c9ef0a9008e14bc-568912367bb193796f59f945a399ed44342a6526862f61d11e71e5d305370377
tracking: none
base: 72588e5f3a4c6569f71144277c9ef0a9008e14bc
manifestState: frozen
requiredProfile: quick
ciRevision: ci-verification-v19
authorityRefs:
  - verification-governance
tasks:
  - id: appcontainer-recovery-owner-behavior-source-lock
    owner: windows-appcontainer-hardening
    ownedPaths:
      - tests/unit/windows-appcontainer-hardening-static.test.ts
  - id: durable-plan-and-control
    owner: development-governance-owner
    ownedPaths:
      - docs/work-packages/default-branch-health-repair-3f78965551272d7cbfec70f66927003c55521aea-7d07a88ab40ad024785d4bb5d532ef21cea018bd7d2b35eab1bbe0ac1e6679df.md
      - docs/work-packages/default-branch-health-repair-72588e5f3a4c6569f71144277c9ef0a9008e14bc-568912367bb193796f59f945a399ed44342a6526862f61d11e71e5d305370377.md
      - docs/work/active-work-package.md
      - docs/work/rolling-plan.md
forbiddenPaths:
  - .agents/skills/
  - .codex/
  - .githooks/
  - .github/workflows/
  - AGENTS.md
  - bun.lock
  - docs/authority.json
  - docs/development-governance.md
  - docs/verification-governance.md
  - package.json
  - platform/compiler/
  - platform/dev-runner/
  - platform/orchestrator.ts
  - platform/orchestrator/
  - scripts/
  - source/
acceptance:
  - the repair binds exact live main 72588e5f3a4c6569f71144277c9ef0a9008e14bc tree 66b53b9d74e625fc61e01a6d2e3dc90d833e8dd0 and reuses automatic MainHealth run 31627912743 job 94219047431 failure fingerprint sha256:72117e6dc947a2a6932fe3adcd0191427f373b2f30cb02122d22f2ef58e97c9b without rerunning the unchanged exact-main complete fast inventory
  - imports typecheck and documentation authority passed on exact main; complete fast stopped in concurrent shard batch six with one observed Windows AppContainer static-test failure while every not-started later invocation remains explicitly unknown rather than claimed passing
  - base and merged-candidate trees contain byte-identical failing test and production executor blobs so this repair is a pre-existing reference drift repair rather than a regression attribution to the preceding verification source-lock candidate
  - the production AppContainer executor blob remains byte-identical to exact base and exact-diff evidence proves this repair adds no public test export; the test appends a private runner only to the exact in-memory module text, invokes the existing assertRecoveryOwner implementation in that module scope, bundles through the executor existing static import graph, and executes every projection in a separate Bun process and unique temporary filesystem transaction
  - the persisted fixture is placed independently through WINDOWS_APPCONTAINER_RECOVERY_CONTRACT_V1.ownerFileName rather than the private recoveryOwnerPath subject; the bounded behavioral matrix accepts a canonically equal owner whose JSON key order differs and requires cleanup for absent malformed or unequal owners including independently valid persisted-only mismatches of every mutable owner identity field plus workspace staging identity staging directory AppContainer name requested-result-path and subject-read-path admission mismatches
  - the exact production behavior matrix rather than a source analyzer or production-derived mutant generator is the acceptance oracle because reordered canonical JSON distinguishes the retired comparator, persisted-only field mismatches reject partial or misbound comparison, independent canonical fixture placement rejects bypassed or wrong-path persisted reads, the canonical positive rejects a non-awaited read, malformed owners reject dereference-before-validation, and admission scenarios reject expected-owner and boundary disagreement under the existing production helpers without claiming that the matrix independently proves those helper semantics
  - every projection uses the canonical observed-process lifecycle with separate fixed stdout and stderr acquisition bounds canonical UTF-8 JSON bytes one execution deadline bounded graceful-to-forced tree termination and proven child close stream drain and tree settlement before the temporary transaction can be removed; any unproven settlement retains the isolated diagnostic root rather than deleting files under an unclosed process tree
  - the behavioral projection claims only the observable recovery-owner admission read and comparison contract exercised by that matrix; it does not claim universal JavaScript capability immutability whole-module effect closure or arbitrary program equivalence
  - the static source-lock no longer claims recovery-owner equality through raw source spelling; apart from the explicitly retired cross-owner source-read and assertion groups, the remaining owner-local AppContainer ABI lifecycle capability redaction and cleanup assertions are unchanged
  - the AppContainer source-lock explicitly reads only the three windows-appcontainer-hardening source identities already bound to it by their unique test-impact owner; the projection starts from the exact executor already imported by the test and follows its canonical static import closure without adding a dynamically named source identity, while unrelated compiler orchestrator facade and staging-tree reads are removed rather than assigned a second owner
  - after the initial semantic delta the AppContainer source-lock test ran before one candidate-tree complete fast calibration used the runner-observed global budget as explicit inner concurrency so the current managed policy yielded outer process concurrency one; that calibration exposed one out-of-scope shared-dependency resource failure and later projection-only hardening reruns only the invalidated focused closure rather than repeating the unchanged failure path
  - pre-final discovery runs are not Gate evidence; one observed an unstaged predecessor-manifest census mismatch and Windows shared-dependency EPERM while another demonstrated that explicit inner concurrency one expands outer process concurrency to the full global budget and produced shared-dependency timeouts plus unrelated mode-recovery failures; none is claimed passing or repeated with identical tree and execution plan
  - an in-scope deterministic complete-fast implementation failure blocks Review and is reconciled only within frozen scope; an out-of-scope product or runner-resource finding stops for its canonical authority decision rather than expanding this manifest, while a pass remains diagnostic pre-merge calibration and does not replace hosted Quick evidence
  - final verification runs only the invalidated manifest-required AppContainer behavioral source-lock document authority imports and typecheck closure; the prior outer-serialized complete-fast calibration remains diagnostic discovery and affected full release nightly and the unchanged out-of-scope local failure path are not mechanically rerun
  - the production repair renderer consumes only the fresh content-addressed MainHealthRepairDecision retires the byte-identical published active projection and preserves unresolved ordinary candidates byte-for-byte and in order without ordinary WorkDecision or manual pointer staging
  - one logical run retains one short-path mutable physical worktree and one active candidate ref; predecessor branch worktree and remote refs are already absent by exact readback and no unrelated tmp worktree is touched
  - this repair tracks no Issue and closes or reopens no Issue; Issue completion remains controlled by post-main acceptance remaining-work and consumer evidence rather than PR text or this temporary manifest
  - the published predecessor repair manifest is deleted only after the new repair freeze while the byte-exact Issue 346 predecessor required by delayed WorkDecision handoff remains present
  - integration requires exact-head independent Review one exact candidate merge candidate-tree parity remote-main pointer readback successful automatic MainHealth and local-main readiness before branch worktree artifact or Issue closeout is reported complete
tests:
  - tests/unit/windows-appcontainer-hardening-static.test.ts
  - tests/contract/docs-doctor.test.ts
  - tests/contract/documentation-authority.test.ts
---

# AppContainer recovery-owner semantic source-lock repair

The automatic MainHealth run for `main@72588e5f3a4c6569f71144277c9ef0a9008e14bc` reached a
previously unobserved fast-test shard and found a source-lock that still required the retired
`JSON.stringify` spelling after production had converged on the canonical equality primitive.
The production recovery-owner boundary is unchanged and already belongs to the
`windows-appcontainer-hardening` impact owner.

This repair replaces the formatting-sensitive assertion with a bounded exact-module behavioral
projection. The test appends a private runner only to the in-memory production source, compiles the
real module and its existing import closure, and observes the recovery-owner boundary through real
filesystem records placed through the public recovery contract in isolated bounded processes. The
canonical scenario distinguishes canonical equality from the retired order-sensitive JSON
comparison, while independent negative scenarios prove that the exercised admission boundary fails
closed. The normal production test does not construct source mutants or depend on comparator,
reader, validator, parameter or local-binding spelling. The test does not claim a
universal JavaScript effect proof and also stops dynamically reading four sources owned by other
architecture domains.

Because the preceding MainHealth execution stopped before later invocations, the candidate ran
one complete fast calibration after the initial semantic delta. Only actually observed outcomes are
claimed. That calibration exposed an out-of-scope shared-dependency resource failure; subsequent
projection-only hardening therefore reruns the invalidated focused closure and reuses the unchanged
failure instead of repeating the same broad execution. A new blocker inside this frozen scope is
reconciled in the same worktree and candidate ref; a blocker outside the scope stops for a new
authority decision and refreeze before any write.
