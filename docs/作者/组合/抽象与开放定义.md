# 工程语言与用户定义

> **阅读范围**：采用范围内的设计规范；实现状态另查未决。

<details>
<summary>相关主题与上下文</summary>

- [抽象可组合的准入与自由度](#抽象可组合的准入与自由度)
- [有限核心、开放定义与固定作者语言](#有限核心开放定义与固定作者语言)
- [语言无关的作者合同与当前规格](#语言无关的作者合同与当前规格)
- [抽象工程语言与表达载体选择](#抽象工程语言与表达载体选择)
- [通用表达与渐进具体化](#通用表达与渐进具体化)
- [小程序与局部构造不是另一种产品](#小程序与局部构造不是另一种产品)
- [意图粒度与可复用功能组合](定义与接口.md#意图粒度与可复用功能组合)
- [需求关系与程序构造分别复用](#需求关系与程序构造分别复用)
- [意图、逻辑与可编译工程模型](定义与接口.md#意图逻辑与可编译工程模型)
- [控制逻辑、能力管脚与库绑定](定义与接口.md#控制逻辑能力管脚与库绑定)
- [工程组成、配置、数据与依赖](定义与接口.md#工程组成配置数据与依赖)
- [类型、效果与错误语义](../../领域/计算基础/通用计算与任务语义.md#类型效果与错误语义)
- [需求、行为、设计选择与实现的关系](#需求行为设计选择与实现的关系)
- [语义扩展、领域方言与通用接口](语义扩展与领域接合.md#语义扩展领域方言与通用接口)
- [领域模块与系统定义编译](语义扩展与领域接合.md#领域模块与系统定义编译)
- [一般程序语义与计算表达能力](../../领域/计算基础/通用计算与任务语义.md#一般程序语义与计算表达能力)
- [用户定义语言、语义与编译扩展](语义扩展与领域接合.md#用户定义语言语义与编译扩展)
- [作者语法与前端支持边界](#作者语法与前端支持边界)
- [类型与包实例的身份](../../领域/计算基础/通用计算与任务语义.md#类型与包实例的身份)
- [声明环与运行初始化](../../领域/计算基础/通用计算与任务语义.md#声明环与运行初始化)
- [并发内存与可观察行为](../../领域/计算基础/通用计算与任务语义.md#并发内存与可观察行为)
- [可选同步通用语义画像](../../领域/计算基础/通用计算与任务语义.md#可选同步通用语义画像)
- [算法、计算结构与事务作者面的分工](#算法计算结构与事务作者面的分工)
- [封装、类库与开放定义的共同位置](#封装类库与开放定义的共同位置)
- [扩展由普通定义到核心演进的实际分层](语义扩展与领域接合.md#扩展由普通定义到核心演进的实际分层)
- [有限层级状态是库与领域方法而非新增全局语法](../../领域/计算基础/通用计算与任务语义.md#有限层级状态是库与领域方法而非新增全局语法)
- [关系不是普通List的别名，也不是数据库的别名](../../领域/计算基础/通用计算与任务语义.md#关系不是普通list的别名也不是数据库的别名)
- [有限对象、流与微分的当前采用边界](../../领域/计算基础/通用计算与任务语义.md#有限对象流与微分的当前采用边界)
- [需求复合定义与实现主体分别闭合](#需求复合定义与实现主体分别闭合)

</details>

### 分题阅读

- [Block边界与管脚](定义与接口.md)
- [语义扩展与领域接合](语义扩展与领域接合.md)
- [通用计算与任务语义](../../领域/计算基础/通用计算与任务语义.md)

<details>
<summary>本页内容</summary>

- [抽象可组合的准入与自由度](#抽象可组合的准入与自由度)
- [有限核心、开放定义与固定作者语言](#有限核心开放定义与固定作者语言)
- [语言无关的作者合同与当前规格](#语言无关的作者合同与当前规格)
- [抽象工程语言与表达载体选择](#抽象工程语言与表达载体选择)
- [通用表达与渐进具体化](#通用表达与渐进具体化)
- [小程序与局部构造不是另一种产品](#小程序与局部构造不是另一种产品)
- [需求关系与程序构造分别复用](#需求关系与程序构造分别复用)
- [需求、行为、设计选择与实现的关系](#需求行为设计选择与实现的关系)
- [作者语法与前端支持边界](#作者语法与前端支持边界)
- [算法、计算结构与事务作者面的分工](#算法计算结构与事务作者面的分工)
- [封装、类库与开放定义的共同位置](#封装类库与开放定义的共同位置)
- [需求复合定义与实现主体分别闭合](#需求复合定义与实现主体分别闭合)

</details>


本章负责高层工程语义、用户定义与扩展。基础作者语言的整体风格、文件、运行语义和开发闭环统一见[语言体系与开发规格](../可替换表示与适用范围.md)，其中明确承认中立基础是一门新语言；语言无关不等于没有语言。这里的高级和历史材料不自动扩大基础文法。

- [语言无关的作者合同与当前规格](../../领域/计算基础/文法类型与求值.md#语言无关的作者合同与当前规格)
- [意图粒度与可复用功能组合](定义与接口.md#意图粒度与可复用功能组合)
- [意图、逻辑与可编译工程模型](定义与接口.md#意图逻辑与可编译工程模型)
- [控制逻辑、能力管脚与库绑定](定义与接口.md#控制逻辑能力管脚与库绑定)
- [工程组成、配置、数据与依赖](定义与接口.md#工程组成配置数据与依赖)
- [类型、效果与错误语义](../../领域/计算基础/通用计算与任务语义.md#类型效果与错误语义)
- [语义扩展、领域方言与通用接口](语义扩展与领域接合.md#语义扩展领域方言与通用接口)
- [领域模块与系统定义编译](语义扩展与领域接合.md#领域模块与系统定义编译)
- [一般程序语义与计算表达能力](../../领域/计算基础/通用计算与任务语义.md#一般程序语义与计算表达能力)
- [用户定义语言、语义与编译扩展](语义扩展与领域接合.md#用户定义语言语义与编译扩展)
- [SEC 文本 DSL 候选与原型边界](#作者语法与前端支持边界)


## 抽象可组合的准入与自由度

公开定义、局部具体化、原生绑定和领域扩展共用[组合边界摘要](../共同语义关系.md#组合边界摘要与义务闭合算法)。新增普通算法只增加作者内容，不扩大核心工具或解析器目录。更高层定义内部能读取的字段、资源和契约必须有声明/实际依赖；把全部用户程序藏成不透明字符串不构成可理解的高层语义。

定义实例化时先绑定输入与所需合同，再检查局部修改对公开保证、捕获、效果和后续使用的影响。改内部算法而保持使用点需要的全部关系，可以保留外部绑定；无法确定保持时只使相应结论未决，不强迫所有消费者读完整展开。若新需求有意改变公开合同，则生成明确合同差异并传播，不以“局部实现”掩盖真实广泛影响。

组合默认从合格调用与边界构建。一个库只给函数名而没有可检查输入/错误/效果规则时，可以保全和正常原生接入，但不能据此完成更强的中立组合承诺。库有完整行为而缺某个优化接口时，允许使用正确基线，不把优化缺项当成表达能力缺项。

摘要与展开都是派生视图；作者只维护定义和真实差异。摘要不足时精化当前使用点或下钻，不追加每个表达式都要填写的合同表。真正新语义不能由接口空实现代替，也不因库不断增长要求AI每次重新阅读全部能力。


## 有限核心、开放定义与固定作者语言

### 不变的产品目标和五个独立坐标

SEC的公共身份、定义、关系、约束、行为、效果与变化合同不以TypeScript或任何目标语言为定义依据。TypeScript-first限定首个实现/目标次序，不取消Java、Go、Rust、C/C++、Python及后续语言的逐项接入。支持一个语言必须按观察、分析、生成、运行检查和往返分别说明范围，不能用一张品牌列表冒充全部支持。

| 坐标 | 谁拥有含义 | 改变它时不应强迫改变什么 |
|---|---|---|
| SEC实现语言 | 编译器与工具实现；当前仓库采用什么由真实实施事实决定 | 已保存工程的业务含义与公共作者协议 |
| 作者表示 | SEC中立文本、结构投影或显式原生前端 | 共同语义身份、要求与本次任务权限 |
| 输入资产语言 | TS、Java、Python等原生源码及各自解释合同 | 不得将其宿主行为悄悄提升为全语言规则 |
| 目标语言与宿主 | 目标程序IR、后端、运行库、ABI和实际环境 | 不能修改上游要求来凑可生成结果 |
| 存储与交换 | 文本、JSON、二进制、数据库各自格式 | 不得因换编码重定义类型、控制流或授权 |

语言无关不等于没有作者语言，也不等于所有目标具有相同能力。正常主链是“工程语义作者源→目标无关语义→按目标降低→真实源码”；原生导入是并列入口，不是先让AI把所有要求写成TS再回猜业务。保留原生路径提供生态和不透明资产自由，不以它替代中立语义的直接创作。

### 必须固定什么，什么可以继续增加

版本内固定的是作者文法、核心构造含义、公开基础库合同、六工具协议；开放的是用户函数、类型、谓词、连接、实例和模块。增加一个业务定义，不增加工具或关键字。可选中立计算文本的具体范围见[作者合同](../../领域/计算基础/文法类型与求值.md#语言无关的作者合同与当前规格)，原生前端单独绑定实际语言版本。

熟悉的调用、字段、条件与函数写法可以复用，但含义以所选作者语言为准。中立Int不是JS Number；中立记录没有隐含prototype/getter；原生TS的number、this、异常和初始化仍保留原生规则。相同外观不是语义等价证据。

### 八类工程构造的边界

这八类是共同模型的职责，不是八张必填表、八个业务原子或整个程序语言只有八个单词；它们通过[递归封装合同](定义与接口.md#block-composition-contract)构造开放定义，而不是对需求名称作集合并集。程序内部采用紧凑、有作用域的表达式和函数；仅需独立编辑、引用、共享约束或迁移的边界取得长期身份。

| 构造 | 输入与结果 | 不可省略的条件 |
|---|---|---|
| 声明 | 作用域、公开合同与定义体 | 类型、重复名、草稿；声明不隐式执行 |
| 引用 | 名称或稳定对象到实际绑定 | 可见性、版本、歧义、披露；引用不授予权限 |
| 实例化 | 定义及参数到本实例 | 参数含义、求值次数、实例寿命；同配置不必同状态 |
| 连接 | 类型化端口或关系及端点 | 类型、顺序、效果、共同不变量 |
| 约束 | 作用对象、谓词、要求与保证范围 | 要求不等于已验证事实；未知保留 |
| 绑定 | 要求与合格实现、后端或原生资产 | 保持、ABI、版本；偏好不覆盖硬条件 |
| 组合 | 成员、关系、局部定义和实现选择 | 卫生、不同种类的环、联合约束 |
| 输出 | 选定语义根、目标与必要布局 | 精确生产者、合法降低；输出声明不写磁盘 |

### 三种扩展不能都叫新增核心能力

新业务算法、类型、规则和组合由已有构造定义；新分析或后端通过明确接口接入；真正改变基本类型、时间、效果或解释规则时才升级语义画像。基础语法与基础库固定，不把“任意kind字符串”送进执行器。陌生方言可以保全，参与分析和生成须有已绑定的解释。

普通定义不能被新方言的发布门槛拖住；新方言也不能伪装成普通函数以绕过信任检查。当前未计划实现的强宿主能力不影响无外部作用的函数创作。

### 紧凑源文本是默认作者投影

语义主路径使用语言中立的紧凑文本或其同义结构投影。AI只写相关定义与差异，不手填节点ID、类型树、哈希、后端AST和回执。已绑定符号由解析器处理，不要求每次搜索能力目录。自然语言是提案入口；其解释被接受后保存明确语义，重建不重新询问模型。

中立源、原生源、目标代码是不同角色。每个区域只有一个可写作者来源，其余分析/展开可重建；不是同一逻辑同时维护TS、JSON和SEC三份源码。文件后缀是定位提示，实际解释由明确的画像和源角色决定。

### 固定基础库不等于重新做全部生态

基础类型环境、导入规则和本期序列库面见[基础环境与导入](../../领域/计算基础/名称导入与契约.md#基础环境与导入)。库不是关键词，不是已经发布的软件，也不需要注册成工具。缺少绑定时准确诊断；用户可以直接写函数定义，不强制检索替代库。

中立值语义与原生辅助库分开。原生getter/Proxy、稀疏数组、动态迭代器和SameValueZero必须按对应宿主合同处理，或者经明确边界解码；readonly不是纯度证明。中立文本的固定环境不能因安装一份库就静默变化。

### 有限性与紧凑性的反例

必须拒绝：已知符号仍让AI查ID；无关库增长改变工具提示；同名原生函数被误当中立算子；纯生成加载整个语言宇宙；更换后端使业务语义变化；依靠原生TS逃生口声称中立算法已经通用；没有目标实现却返回空函数。应允许：闭合短函数零工程目录、用户自定义算法与组合、库不合适时直接展开局部逻辑、不透明原生资产被保全但不获得虚假跨目标保证。

### 当前阅读层次

[语言体系与开发规格](../可替换表示与适用范围.md)规定基础文本、范围和完整开发过程；[产品级类型与资源](../../领域/计算基础/通用计算与任务语义.md#产品级复合类型调用与资源的具体默认设计)规定按需启用的更广语义；[历史结构子模型](../../领域/计算基础/通用计算与任务语义.md#参考核心的精确定义)只解释另一输入/内部形式。三者不相互增加未声明关键词，不能把本篇长度转化成AI每次必须读取的上下文。正常任务只取已用定义和相交规则；更细规则在出现消费者时展开。


## 语言无关的作者合同与当前规格

基础文本的词法、文法、类型/效果、调用与契约规则已完整迁到[语言体系与开发规格](../../领域/计算基础/文法类型与求值.md#语言无关的作者合同与当前规格)。该章同时拥有语言身份、基础环境和完整开发范本；本章继续拥有高层语义、用户定义、领域扩展及更广产品语义。不能分别维护两份基础作者标准。


## 抽象工程语言与表达载体选择

### 一、中立语义与紧凑表示分别负责什么

SEC必须定义合法对象、关系、类型、行为、约束、作用域、错误及可编译含义。当前语义主线采用[中立作者规范](../可替换表示与适用范围.md)及共同模型，不把TS语法/类型当工程真相；原生前端仍复用各语言成熟工具。紧凑作者表示不等于手填JSON IR，也不等于换文件后缀即可语言无关。

最小不可绕过约束是：每个可生成输出必须能追溯到本次冻结候选或已采纳语义、明确策略、精确绑定及确定推导规则。候选生成不以正式采纳为前提；实际业务差异不能靠生成时猜测补齐。该约束可以由结构化图、AST、关系模型和版本化协议满足。

### 二、四层分离

| 层 | 内容 | 变化的影响 |
| --- | --- | --- |
| 抽象语义 | 类型、控制/数据/状态语义、引用与约束 | 改义需版本兼容与迁移 |
| 编辑协议 | 查找、声明、连接、替换、删除、校验、采纳 | 接口兼容，不得改变同操作的含义 |
| 作者呈现 | 默认源文本与普通高层调用；规则表/图/结构化JSON可按声明角色作为投影或结构作者源，自然语言形成待检查候选 | 前端表达范围、错误恢复与往返保全；受影响的消费者需重查 |
| 持久化格式 | 分片、版本化序列化、摘要和索引 | 格式迁移，不凭存储顺序改变语义 |

### 三、实际选择

目标主路径采用“AI/脚本的紧凑源文本或局部差异＋可导出、可diff的组合定义与原生作者资产”；首期不开发交互式编辑器。默认持久画像与区域来源规则由[工程实体与持久表示](../工程源/清单模块与绑定锁.md)明确。AI 不需要手工拼全图 ID 和后台回执，工具从已观察上下文解析引用、补机械字段、返回候选差异；作者必须仍能看到未决假设和行为改变。

中立文本是当前主表示；不同投影按任务使用。文本适合算法密集、离线审阅、合并、专家快捷输入的场景；表格适合规则表和约束矩阵；图形适合拓扑、状态机和能力连接；中立基础构造可以直接定义独特算法；原生源码是用户可选的作者方式，不是独特算法的必经去处。没有原生作者区域的工程也合法。相同工程可以按区域使用不同表达，但混合并不自动优于单一作者源。

### 四、替代方案与成本

**只用自然语言**：创作门槛低，但每次重新理解会改变含义；应记录解析结果与采纳决定，不能把对话当唯一构建输入。

**直接编辑底层IR**：接口少，但阅读、引用、结构不变量和升级成本可能高。默认提供高层操作，同时允许有相应能力的开发者选择底层入口；不以尚未做收益实验作为禁止使用的理由。实际写入仍按同一任务、验证与权限处理。

**中立文本前端**：直接表达可移植语义，承担有限parser、类型/效果检查、格式与错误恢复成本；当前选用以避免将首个目标的语义提升为核心。原生和结构入口仍可用，不强迫全部存量资产迁写。实际成本不合理时优化表示或增加前端，不能未经需求决策撤销多语言语义主线。

**结构模型作为某区域作者源**：适合显式连接和规则表，可提供图形或机器编辑。必须保留紧凑表达式、结构检索、版本合并及有范围的文本导出。对全部任务强制画图才是被拒绝的方案，并非否决结构作者源；表达能力不足先补适配或保全，不强制转成原生代码。

**JSON/YAML 唯一作者界面**：机器互操作容易，但结构可读不等于语义易写；序列化格式不能代替面向任务的编辑协议。

### 五、行为不是只有 `fn/if/return`

基础专用解释没有`return`保留词；当前通用组合已按[局部控制合同](../../领域/对象与控制/控制流与存储寿命.md)提供词法函数退出。此标题中的return仍表示一般行为退出，不能借它为未启用相应解释的旧源增加语法。高层作者按领域归属：事务声明见[事务应用](../../领域/状态关系与流/事务与交付.md)，有限层级/正交模型见[状态反应](../../领域/状态关系与流/层级状态与事件.md)，有限多重集、连接与聚合见[关系查询](../../领域/状态关系与流/关系查询与维护.md)。它们复用现有类型和表达式，不要求作者写目标代码生成器；仍缺完整作者形式的领域继续登记，不能由其中一种占据整个高层入口。


行为可以由状态机、决策表、表达式、数据流、受控调用、事件处理、事务协议、领域算子、约束求解目标或绑定的原生实现表达。每种构造必须有明确输入输出、顺序/并发、错误和效果语义。领域构造可以向较低层程序 IR 降低，作者无需先展开所有胶水。

存在一般算法时，开发者可以直接使用中立基础语言定义它，不以模板或原生出口为前提；已有语言实现也是可选择的路线。基础层、领域层和实现策略必须相互接合，而不是为每个算法新增业务关键字。高层作者能力若缺失，应明确归入设计工作，不通过要求改写目标语言将缺口永久外包。

### 六、旧 `.sec` 原型的身份

整数、布尔、条件和函数的现行含义由[语言规范](../可替换表示与适用范围.md)拥有。局部语法例仅用于说明，不构成第二份文法；不同前端的支持范围和当前采用由作者基线明确。

### 七、验收与反证

相同语义经不同入口应得到等价模型含义；只有语言合同明确为纯装饰的注释/布局才可排除相应业务摘要。文档输出、编译注解或目标空间布局的真实消费者仍参与依赖。新 API 必须测引用错误、陈旧变更、草稿不完整、非法连接、并发编辑和无改动往返。首期应比较AI任务正确率、语义返工、上下文和多轮成本；未来启用人类客户端再验证其真实体验。不能依据“看起来简单”决定主入口，也不能以尚无界面为由阻断当前机器路径。


## 通用表达与渐进具体化

### 当前目标不是只有模板和原生代码两档

保留一般计算底座，在其上提供可嵌套的行为/领域定义；允许同一候选的不同区域处于不同抽象深度。已知大功能直接实例化；特殊局部可以写紧凑表达式、组合已有规则、选定另一实现，或使用原生程序。原生是正常表示与接入路径，不是“没有模板就退出整个语义体系”的唯一分支。实现可复用成熟工具；当前中立计算层本身是高级语言的有界设计，不是机器级低级语言。它还不等于高层工程作者面。

高层可以比低层更短，一般计算也不因此被删掉；其构造和准确限制见[理论界限](../../依据/理论/表达证明与搜索边界.md#通用性与简化可以共存)。不同区域仍保有自己的类型、效果、目标和保证范围，不把一个原生出口算成所有高级语义已经实现。

### 定义、要求与实现选择不再强绑在同一个大模板里

沿用现有Definition、Requirement和ImplementationBinding，不新建一套元模型。定义说明可观察行为或允许的行为集合；要求说明本实例额外必须满足的条件；实现选择说明采用哪种已合格的布局、算法、库和调度。纯计算不填空的事务和授权字段。

不是所有底层细节都属于用户意图。已经由合同确定的值应推导；被项目政策允许的自由可以由固定策略选择；真正影响业务而尚未决定的部分才保持待决。用户要求指定算法或布局时，它成为实现要求，不能继续被优化器任意改掉。Halide的算法/调度分离只提供这种分离的具体参照，不证明任意业务都能这样分离。

一个定义可以引用其他定义、声明局部函数、绑定原生实现或携带可移植表达式。无需全局固定“一级意图/二级意图/原生”的深度，也不要求每次调用展开所有子图。公共合同与局部变化的边界决定需要读取和保留的层级。

### 看展开、改实例和接管源是不同操作

| 作者动作 | 保存的源及保持要求 | 失败或升级分支 |
|---|---|---|
| 查看展开 | 从精确定义/实例/策略产生只读视图；零修改时不新增作者副本，不改变行为 | 未支持的内部只能按可见合同查看，不凭投影视图冒称可编辑 |
| 修改参数或已公开扩展点 | 实例保留差异，重新检查参数与组合前提；子身份不随数组位置漂移 | 参数不足时允许向下具体化，不强迫不断向公共模板增加开关 |
| 局部具体化 | 对可编辑的选定子定义建立本实例拥有的实现选择或局部定义；记录精确基础及对应稳定来源 | 一对多或多对一映射不唯一时，选择更明确的作者边界或返回需选择；不直接修优化后的机器代码 |
| 采用原生或可移植程序 | 明确该区域的作者源、类型/效果/目标合同和需要的保证 | 原生不自动跨所有目标；可移植语义不足则扩展相应画像，不静默退化 |
| 提取复用定义 | 从多个真实结构或新设计建立参数化候选，重绑调用者并核查行为和副作用 | 不靠相似字符串宣布任意算法等价，不为一次短表达强建公共框架 |
| 脱离上游或升级上游 | 脱离即转移作者责任；继续跟随上游则按旧定义/本地差异/新定义做三方检查 | 不承诺既任意手改又无冲突自动吸收所有未来更新 |

局部具体化不等于把整个展开树复制成长期作者资产。可以保存有明确基础和结构选择器的小型差异；若差异持续依赖大量私有结构、频繁冲突或比局部完整定义更难理解，就转为独立局部定义并显式结束那部分自动跟随。相同实例配置不代表有状态实例可合并。删除、重命名、效果变化与共享约束仍可能扩大真实影响，不用“只改局部”掩盖它。

常见高层构造的展开应保持参数求值次数、名称卫生、错误与效果顺序。可分析的保持规则用对应检查，未证性质保留边界；这个协议不要求存在任意程序的等价判定器。用户主动改变行为时形成新需求/版本，不把它误标为保持原语义的精化。

### 再抽象必须有可核对的含义

从自己维护的精确定义、参数和已知展开轨迹恢复紧凑引用，可以在已声明范围内确定完成。从任意优化后程序反推唯一高层意图，不是本设计的普遍承诺。两者不能共用一个“折叠”按钮语义。

AI可以提出新的复用定义与组合规则，其验证和采纳仍按已有合同处理；通过后成为普通库定义，以后重建不重新向模型猜测。类型检查器、宏展开器和用户验证器的来源与共同错误要分别考虑。并非每个提取结果都需签名、发布到市场或生成新服务。

### 简化的验收不是是否提供了大功能名称

至少应设计检查：同一高层定义可正常使用；新增原来没有参数的局部条件无需核心业务分支；只查看展开不增加作者体量；局部修改后不手修派生源码；上游升级冲突可定位；无法压缩的独特程序可以紧凑保留；另一个作者可构造不同的复用定义。相同共享知识下与成熟原生方案比较，而非与故意展开的低层手写版本比较。

这些是现有六类接缝上的具体协议，不是新增第七个万能引擎。首期仍AI/脚本、单个成熟目标、模块化单体；分阶段把本协议的最小完整路径接入原生产链，不等待全语言或全功能库。


## 小程序与局部构造不是另一种产品

过滤、分组、变换、匹配、错误映射、调用连接和控制流定义，同样可以组合成十几行函数；它们不只服务完整应用。按语言规则选择参数、局部状态与实际依赖，必要时以正常原生表达式补充局部算法，不为每个表达式创建全局工程对象。

完整函数、表达式、语句区域和模块的差异在输入与输出闭包，不在能否进入SEC。“工程编译”意味着能够继续组合和保持跨产物合同，不意味着请求必须包含构建脚本、发布流程和全项目对象。临时单位、自由引用、控制退出及晋升持久模块由[单位输入合同](../工程源/清单模块与绑定锁.md#片段函数与工程使用同一输入合同)拥有。

高层组合的逐项语义必须足够明确。例如先过滤再去重，与先去重再过滤不同；保留最后一个值时，结果可以按首次键位置或最终保留元素位置排列，这两种业务不能被同一个模糊的“保持顺序”覆盖。组合定义写清顺序、键等价、回调求值和错误行为，后端才能决定融合循环。类型为readonly并不证明原生getter、Proxy、迭代器或回调没有效果；缺少相应条件时保留原阶段顺序或明确所需假设，不为短代码制造不成立的优化。

查看高层、展开局部、使用普通代码和提取新定义，仍沿原渐进具体化合同。片段例不是新的内置行业分支，完整输入到目标示意见[短函数完整设计例](../../../examples/连续开发/组合结构与任务轨迹.md#一段短代码从组合到具体源码)。


## 需求关系与程序构造分别复用

声明、引用、实例化、连接、约束、绑定、组合、输出是工程构造职责，不足以单独说明“需求如何抽象”。同一组构造可以承载不同的结果关系、状态演化、结构和进展要求；完整要求模型由[跨软件意图合同](跨软件需求合同.md#cross-software-intent-contract)给出。

共同抽象不要求所有需求都成为纯函数：逆关系与不变量不只是字段，时间协议不只是返回值，资源保持也不只是类型。普通函数、原生供给及领域结构可分别实现这些关系；在相同公开合同下可选择较小实现。不强制把每个需求标成block，也不因为未预装高级接口就将普通作者退到所有低层步骤。

“结果允许哪些”与“程序按照什么次序产生它”不相等。一个本来要求左到右求值和首个错误的实现，不能仅以最终正确值集合替代；反过来，原任务只要求一种满足约束的结果且已委派选择，就不强加某一种算法轨迹。作者的独立要求与实现主体各自有接受关系，细化不得伪造或收窄它们。


## 需求、行为、设计选择与实现的关系

需求说明希望满足的结果，行为模型说明被采纳的控制逻辑，设计选择说明某个非唯一方案为何被采用，规则或实现绑定说明如何物化它。四者可以共享引用，不能互相冒充。

```text
自然语言/图形/结构化输入
→ 需求、逻辑、假设和选择候选
→ 校验、冻结候选或按目的正式采纳
→ 明确版本与解释的工程模型
→ 按所请求输出规则推导及目标生成
```

AI 可以同时提出需求结构与行为设计，但必须保留用户要求与 AI 推断的区别。若用户只说“更安全”，系统应将可推导安全措施、待采纳风险政策和未知效果分开，而不是用一个默认模板宣称完成。

确定性规则能推导类型校验、序列化、常规状态更新和其他已建立合同的实现；未决业务条件、数据处理目的和不可补偿权利不能被编译器自行填补。固定政策可以确定选择合法实现，不要求人逐行决定。

已接受工程模型可以含未实现的合同和草稿；编译某个输出时只要求其必要语义与实现闭合。文本、图、JSON 都不是这个要求的替代品。表格可用于复杂政策与设计审查，但表格齐全不证明含义完整。

验收要求与实现候选分开维护。反例是 AI 为通过生成器测试擅自改业务阈值，或从同一错误行为生成“预期答案”。修复应回到需求来源与独立判断，不把全部生成测试当用户需求的真值。


## 作者语法与前端支持边界

当前基础文法由[中立作者规范](../../领域/计算基础/文法类型与求值.md#闭合结构文法)唯一维护。一个前端的采用需要产生式、消歧、绑定、类型/效果、动态含义、库环境、错误及编辑接合共同成立；不能从一个kind名、范本或关键词数量推导支持。

基础语言支持一般程序，不等于全部工程领域的作者形式都已完成。新前端按[D02](../../决策/设计理由-目标与核心.md#d02-中立语义与可替换作者表示)作明确采用决定，并声明其同义范围、迁移和源码保持。当前不交付嵌套作者SDK。不同表面可以共用语义，但不按示例拼写自动开放未实现入口；作者成本在相同库与保证条件下比较。


## 算法、计算结构与事务作者面的分工

高层工程表达有不同方向：状态和事务只是其中一种；形状、访问映射、边界、递推、归约、数值与调度同样是第一类高层语义。不能用多写几个fn、也不能用多写几个aggregate替代它们。已有数值/效果/资源规则不重抄，完整的作者—目标—变更合同见[计算结构与算法实现设计](../../领域/计算基础/计算结构与算法.md)。

普通新算法仍用现有函数、类型和用户定义组合表达；高层库可以保存独立的计算结构，并提供正确的展开/分析/降低。没有“算子目录预装全世界”前置，也不能把巨大内核硬编码为一个用户不可修改的原生调用。实际不足时区分表达不足、库合同不足、后端不足和目标能力不足；不能全部叫“业务缺信息”。

`decision`属于可纯用的规则表达；`aggregate/operation/subscription`在事务画像承担特定持久语义。一般函数调用、循环、GPU线程和事件循环不因同名词而取得事务请求身份。未选能力与当前不需要部分的处置由[适用性协议](../../演进/适用性与能力协商.md#不需要部分的处置与退出协议)拥有。


## 封装、类库与开放定义的共同位置

开放定义通过[库规范](模块封装与复用库.md)提供私有默认、opaque类型、包门面、实例、公开合同和升级。它不把复杂行为封成无法修改的字符串。类型修饰的语法由语言规范维护，其他表达式和事务画像仍按各自合同解释。

一个高层意图可以由普通函数、契约、关系、结构计算或事务声明形成公开定义；使用者传入差异，编译器记录展开与来源。库内部仍可自由编写中立算法，客户端通过公开操作使用它。源码可得时，有权创作者可以建立局部派生并明确新的维护责任；只查看展开不是改变原库，也不赋予外部代码私有类型权限。

类的封装、实例方法、接口多态和实现继承分别选择。当前用模块/opaque、实例参数或函数记录和明确资源满足相交能力；不因没有class关键字就说没有类库，也不把这些机制冒称已实现全继承和反射。原生类继续按其真实语言语义适配，公共中立规则不依赖Java/TS类对象。

固定表达规则、标准库、领域库与产品任务分开。首个计算库是验证载荷，不能消除工程意图、共享约束、真实作用和演进；同样，事务的完整作者协议不是普通算法的前置。


## 需求复合定义与实现主体分别闭合

`design`仍是同一个Definition的有类型需求角色，`use`复合后对外仍是可引用需求定义。运行函数、状态/关系模型和实现方法并列提供相应实现；复合闭包不等于所有require可计算为普通函数。产品作者没有提供算法体时不能自动判需求不完整，应区分含义、证据、方法和目标缺额。默认实现缺额由受委派供给承担，不将其展开为产品源的手工维护负担。原block及管脚的共享/隔离/状态/效果/寿命条件照旧。
