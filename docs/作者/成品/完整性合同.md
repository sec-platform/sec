# 成品设计完整性合同

本篇定义本目录中一个**已选成品形态何时已经设计完整**。它不是要求产品作者填写的清单，也不是第三份作者源；作者仍只维护实际意图、参数和独特差额。这里约束的是语义解释、供给、目标生成和验收：任何成品若缺少其适用的公开边界、状态、失败、资源或成员，就只能保存为准确的未完成设计，不能因为已有名称、JSON根或接口草图而标成完整。

## 1. 完整设计必须闭合的十二个方面

对一个具体成品，以下方面必须由当前作者源、复用定义、目标合同或明确宿主进口共同确定；不适用可以明确为“不适用”，不能用省略表示“随便”：

1. **意图与主体**：它要完成什么，约束哪个准确主体，哪些选择已固定、继承或仍委派。
2. **公共消费边界**：消费者通过什么入口、消息、文件、值或安装动作取得结果；同步/异步、一次性/持续必须明确。
3. **输入与表示**：类型、单位、编码、顺序、重数、身份、快照及不可信输入的解码边界。
4. **正常行为与进展**：合法输入产生什么结果，何时必须完成，何时允许等待、分批或持续运行。
5. **实例与生命周期**：创建、就绪、活动、关闭、卸载、重启以及运行实例与定义的身份关系。
6. **失败、取消与恢复**：业务拒绝、协议错误、资源故障、取消、未知效果和可重试条件分别怎样表示。
7. **并发、顺序与一致性**：可并行范围、顺序保证、共享主体、竞态、幂等、背压或事务边界。
8. **资源与权限**：所有权、借用、释放、预算、秘密、文件/网络/设备能力和真实授权位置。
9. **宿主进口与隔离**：哪些依赖由成品内部闭合，哪些由消费环境提供；不可信代码需要什么物理隔离。
10. **目标成员与公开符号**：最小完整产物包含哪些代码、类型、资源、清单、配置及公开入口；不能以空目录或声明文件代替实现。
11. **兼容、升级与再次修改**：参数、公开接口、实现、数据和运行实例变化时哪些消费者失效，是否需要迁移或新实例。
12. **验收与证据**：什么可以静态判断，什么必须运行、测量、形式检查或在真实宿主验证；设计稿不能自签运行成绩。

这些方面可以由一个小文件共同承担，也可以跨定义、宿主和目标规范分担。完整性要求的是**信息闭合**，不是固定文件数或固定模板。

## 2. 四种完成状态不能合并

- **作者设计完整**：上述适用方面已经有准确含义，没有需要实现者猜业务决定的空洞。
- **目标合同完整**：所选目标语言/平台的公开接口、进口、成员和生命周期可以据以实现。
- **实现完整**：真实代码、库、资源和适配已经取得，所有必需根可以形成制品。
- **验证/采用成立**：针对准确实现和环境取得相应证据，实际安装/运行/采用另有真实事实。

因此，规格包可以作者设计完整而没有运行实现；CLI只有`.d.ts`不能称实现完整；已经构建的固件没有真实板卡和时限依据不能称目标采用成立。

## 3. 本例36个成品的当前完整入口

下表给出每个已选根的**作者源、消费接口和拥有详细行为的正文**。接口是目标合同，不是产品作者的第二份源码；实际实现可以是TS、原生库、Wasm、设备代码或其他合格供给。

| 成品 | 作者根 | 公共边界 | 详细合同 |
|---|---|---|---|
| Definitions | `model/definitions.json` | `Definitions` | [规格与静态资产](规格与静态资产.md) |
| StaticValue | `model/staticvalue.json` | `StaticValue` | [规格与静态资产](规格与静态资产.md) |
| Resources | `model/resources.json` | `Resources` | [规格与静态资产](规格与静态资产.md) |
| Expression | `model/expression.json` | `Expression` | [片段与已有工程变化](片段与已有工程变化.md) |
| ContextPatch | `model/contextpatch.json` | `ContextPatch` | [片段与已有工程变化](片段与已有工程变化.md) |
| Library | `model/library.json` | `Library` | [库与协议客户端](库与协议客户端.md) |
| SDK | `model/sdk.json` | `SDK` | [库与协议客户端](库与协议客户端.md) |
| ProtocolClient | `model/protocolclient.json` | `ProtocolClient` | [库与协议客户端](库与协议客户端.md) |
| Command | `model/command.json` | `Command` | [命令与批处理程序](命令与批处理程序.md) |
| BatchCommand | `model/batchcommand.json` | `BatchCommand` | [命令与批处理程序](命令与批处理程序.md) |
| HttpHandler | `model/httphandler.json` | `HttpHandler` | [请求处理器与服务](请求处理器与服务.md) |
| Service | `model/service.json` | `Service` | [请求处理器与服务](请求处理器与服务.md) |
| Worker | `model/worker.json` | `Worker` | [工作器组件与插件](工作器组件与插件.md) |
| Component | `model/component.json` | `Component` | [工作器组件与插件](工作器组件与插件.md) |
| Plugin | `model/plugin.json` | `Plugin` | [工作器组件与插件](工作器组件与插件.md) |
| WebPanel | `model/webpanel.json` | `WebPanel` | [交互应用](交互应用.md) |
| Desktop | `model/desktop.json` | `Desktop` | [交互应用](交互应用.md) |
| Mobile | `model/mobile.json` | `Mobile` | [交互应用](交互应用.md) |
| Family | `model/family.json` | `Family` | [产品族与共同发行](产品族与共同发行.md) |
| Query | `model/query.json` | `Query` | [查询批数据与持续流](查询批数据与持续流.md) |
| BatchData | `model/batchdata.json` | `BatchData` | [查询批数据与持续流](查询批数据与持续流.md) |
| WindowStream | `model/windowstream.json` | `WindowStream` | [查询批数据与持续流](查询批数据与持续流.md) |
| Migration | `model/migration.json` | `Migration` | [数据与配置迁移](数据与配置迁移.md) |
| Compiler | `model/compiler.json` | `Compiler` | [编译分析与生成工具](编译分析与生成工具.md) |
| Analyzer | `model/analyzer.json` | `Analyzer` | [编译分析与生成工具](编译分析与生成工具.md) |
| Generator | `model/generator.json` | `Generator` | [编译分析与生成工具](编译分析与生成工具.md) |
| Inference | `model/inference.json` | `Inference` | [模型概率与优化](模型概率与优化.md) |
| Trainer | `model/trainer.json` | `Trainer` | [模型概率与优化](模型概率与优化.md) |
| Sampler | `model/sampler.json` | `Sampler` | [模型概率与优化](模型概率与优化.md) |
| Optimizer | `model/optimizer.json` | `Optimizer` | [模型概率与优化](模型概率与优化.md) |
| GpuKernel | `model/gpukernel.json` | `GpuKernel` | [计算核固件与实时程序](计算核固件与实时程序.md) |
| Firmware | `model/firmware.json` | `Firmware` | [计算核固件与实时程序](计算核固件与实时程序.md) |
| RealTime | `model/realtime.json` | `RealTime` | [计算核固件与实时程序](计算核固件与实时程序.md) |
| Installer | `model/installer.json` | `Installer` | [安装与部署](安装与部署.md) |
| Deployment | `model/deployment.json` | `Deployment` | [安装与部署](安装与部署.md) |
| NativeChange | `model/nativechange.json` | `NativeChange` | [片段与已有工程变化](片段与已有工程变化.md) |

上表中的作者根均位于`examples/开发成品/`，公共边界统一收录于[目标接口](../../../examples/开发成品/公开边界.d.ts)。此表只负责完整性导航，不复制各页的算法、错误和资源合同。

## 4. 目标接口的失败表示统一原则

目标接口应使调用者能够区分“合法空结果”和“没有取得结果”。可预期的业务拒绝、协议拒绝、取消、资源或宿主故障使用明确的结果联合；不能仅以空数组、空diagnostics或Promise完成代替。真正违反宿主编程合同的不可恢复异常仍可由目标语言的异常机制处理，但不能成为普通运行失败的唯一通路。

异步接口的Promise只表示**结果将在以后取得**，不意味着其解析时所有外部效果、保存、排空或设备工作都已完成；具体终点由各形态合同定义。操作型接口使用稳定操作身份和可查回状态，不用重复调用产生副作用来“查询”结果。

## 5. 成品结构的机械门槛

当前文档核验器必须拒绝以下结构缺口：

- `definitions.json`导出了一个成品定义，却没有对应`model/*.json`作者根；
- 作者根没有通过`use`引用同名完整定义，或者公开主体无法解析；
- `contracts/spec.json`缺少该成品规则，或规则正文来源/标题已经过期；
- `公开边界.d.ts`缺少同名公共命名空间；
- 工程清单没有登记作者模块；
- 运行型成品只有类型名称而没有任何消费入口；
- 规则目录中的字段与复合定义实际实例化字段不相容。

这些检查只能防止“有名字、没合同”的结构缺口，不能证明行为实现正确。真正的类型、效果、目标、运行和证据仍由对应SEC实现完成。
