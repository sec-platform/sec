# 用户能力模块化开发：信息抽取台账

> 性质：非权威抽取台账。`docs/archive/用户能力模块化开发.md` 是原始对话录；本文件用于确保其中信息被全量覆盖、去重、归类和映射。权威结论仍以 `docs/00-11` 编号文档为准。

## 抽取原则

- 先全量覆盖，再判断是否进入权威文档。
- 不只抽取高信号结论；事实、判断、类比、开放问题、延期项、反复确认的偏好都要保留。
- 重复内容归并到同一主题，不在编号文档中制造重复规范。
- 不稳定、发散或愿景性内容可保留在本台账，不强行写入 `00-11`。
- 每条信息最终标记为：`已整合` / `已有覆盖` / `待整合` / `仅素材保留` / `冲突待决`。

## 覆盖范围

- 源文件：`docs/archive/用户能力模块化开发.md`
- 总行数：26893（按当前归档 line index）
- 对话轮次：82
- 当前状态：已建立覆盖索引，已完成 001-071 分段抽取与初步去重映射；新增 071 续写与 072-082 已补充分段抽取，稳定工程缺口需按现有架构边界复核后再进入编号文档。

## 对话区间索引

| 轮次 | 行号 | 主题压缩 | 抽取状态 |
| --- | --- | --- | --- |
| 001 | 3-303 | 功能块/文件组织作为 AI 单元；面向用户测；开发者关注用户功能而非实现测；与 n8n 的区别 | 已分段抽取 |
| 002 | 304-665 | 是否像 vibecoding 开发者语言；IDE+AI agentic；有没有人在做；商业化、开源、社区模板 | 已分段抽取 |
| 003 | 666-1165 | 官方/社区模板化技术方案；高性能/高并发/高可用/低延迟方案；是否降低 AI 智能和 token；组合模式 | 已分段抽取 |
| 004 | 1166-1427 | 是否改变世界；是否削弱程序员/架构师；减少重复造轮子和商业代码重复逻辑 | 已分段抽取 |
| 005 | 1428-1658 | 是否前人已有类似想法；失败原因；官方/论坛调研；最可行 MVP | 已分段抽取 |
| 006 | 1659-1723 | 是否主要复用微软模块化代码；IR 后是否仍是复制代码；与 skill 的区别 | 已分段抽取 |
| 007 | 1724-2165 | 哪些代码 AI 不必细写；哪些仍需 AI 写；模块使用方式；与 SDK 的区别 | 已分段抽取 |
| 008 | 2166-2988 | 用覆盖最广的例子设计基建 | 已分段抽取 |
| 009 | 2989-3260 | 飞轮；文件复制；MVP 各部件代码量；vibecoding 时代新项目是否还用 Java | 已分段抽取 |
| 010 | 3261-3430 | 开源模板复用；官方解决方案参考；能否复用成功能块；TS 与 Java 新项目选择 | 已分段抽取 |
| 011 | 3431-3983 | 生成源码是否 100% 可控；是否只是快速搭建工具；AI 介入时机；任务控制；编译器类比 | 已分段抽取 |
| 012 | 3984-4265 | 没有 AI 时是否也该有人想到；前史 7 条线为什么没人做到 | 已分段抽取 |
| 013 | 4266-4653 | 是否开源；缺少代码审查/系统方案经验；是否只改后端；前端能力不足 | 已分段抽取 |
| 014 | 4654-4772 | 快速增长项目是否人工审查；vibe review；资深 Java 项目能否用 TS 路线做到 | 已分段抽取 |
| 015 | 4773-5403 | 从 0 动工前重新审查最小 MVP 例子 | 已分段抽取 |
| 016 | 5404-5448 | 是否不用 Elysia | 已分段抽取 |
| 017 | 5449-5564 | 全栈开发式微下，对大模型开发和 agentic 开发的帮助 | 已分段抽取 |
| 018 | 5565-5645 | agent 开发与基础大模型开发分工；项目是否属于 agent 开发；大型项目需求 | 已分段抽取 |
| 019 | 5646-5977 | 改变开发模式；模型训练关系远；agent runtime/openclaw 成熟与护城河；项目影响力 | 已分段抽取 |
| 020 | 5978-6246 | AI 乱干比例、不确定性；便宜/速度/智能要求降低；上层逻辑管脚对齐；插件实时逻辑图 | 已分段抽取 |
| 021 | 6247-6289 | 游戏行业适用性 | 已分段抽取 |
| 022 | 6290-6353 | 游戏行业模板化现状 | 已分段抽取 |
| 023 | 6354-6657 | 项目形态分类；高 token 消耗案例；工程编译器适用项目形态 | 已分段抽取 |
| 024 | 6658-7141 | 高性能/底层/基础设施项目如何使用；性能影响；outer shell/hybrid kernel/spec-to-harness | 已分段抽取 |
| 025 | 7142-7360 | AI 受控生成部分是源码硬链接还是 UX 映射；源码与 slot 映射关系 | 已分段抽取 |
| 026 | 7361-7519 | 项目解决了什么不足；是否必要 | 已分段抽取 |
| 027 | 7520-7588 | 全球是否有高度相似产品 | 已分段抽取 |
| 028 | 7589-7866 | 硬件芯片封装类比；软件为什么不能封装芯片 | 已分段抽取 |
| 029 | 7867-8110 | 顶级硬件世界；vibecoding 后项目开发维护成本；行为细节占比；项目预期 | 已分段抽取 |
| 030 | 8111-8184 | 大模型 benchmark 关注点；平台不能解决的问题边界 | 已分段抽取 |
| 031 | 8185-8489 | 管脚硬约束下 issue 层级；是否仍要看源码；源码层问题定位 | 已分段抽取 |
| 032 | 8490-8713 | AI 通过配置/连接/管脚推演逻辑并定位源码；降低满仓搜索复杂度 | 已分段抽取 |
| 033 | 8714-9005 | 项目编译器与传统编译器类比；业务管脚配置作为业务语法 | 已分段抽取 |
| 034 | 9006-9359 | 数据流图、业务流程图、数据库表结构等工程图谱；原生内置视图 | 已分段抽取 |
| 035 | 9360-9544 | 基建成熟后 AI 维护、修改、增添是否大幅简化 | 已分段抽取 |
| 036 | 9545-9692 | 降低 AI 算力/智能要求；终端小模型；终端快速产出系统性项目 | 已分段抽取 |
| 037 | 9693-9827 | 平台成功后 AI 仍需扩大规模的领域；未来快速产生代码项目的新需求 | 已分段抽取 |
| 038 | 9828-10027 | 平台解决不了的问题；更天才思路；创新级别对比 | 已分段抽取 |
| 039 | 10028-10165 | 可验证语言、组织编译器、Cedar/Palantir/Camunda/Temporal/OpenClaw/n8n；自维护基础 | 已分段抽取 |
| 040 | 10166-10543 | 是否替代 n8n；集成 OpenClaw；最终代码量；论文可能；最终产品形态 | 已分段抽取 |
| 041 | 10544-10896 | 工程语言与自举可能性 | 已分段抽取 |
| 042 | 10897-11297 | 自举作为目标是否应提前纳入指导；需要主动给全面信息 | 已分段抽取 |
| 043 | 11298-11418 | 需要参考大厂工程解决方案的母例 | 已分段抽取 |
| 044 | 11419-11659 | 学习成本、心智负担、程序员接受度 | 已分段抽取 |
| 045 | 11660-11838 | 项目最终代码量可能不大 | 已分段抽取 |
| 046 | 11839-12062 | Claude Code 等插件的 AI 调用机制；平台驱动 AI；权限/安全防止改确定性内容 | 已分段抽取 |
| 047 | 12063-12365 | IDE 插件内开发；AI 对话框是否弱化；视图优先、源码可达 | 已分段抽取 |
| 048 | 12366-12509 | Opus 4.7 做大型项目耗时；平台前后效率对比 | 已分段抽取 |
| 049 | 12510-12646 | 发散/工程能力强模型受控效果；与 Git/Linux 影响力对比 | 已分段抽取 |
| 050 | 12647-12693 | 更多具有强大影响力的系统/技术 | 已分段抽取 |
| 051 | 12694-13143 | 尽可能列举高影响力事物 | 已分段抽取 |
| 052 | 13144-13173 | Redis 为什么未列入；Redis 定位 | 已分段抽取 |
| 053 | 13174-13556 | MQ、Elasticsearch；GPT-5.5 更强但更会偷懒；是否适合平台开发模式 | 已分段抽取 |
| 054 | 13557-13714 | 从 vibecoding 到受控 AI 编程的命名；叫什么 coding | 已分段抽取 |
| 055 | 13715-13804 | SpecEngineer 命名 | 已分段抽取 |
| 056 | 13805-14189 | 平台成功后世界变化和新需求 | 已分段抽取 |
| 057 | 14190-14579 | 对世界颠覆不够极致；更暴力愿景 | 已分段抽取 |
| 058 | 14580-15346 | 类似方法可应用到哪些具体事情 | 已分段抽取 |
| 059 | 15347-15562 | Harness Engineering 命名 | 已分段抽取 |
| 060 | 15563-15932 | Claude 也使用 Harness Engineering；Anthropic/Claude Code 相关情况 | 已分段抽取 |
| 061 | 15933-16192 | DeepSeek v4/未来 AI 更强是否削弱项目作用 | 已分段抽取 |
| 062 | 16193-16632 | harness 概念爆火；吸收 Claude Code 优点；开源生态；Anthropic 是否往工程发展 | 已分段抽取 |
| 063 | 16633-17009 | 降低 token 与模型商业激励；MCP 是否退化为 AI 控工具；平台控制 AI 实现细节 | 已分段抽取 |
| 064 | 17010-17363 | AI 控制平台管脚逻辑；平台再调用模型个性化实现；主控 AI 对代码要求降低；benchmark 失效 | 已分段抽取 |
| 065 | 17364-17833 | 代码细节浩如烟海；模型如何完备；是否需要开放区 | 已分段抽取 |
| 066 | 17834-18207 | 开放区不能太开；结构治理下的开放实现 | 已分段抽取 |
| 067 | 18208-18523 | Harness+model 是否就是 agent；agent 前景争议；更好的 model 使用方式 | 已分段抽取 |
| 068 | 18524-19068 | 技术社区是否拒绝封装轮子；生态意愿；Harness 是否最终形态；更大想象力 | 已分段抽取 |
| 069 | 19069-19522 | 六层似乎都容易做到；实现难点和真正护城河 | 已分段抽取 |
| 070 | 19523-19814 | 机器码膨胀；编译器后是否仍有人写机器码；项目编译器与传统编译器难度 | 已分段抽取 |
| 071 | 19815-21340 | 项目上下文远超 AI；AI Context Packet；工程语义 review；四根 workspace / control plane / opaque code 架构审查 | 已分段抽取 |
| 072 | 21341-21630 | 严密结构不证明所有逻辑正确；证明分层可治理；参考 MDA、BPMN、MPS、TLA+、Cedar、Temporal | 已分段抽取 |
| 073 | 21631-22044 | NIST / 停机问题 / Rice 定理边界；Git 类比；业务逻辑容器；受限递归和逻辑套娃 | 已分段抽取 |
| 074 | 22045-22650 | block 必要性；block 版本升级；Graph/Repo Harness 迁移入口；Agent Project Compiler；双 demo 路线 | 已分段抽取 |
| 075 | 22651-22903 | Graph-only 工具红海；Repo Harness 命名；Graph → Harness → Slot → Block → Compile 迁移链 | 已分段抽取 |
| 076 | 22904-24223 | 项目信息宇宙；Project Knowledge Graph；16 类信息对象；MVP/成熟平台视图数量与分层 | 已分段抽取 |
| 077 | 24224-24630 | 顶级工程全貌不可完全形式化；SysML/ArchiMate/C4/BPMN/IaC/Git 的局部启发；可治理全貌 | 已分段抽取 |
| 078 | 24631-25006 | AI 后竞争格局；Spec Kit、Claude/Codex、Cursor、Lovable、Backstage 等相邻方向；10 项接近度清单 | 已分段抽取 |
| 079 | 25007-25727 | 深挖项目含义事实源；权威可编译工程图谱；block 资产、升级、生存线、Agent Project 与熵控制 | 已分段抽取 |
| 080 | 25728-26015 | GitNexus / Graphify 与 Graph-only 边界；外部图谱工具只能作为传感器；Graph 是入口，Block 是资产，Compiler 是护城河 | 已分段抽取 |
| 081 | 26016-26472 | 当前 jscpd、dependency-cruiser、Graph-It-Live、自研脚本组成 Repo Harness v0；control ingest 与 evidence pipeline | 已分段抽取 |
| 082 | 26473-26893 | GitNexus / Graphify 集成策略；provider adapter、弱依赖、只读化、归一化、禁止 MCP 主控 | 已分段抽取 |

## 分段抽取结果

> 说明：本节先保存分段抽取结果，避免信息只停留在会话上下文。这里不直接宣称权威结论；后续还需要逐条去重、归类、判定状态，并只把稳定结论写入 `00-11` 编号文档。

### 001-008（行 3-2988）

抽取状态：已分段抽取，待逐条去重与映射。

- 功能块 / 文件组织应作为 AI 时代的工程单元，而不是普通代码目录切分。
  - 功能块面向用户能力组织，例如登录、租户、客户、工单、附件，而不是按 controller/service/model 等实现层拆分。
  - 功能块需要携带 manifest、接口、依赖、验收、来源和可组合信息，才能成为 AI 可理解、可复用、可验证的单元。
  - 开发者关注点应从“实现测”转向“用户能力是否成立”，但实现仍要可追踪、可覆盖、可回退。
- 与 n8n 的边界被反复区分。
  - n8n 更像 workflow/API glue/automation runtime。
  - 本项目目标是生成和维护一个真实代码工程，并对 block、slot、graph、acceptance、provenance、upgrade 形成工程级治理。
  - n8n/OpenClaw 可被当作后续 runtime adapter 或 strategy backend，而不是第一版产品核心。
- 与 vibecoding / AI agentic IDE 的关系。
  - 用户直觉是：这可能像 vibecoding 时代的新开发者语言，但不应退化为“更强聊天式代码生成”。
  - IDE + AI agentic 工具能提高编辑效率，但缺少项目级 contract、来源、验收和升级治理时，会继续依赖整仓搜索和自由改代码。
  - 本项目更接近“工程编译器 / 受控 AI 编程 / 规格驱动项目生成”。
- 官方/社区模板、开源与商业化。
  - 官方 block 提供稳定、可验证、可维护的高质量基础能力。
  - 社区 block / template 可形成生态，但必须有 trust、version、provenance、policy、upgrade 约束。
  - 商业化不应只卖模板，而应卖可验证的能力供应链、行业包、治理控制台、企业私有 registry 和升级保障。
- 高性能、高并发、高可用、低延迟能力的处理方式。
  - 不应让 AI 临场手写所有专家级基础设施代码。
  - 更适合沉淀为 Strategy Pack / Infra Pack / Kernel Boundary，由平台通过 pins、constraints、benchmarks 和 adapter slot 组合。
  - 这类能力可减少 AI token 和不确定性，但不会消灭专业工程判断。
- 对 AI 智能和 token 的影响。
  - 平台不是让模型“变笨”，而是减少模型需要猜测和全仓搜索的范围。
  - 通过 block graph、manifest、pin、slot、acceptance、provenance，把上下文从“全代码仓库”压缩为“任务相关因果上下文”。
  - 模型仍需处理开放 slot、局部业务逻辑、接口适配、修复解释和设计建议。
- 对程序员和架构师的影响。
  - 项目可能减少重复造轮子和重复商业代码逻辑。
  - 程序员不会消失，但工作重点会从重复 CRUD、样板集成、胶水逻辑，转向规格、验证、治理、行业能力沉淀和异常处理。
  - 架构师价值会从口头设计转向可执行 block、contract、policy 和 graph。
- 前人类似思路与失败原因。
  - 低代码、脚手架、模板市场、框架、SDK、组件库都覆盖过局部，但通常缺少完整工程生命周期和 AI 时代的上下文控制。
  - 失败或未成型的原因包括：源码所有权不足、验收不可执行、模板不可升级、生态质量不可控、缺乏 provenance、AI 未成熟、开发者不接受黑盒。
- 最可行 MVP 方向。
  - Git-native Capability Registry + Composer + Acceptance Runner 是更小切口。
  - 第一版不追求完整工作台、实时图编辑器、全行业 registry 或复杂 runtime。
  - 用覆盖最广的多租户 B2B SaaS 母例来验证：登录、租户、客户、附件、CSV、主验收链路。
- IR、复制代码、skill、SDK 的区别。
  - 即使存在 IR，落地仍可能复制或生成源码；关键差异不是是否生成源码，而是生成前后的 contract、provenance、verification 和 upgrade 语义。
  - Skill 更像给 AI 的操作知识或流程提示；Block 是可安装、可组合、可验证、可治理的工程资产。
  - SDK 提供调用能力，不能自动解决项目组合、验收覆盖、来源追踪和演进治理。
- AI 不必细写与仍需细写的代码。
  - 不必每次重写通用登录、租户隔离、基础 CRUD、标准权限、标准导出、基础附件等成熟能力。
  - 仍需实现业务特定 normalization、adapter、policy 细化、字段映射、边界胶水、局部 UI/交互和异常修复。

### 009-018（行 2989-5645）

抽取状态：已分段抽取，待逐条去重与映射。

- 飞轮与文件复制。
  - Registry 中的 block 越多，AI 和开发者越倾向复用已有可验证能力，进一步推动 block 沉淀。
  - 第一阶段可以通过文件复制 / scaffold 落地，但必须同时生成 lock、manifest、provenance 和 acceptance，否则只是普通模板复制。
- MVP 代码量与边界。
  - MVP 应压缩为一个母例、三类核心 block、一个 slot、一条 acceptance 主链路。
  - 编译器、registry、生成项目、验收、报告和 CLI 的代码量可控；不应第一版就做完整平台和生态。
- TS / Java 选择。
  - 新项目在 vibecoding / agentic 时代更适合选择 TypeScript/Next.js 这类全栈样例，便于端到端演示和快速 dogfood。
  - Java 仍适合成熟企业生态、高并发后端和强治理团队，但第一版母例不必选 Java。
  - 资深 Java 工程师也可以用 TS 路线做出第一版，只要把 contract、验证和边界设计好。
- 开源模板与官方方案复用。
  - 可以参考官方 starter、社区模板、框架最佳实践，但不能把它们直接等同为 capability block。
  - 只有补上 manifest、pins、acceptance、policy、provenance、version 和 upgrade 语义后，模板才接近可治理 block。
- 源码 100% 可控与快速搭建工具边界。
  - 生成源码必须留在用户仓库中，允许开发者阅读、调试、修改和接管。
  - 但源码可见不代表源码是唯一权威；权威还包括 spec、graph、lock、manifest、acceptance、policy 和 provenance。
  - 项目不能只定位为快速搭建工具，否则会失去长期维护和治理价值。
- AI 介入时机与任务控制。
  - AI 应在 compile/adapt/repair/review/upgrade planning 等受控 pass 中介入。
  - 每次 AI 任务都应有 task envelope、allowed paths、required symbols、forbidden operations、tests 和 budget。
  - 不能让 AI 作为系统主循环自由决定新增 block、改验收或改 generated artifacts。
- 编译器类比。
  - app spec / block graph / pins 像业务源语言。
  - resolved graph / install plan / lock 像项目 IR。
  - generated project 像编译产物，但仍是人类可读可调试源码。
- 为什么过去没人做到。
  - 单靠模板、低代码或框架缺少 AI 补 slot 的弹性。
  - 单靠 AI 缺少 deterministic contract 和验收闭环。
  - 单靠平台缺少源码所有权和开发者信任。
  - 三者在 AI 时代才更容易组合成可运行闭环。
- 开源、代码审查和系统方案经验。
  - 开源有助于建立 block 生态和可信度，但需要先有足够清晰的核心 contract。
  - 快速增长项目不可能所有细节都人工审查，因此需要 machine-checkable contract、policy gate、verification report 和 provenance。
  - `vibe review` 不能只看代码 diff，应逐渐转向语义 diff、acceptance coverage 和 provenance diff。
- 前后端范围。
  - 早期不能只做后端，否则用户能力链路不完整。
  - 但前端能力也不必一开始做成通用 UI builder，可先做 Next.js 页面、表单、登录、列表、工作区等母例。
- 不使用 Elysia。
  - 第一版不引入 Elysia，避免增加 runtime 变量。
  - Next.js/TypeScript/Prisma/SQLite 更适合作为 v0.1 可演示全栈闭环。
- 全栈开发式微与 Agent 分工。
  - 全栈样板能力会被 block 和 compiler 吸收，人的价值转向需求表达、能力选择、验收和异常治理。
  - 基础大模型开发与 agent/runtime 开发是不同层；本项目更接近 agentic 工程控制层 / 项目编译层，而不是模型训练层。

### 019-028（行 5646-7866）

抽取状态：已分段抽取，待逐条去重与映射。

- 主战场定位。
  - 项目主战场不是模型层，也不是通用 agent runtime，而是 AI 时代的软件工程编译层。
  - OpenClaw、Claude Code、MCP 等可以作为执行或集成基座，但语义控制应由工程编译器定义。
- 改变开发模式。
  - 从“人或 AI 在全仓里写代码”转为“人定义能力与约束，编译器组合确定性资产，AI 只补受控开放点”。
  - 上层逻辑、管脚和验收对齐后，AI 的随机性被压缩在 slot 内。
- AI 乱干、不确定性与成本。
  - AI 乱改文件、改验收、绕过约束、全仓搜索，是现有 agentic 编程的关键痛点。
  - 平台通过 allowed paths、diff budget、zone、provenance、verification、forbidden operations 来降低不确定性。
  - 便宜模型或小模型在强约束 slot 中可能足够；强模型则用于复杂 alignment、repair、review 和 planning。
- 插件实时逻辑图。
  - IDE 插件可展示 block graph、pin/interface、slot、provenance、acceptance coverage 等视图。
  - 视图不是权威源；源仍是 spec、manifest、lock、policy、acceptance、provenance 和 verification report。
- 游戏行业适用性。
  - 游戏行业已有大量引擎、资产管线、组件和编辑器，模板化程度高。
  - 本项目更适合游戏外壳、账号、支付、运营后台、任务系统、活动配置、数据分析等工程层。
  - 高性能渲染、核心玩法手感、引擎底层不应直接交给普通 slot。
- 项目形态分类。
  - 高适配：B2B SaaS、管理后台、CRM、工单、工作流、行业 CRUD、内部系统、数据产品外壳。
  - 中适配：电商、内容系统、集成平台、低代码应用、行业门户。
  - 低适配或需 hybrid kernel：数据库、编译器、游戏引擎、操作系统、实时交易、底层网络、高性能基础设施。
- 高 token 消耗案例。
  - AI 在全仓找上下文、反复理解项目结构、重复生成样板、定位失败来源时消耗高。
  - 通过 graph/manifest/pin/slot/acceptance/provenance 预组织上下文，可把任务输入压缩到相关因果路径。
- 高性能 / 底层 / 基础设施项目的使用方式。
  - 采用 outer shell：业务外壳、配置、控制台、管理面由 compiler 处理，核心热路径保持人工或专家维护。
  - 采用 hybrid kernel：核心 kernel 有严格 benchmark、correctness harness、adapter contract，AI 默认不能写。
  - 采用 spec-to-harness：平台生成测试、约束、胶水和接口，不直接生成不可验证核心算法。
- 源码硬链接还是 UX 映射。
  - 平台既要生成源码落地，也要保留 slot 到源码的映射。
  - UX 中的 block/pin/slot 与源码文件之间必须通过 provenance 和 source map 关联。
  - 推荐一槽一文件，降低回溯、权限和修复复杂度。
- 项目解决的不足。
  - 解决普通模板不可维护、低代码黑盒、AI 自由改仓库、SDK 只提供调用、n8n 不产出完整工程、agent 缺少工程语义的问题。
- 全球相似产品判断。
  - 可能有局部相似产品，但高度同时满足源码可控、block supply chain、AI slot、verification/provenance、project compiler 的形态较少。
- 软件芯片封装类比。
  - 硬件芯片把复杂专家能力封成标准接口和验证过的模块。
  - 软件也可把成熟能力封成 capability block，但软件变化更快、上下文更多、组合语义更复杂，因此需要 acceptance、policy、provenance 和 upgrade。

### 029-038（行 7867-10027）

抽取状态：已分段抽取，待逐条去重与映射。

- 顶级硬件世界类比。
  - 硬件产业依赖标准接口、模块封装、供应链、测试和分层协作。
  - 软件长期难以形成同等封装，是因为行为细节、环境差异、需求变动和集成语义更复杂。
  - AI 时代让“补胶水 / 适配 / 局部生成”更便宜，使软件芯片化更可行。
- vibecoding 后的开发维护成本。
  - vibecoding 能快速产出，但长期成本集中在行为细节、验证、回归、维护、上下文恢复和边界不清。
  - 行为细节占比极高，不能只靠自然语言 prompt 管理。
- 平台预期。
  - 平台应降低项目创建、修改、维护和升级成本，而不是只提高首次生成速度。
  - 成功标准包括：可重复生成、可解释、可验证、可回放、可升级、可接管。
- Benchmark 关注点。
  - 平台不一定提升裸模型数学、代码竞赛或开放推理 benchmark。
  - 更应建立 Spec-to-System、Harnessed Engineering、项目修复率、升级安全、上下文效率、验收通过率等工程 benchmark。
- 平台不能解决的问题边界。
  - 不能保证所有复杂业务需求自动正确。
  - 不能替代底层专家系统设计。
  - 不能消除需求歧义、组织决策、产品判断和不可形式化约束。
- Issue 层级。
  - Spec Issue：规格本身错误、不完整或冲突。
  - Composition Issue：block 选择、连接、pin 兼容、策略组合错误。
  - Slot Issue：AI 填写的局部逻辑错误。
  - Kernel Issue：专家维护核心或外部底层能力问题。
  - issue 分层让 AI 和人先定位问题类型，再决定能否修、谁能修、是否需要改规格。
- 是否仍要看源码。
  - 源码不会消失，仍是调试、审计、逃生和接管层。
  - 但默认问题定位应从 spec/graph/pin/slot/acceptance/provenance 开始，再下钻到最小源码窗口。
- AI 通过配置/连接/管脚推演逻辑。
  - 如果 block 和 pin 结构足够完整，AI 可先沿 graph 推断业务路径。
  - 源码定位从“满仓搜索”变成“由失败 acceptance、slot、file provenance 和 graph edge 推导”。
- 项目编译器与传统编译器类比。
  - 传统编译器把语言转为机器可执行物。
  - 项目编译器把业务规格、能力选择、策略、验收和治理约束转为可运行项目。
  - 业务管脚配置可以视为业务语法，resolved graph / install plan 是项目 IR。
- 工程图谱。
  - 数据流图、业务流程图、数据库表结构、状态机、事件拓扑、policy graph、slot graph、provenance overlay 等都应从同一套结构化对象投影。
  - 图谱是解释层，不应成为另一个手动维护源。
- 基建成熟后的 AI 维护。
  - 当 block、pin、slot、graph、acceptance、provenance 成熟后，AI 修改/维护/新增能力会显著简化。
  - 简化来自更小上下文、更明确边界、更高复用和更强验证，而不是模型突然完美。
- 降低 AI 算力/智能要求。
  - 在强约束任务中，终端小模型可承担局部 slot、字段映射、简单 repair、解释和导航。
  - 复杂需求拆解、架构冲突、开放设计仍需要更强模型或人类。
- 未来新需求。
  - 更快产生代码会带来更多项目变体、行业微产品、内部系统、临时业务工具、个性化工作流和自动化治理需求。
- 更天才思路与创新级别。
  - 项目不是单点算法突破，而是把多条已有技术线重新组织成 AI 时代工程系统。
  - 创新更像 Git/Docker/Terraform/Kubernetes 级的工程组织范式，而不是 Transformer/GPU 级基础科学突破。

### 039-048（行 10028-12509）

抽取状态：已分段抽取，待逐条去重与映射。

- 与 Cedar、TLA+、Alloy、Palantir、Camunda、Temporal、OpenClaw、n8n 的关系。
  - Cedar 更偏权限策略语言。
  - TLA+/Alloy 更偏形式化规格和验证。
  - Palantir 更偏企业数据/本体/运营平台。
  - Camunda/Temporal 更偏 workflow/runtime orchestration。
  - OpenClaw/n8n 更偏 agent/workflow/automation 执行。
  - 本项目定位为工程 compile layer，可吸收其中部分思想或适配其 runtime，但不直接等同。
- 是否替代 n8n。
  - 短期不替代 n8n。
  - 后期可以“上吃”一部分 n8n/OpenClaw 场景：当 workflow 被纳入项目能力、验收、provenance 和代码工程时，平台可生成或治理它。
- 自维护基础。
  - 自维护需要 observability、diagnosis、task envelope、permissions、rollback、policy、verification、provenance、audit log。
  - 不能让 AI 自己决定随意改系统核心。
- 最终代码量和产品形态。
  - 核心可能不是巨量代码，而是精确 contract、registry 协议、compiler passes、verification pipeline 和 adapters。
  - 产品形态可能包括 core compiler、registry/control plane、CLI、IDE plugin、workbench、enterprise registry、governance console。
- 论文可能性。
  - 可围绕 project compiler、AI context compiler、capability supply chain、verification/provenance、Spec-to-System benchmark 写论文或技术白皮书。
- 工程语言与自举。
  - 长期可把自身开发也纳入 SpecEngineer，即用自身规格、block、verification、upgrade workflow 维护自身。
  - 自举应作为方向提前影响设计，但不应成为 v0.1 目标。
- 需要参考的大厂工程母例。
  - 多租户 B2B ticket/work tracking SaaS 是高覆盖母例。
  - 需覆盖用户登录、租户隔离、客户/工单/附件/导出、审计、权限、工作区、基础 UI、数据库和验收。
- 学习成本与接受度。
  - 程序员若感觉只是被迫学习另一个低代码平台，会抗拒。
  - 接受条件包括：源码可见、可接管、验证可信、能减少重复劳动、不锁死技术栈、不妨碍调试、不绕过已有工程习惯。
- AI 调用机制与平台驱动 AI。
  - Claude Code 等插件通过工具、权限、hooks、上下文和 agent 执行任务。
  - 平台应定义 AI Runtime API：task envelope、context pack、permissions、writeback、verification、provenance。
  - CLI first，MCP second，IDE plugin third；后续 workbench 也只是 adapter。
- IDE 插件内开发。
  - 插件应是 view-first 控制台和导航层，而不是封闭牢笼。
  - 源码可达，但写入必须回到平台权限、task envelope 和验证语义。
- Opus / 大模型效率对比。
  - 直接用强模型做大型项目仍受上下文、边界、验证、维护和回归限制。
  - 平台成熟后，强模型可更像 compiler pass worker，在更小任务和更清楚约束下获得更高产出。

### 049-058（行 12510-15346）

抽取状态：已分段抽取，待逐条去重与映射。

- 强模型、发散模型和受控效果。
  - 更强模型更能理解复杂任务，但也可能更会“偷懒”、自作主张、绕开约束或过度改动。
  - 强控制不会抹平模型差距，但会让模型差异集中体现在高价值 pass，而不是全仓自由编辑。
  - 因此更强模型反而增加 deterministic zones、diff budget、intent consistency check、provenance 的价值。
- 与 Git/Linux 等影响力对比。
  - 项目影响力更接近 Git、Docker、Terraform、Kubernetes、npm/Maven、CI/CD 这类工程组织基础设施。
  - 不宜夸大为 Linux/GPU/Transformer 级基础科学或底层平台替代。
- 高影响力技术参照。
  - 参考对象包括 Git、Docker、Kubernetes、Terraform、PostgreSQL、Redis、Kafka、RabbitMQ、Elasticsearch、NPM/Maven、CI/CD、VS Code、Linux、LLVM、React 等。
  - 不同参照用于不同维度：版本协作、封装部署、编排、基础设施声明、数据系统、消息、搜索、包生态、开发入口、编译器、UI 组件生态。
- Redis 定位。
  - Redis 未必是宏大基础范式，但作为缓存、会话、限流、短期状态、队列、语义缓存的标准件非常重要。
  - 在本项目中应通过 Infra Pack 暴露能力，而非让业务 block 直接散落依赖。
- MQ / Elasticsearch 定位。
  - Kafka/RabbitMQ 属于事件流、消息总线、异步任务和解耦能力标准件。
  - Elasticsearch 属于搜索、日志检索、分析和 RAG 检索标准件。
  - 它们应通过 Infra Pack + Strategy Pack 进入项目选择与图谱。
- 命名。
  - 从 vibecoding 到受控 AI 编程，可称 Spec Coding、Spec Engineering、Compiler-Driven AI Engineering、Spec-Driven AI Engineering。
  - 产品名倾向 `SpecEngineer`。
  - `Harness Engineering` / `Project Harness Engineering` 可表达把模型、工具、验证和权限套进工程 harness 的方法，但可能与更泛化的 harness 概念碰撞。
- 平台成功后的世界变化。
  - 项目创建成本下降后，会产生更多项目变体、产品线、一次性工具、行业包、企业内部应用和个性化系统。
  - 维护、升级、合规、验收、监控、block 认证、行业能力包会成为新市场。
- 更暴力愿景。
  - 长期可以从 Project Harness 走向 Capability Supply Chain、Executable Expertise、Organization Compiler、Venture Compiler、Reality Compiler。
  - 这些属于愿景和素材，不应直接写成 v0.1 权威规格。
- 可迁移方法。
  - 可迁移到合规/安全、数据产品、agent teams、行业 SaaS 变体、API 集成、知识系统、教育、法律、机器人/IoT、安全防御、云基础设施、研究实验、个人生产力、内容工厂等。
  - 共同公式是：隐性专家劳动 -> 结构化规格 -> 标准模块 -> 管脚连接 -> AI 填 slot -> 验收验证 -> 可持续维护。

### 059-066（行 15347-18207）

抽取状态：已分段抽取，待逐条去重与映射。

- Harness Engineering 命名。
  - `Harness Engineering` 能表达“把 AI 放进受控执行架”的方法，但命名可能过宽。
  - `Project Harness Engineering` 或 `Spec Harness` 更接近本项目语义。
- Claude / Anthropic 相关情况。
  - Claude Code 的 hooks、permissions、subagents、checkpoints、slash commands、skills、managed agents 等体现了 agent harness。
  - Anthropic 方向更多是 AI agent 工具和执行 harness，不等同于项目级工程编译器。
  - 本项目应吸收其优点，但把 hooks、permissions、skills、agents 提升为项目语义 hooks、semantic permissions、blocks、compiler agents。
- Brain / hands / session 架构。
  - 模型是 brain，工具和文件系统是 hands，session 是上下文窗口。
  - Project harness 要把这些能力放进 task envelope、zone、verification、provenance 和 rollback 中。
- 更强未来模型是否削弱项目。
  - DeepSeek v4 或未来更强模型不会消除工程治理需求。
  - 模型越强、越会自主完成复杂任务，越需要机器可校验边界和可回放证据。
- 开源生态。
  - 开源能推动 block 生态，但要防止低质 block、供应链风险、版本漂移、接口不稳和恶意代码。
  - 需要官方/认证/社区/私有 registry 分层。
- MCP 定位。
  - MCP 可能退化为 AI 控工具协议；本项目不能让 MCP 成为主控。
  - MCP 可作为 registry、verification、observability、deployment adapters 的连接方式。
- 平台控制 AI 实现细节。
  - 平台决定任务、上下文、写入边界、验证和回放；AI 只在 envelope 内产出。
  - 主控 AI 对代码要求降低，是因为平台把工程语义和上下文先编译出来。
- benchmark 失效问题。
  - 传统 coding benchmark 可能无法衡量“平台 + 模型 + harness”的工程产出。
  - 需要新 benchmark 衡量从 spec 到可运行系统、从失败到修复、从升级计划到安全迁移的能力。
- 代码细节浩如烟海与完备性。
  - 模型不可能天然记住所有项目因果上下文。
  - 完备正确性不可保证；目标应是收敛、可定位、可验证、可恢复。
- 开放区与治理。
  - 完全封闭会失去灵活性；完全开放会退回 AI 乱改。
  - 需要 Closed Core / Open Edge、Open Implementation / Governed Structure。
  - slot taxonomy 可包括 Pure Logic、Adapter、Policy、Effect、Kernel Override。
- 三种真实。
  - spec truth：规格、block、graph、acceptance、policy。
  - source truth：真实仓库源码。
  - runtime truth：运行时行为、日志、监控、用户数据。
  - 平台必须承认三者可能不一致，并通过 verification/provenance/observability 收敛。

### 067-071（行 18208-21340）

抽取状态：已分段抽取，待逐条去重与映射。

- Model + harness 是否就是 agent。
  - 不一定。Agent 只是模型使用方式之一。
  - 未来更大的方向可能是结构化 workflows、compiler-controlled model use、deterministic passes 和 task-specific AI workers。
  - SpecEngineer 不应定位为 agent 产品，而应定位为 project compiler / project harness / executable engineering substrate。
- 技术社区接受度。
  - 技术社区可能拒绝“封装轮子”，担心黑盒、锁定、质量、抽象泄漏和失去控制。
  - 必须用源码可达、可接管、可验证、可解释、可升级、可回滚来降低抗拒。
  - 官方、私有、认证社区、普通社区 block 应有不同信任等级。
- 更大想象力与六层路线。
  - Project Harness：项目级受控生成和维护。
  - Capability Supply Chain：可验证能力供应链。
  - Executable Expertise：专家经验变成可执行资产。
  - Organization Compiler：组织流程、权限、数据和工具被编译成系统。
  - Venture Compiler：业务想法到产品/运营系统的编译。
  - Reality Compiler：更广义地把现实约束、流程和执行系统连接起来。
  - 后三层偏愿景，应先作为素材保留。
- 六层是否容易做到与护城河。
  - 表面上每层都能讲清楚，但难点在高质量 block 生态、稳定 contract、验证闭环、升级治理、开发者信任和长期维护。
  - 真正护城河不是几张图或一个 CLI，而是可复用资产、数据、benchmark、生态、治理和 dogfood 积累。
- 机器码膨胀与编译器类比。
  - 有了编译器后仍有人写汇编/机器码，但大多数人不会默认从机器码写起。
  - 有了项目编译器后仍会看源码、改源码、写底层，但默认入口可上移到 spec/block/graph。
  - 项目编译器比传统编译器难在需求语义、外部系统、长期演进、组织约束和不完全形式化。
- 项目上下文远超 AI。
  - 全源码不等于全上下文，项目真正上下文包括需求、历史决策、约束、运行行为、事故、组织规则和未写下的业务知识。
  - AI 遗漏上下文会导致错误堆积。
  - 平台应成为 AI context compiler，把任务相关的全信息熵组织成 context packet。
- Context Packet 内容。
  - 应包含 task、affected graph、blocks、pins、policies、writable anchors、readonly anchors、cannot_modify、must_preserve、verification、provenance、issue classification、runtime evidence。
  - 目标是减少对全代码仓库的依赖，让 AI 先处理因果上下文，再必要地下钻源码。
- Review 方式变化。
  - Review 不应只看代码 diff。
  - 应看 engineering semantic diff：能力变化、block 变化、policy 变化、acceptance coverage、provenance、runtime impact、upgrade impact、risk class。
- 四根 workspace 与 control plane 架构审查。
  - `source/` 是 authoring truth，`project/` 是 runnable materialization，`control/` 是 project control plane，`.pjc/` 是 disposable local state。
  - `project/**` 可运行、可调试、可部署，但不作为长期事实源；直接修改应进入 drift / override / 回写流程。
  - `control/**` 不应变成报告垃圾桶，需要明确哪些是协作事实、哪些是 CI/runtime artifact、哪些是本地状态。
  - `source/code/opaque/**/module.yaml` 的价值在于平台不理解内部实现时仍控制 entry、exports、输入输出、副作用、权限、验证和 provenance。
  - Workbench 视图不能成为第二事实源；图上编辑必须生成 structured mutation 并回写 `source/**`。
- 新增 lock / manifest 候选。
  - 对话录提出 `control/manifest.json`、`registry.lock.json`、`compiler.lock.json`、`block.lock` 等候选。
  - 实际工程设计不能照搬新增文件；必须先复核当前 `graph.lock.json`、CI artifact manifest、provenance、review summary 是否已覆盖同一事实源。
  - 当前可稳定吸收的是“registry/block 解析、compiler version、source/project/control 路径必须可复现可审计”的原则。
- 不追求完备证明。
  - 平台不证明任意业务逻辑永远正确；它证明逻辑有归属、边界、入口、出口、验证面、来源和影响路径。
  - 可治理对象分三圈：强语义核心（block、pin、capability、entity、flow、policy、state machine、acceptance、provenance）、弱语义开放区（slot、adapter、view、private block、integration、patch、opaque module）、逃生与回收机制（lab、manual override、emergency patch、legacy integration）。
  - 关键公理：每一段逻辑都必须有归属、有边界、有来源、有验证面。

### 072-082（行 21341-26893）

抽取状态：已分段抽取，待逐条去重与映射；本段外部产品与竞品判断只作为调研线索，写入权威编号文档前必须额外验证，不可把对话内容直接当项目事实。

- 受限递归与统一逻辑容器。
  - 业务结构可套娃，但必须是受限递归，不是任意嵌套。
  - `Project / Capability / Block / Flow / Policy / Slot / OpaqueModule / Patch / View / Agent` 可共享一组统一治理问题：provides、requires、pins、effects、policies、acceptance、provenance、children。
  - 第一版不应结构化 slot 内部细节，优先保持文件级 slot，再逐步回收为 private block 或更细粒度 contract。
- Block 是项目编译器的必要资产。
  - 只有 Graph/Context 压缩，没有可复用 block，平台更像 repo harness 或 AI context tool。
  - 高质量官方 block 库、pin/slot contract、acceptance、provenance、upgrade rule 才是“软件芯片”资产。
  - 对话录提出的初始 block 清单（auth、tenant、rbac、entity、audit、workflow、notification、file、search、agent worker）与当前官方块方向大体一致，但是否新增 agent/search/workflow 需由路线图和母例需求决定。
- Block 版本治理。
  - block 版本不仅是 semver/API，还涉及 pin 兼容、slot contract migration、schema migration、acceptance migration、override conflict、compatibility matrix 和 rollback boundary。
  - 对话录提出独立 `block.lock` / `registry.lock`；实际工程应先复核当前 `graph.lock.json.resolvedBlocks[]` 已记录 version、registrySourceId、registryKind、registryLocation、registryPath，不应无消费方地新增第二 lock。
- Graph / Repo Harness 的真实边界。
  - Graph-only / repo graph / context graph 已是拥挤能力，不应成为主护城河。
  - 可作为旧项目迁移入口：existing repo → code/knowledge graph → context packet → controlled edit → candidate slot/opaque/private block → block-compiled mode。
  - 正确命名更接近 Repo Harness 或 Graph Harness；它是 Level 0，不是最终产品主形态。
- Project Knowledge Graph 与信息宇宙。
  - 项目信息不是全部“图”，而是结构化事实源与多种投影：图、表、矩阵、树、时间线、diff、报告。
  - 底层不应维护 40 套图，而应有统一 Project Knowledge Graph / Engineering IR / Explain Graph，再派生 block graph、pin graph、policy graph、provenance view、semantic diff 等视图。
  - MVP 只需要少量核心对象和 6-8 张核心视图；成熟平台才逐步扩展到几十种对象和视图。
  - 每个重要投影都应能下钻到 source fact、generated artifact、verification evidence、provenance 和 related issue。
- 顶级工程全貌与外部方法论。
  - SysML/MBSE、ArchiMate/TOGAF、C4、BPMN/DMN、IaC/Kubernetes、Git 等分别覆盖复杂系统、企业架构、软件沟通、流程决策、基础设施声明和历史协作。
  - 本项目不应变成重型万能模型；目标应是“可治理全貌”：什么必须模型化，什么边界化，什么证据化，什么交给人负责。
- 竞争与外部产品判断。
  - GitHub Spec Kit、Claude Code/Codex、Cursor/Sourcegraph、Lovable/Spark、Backstage、GitNexus、Graphify 等只作为相邻方向调研线索。
  - 写入权威文档前必须独立验证它们的实际能力、license、输出 schema、稳定性和是否存在本仓可集成入口。
  - 稳定结论不是“没人做”，而是本项目不能停留在 spec-driven、agent harness、repo context、AI app builder、template portal 或 graph-only 任一单点。
- Agent Project Mode。
  - Agent 项目未来可能需要 agent role、tool pins、permission policy、memory contract、human approval、evals、sandbox、audit log、runtime target。
  - 这说明 agent 项目应能进入 block/pin/slot/acceptance 体系，但当前不应变成 agent runtime。
  - 当前路线只可把 Agent Project Compiler 作为 v1+ / later 方向或第二 demo 候选，不抢 v0.2 Ticket SaaS 主线。
- 当前工具链与 Repo Harness v0。
  - 现用 `jscpd`、`dependency-cruiser`、Graph-It-Live/MCP、自研 ts-morph 脚本，本质上是手工 Repo Harness：用于减少 AI 满仓搜索、发现重复、检查依赖边界和提供 review 结构信号。
  - 它们应进入 `control/evidence/**` 或 `control/graph/*-overlay.json`，而不是替代 `source/**`、manifest、graph lock、provenance、review summary。
  - 工程上应优先把现有工具输出归一为 evidence/provider，而不是继续堆新图工具。
- GitNexus / Graphify 集成策略。
  - 可作为 Repo Harness 的图谱输入 provider：GitNexus-like 工具偏 call/execution/impact，Graphify-like 工具偏 docs/code/diagrams 多模态 knowledge graph。
  - 必须弱依赖、可替换、只读化、归一化；provider 输出只能成为 observed evidence，不能直接成为 authoring truth 或主控图。
  - 禁止让 MCP agent 通过 provider 查询后自行决定怎么改项目；正确方向是平台读取 provider evidence，生成 task envelope，再派发 AI。
  - 对话录提出“先 Graphify 后 GitNexus”的顺序只是素材；实际是否集成、何时集成必须基于本仓已有 Graph-It-Live 与 tool evidence pipeline 的缺口分析。
- 最底层定位。
  - SpecEngineer 的底层问题是“谁承载项目真实含义”，不是“AI 如何多写代码”。
  - 稳定定位可收束为：把软件项目从源码堆提升成可编译、可治理、可验证、可追踪、可演化的工程事实系统。
  - “项目熵控制器”是强叙事素材，但权威实现文档应落到 fact source、control plane、block supply chain、acceptance、provenance、semantic diff、upgrade/migration 等可执行对象。

## 主题归类表

> 初始归类：用于下一步去重和权威文档映射；状态是初判，不代表已经完成统合。

| 主题 | 覆盖轮次 | 初始状态 | 处理建议 |
| --- | --- | --- | --- |
| 原始对话录治理与抽取流程 | 全部 | 已整合 | `00` 已说明原始对话录、抽取台账和编号文档之间的权威层级与迁移流程。 |
| 产品定位：SpecEngineer / 工程编译器 | 001-018,039-048,067-071,079-082 | 已有覆盖 | `01/02` 已覆盖稳定定位；“项目熵控制器”等表述只作为叙事素材，权威文档落到事实源、contract 和治理闭环。 |
| 方法论命名：Spec Engineering / Spec Coding / Harness Engineering / Repo Harness | 049-066,074-082 | 已有覆盖 | `01/02/11` 已覆盖稳定命名和 graph/tool 边界；Repo Harness 作为迁移入口或证据层命名，不作为最终产品主形态。 |
| 功能块 / Capability Block / Strategy Pack / Infra Pack / Governance Pack | 001-008,049-058,074,079 | 已有覆盖 | `06` 已覆盖 block 分类、pack 协议、kernel block 与信任治理；新增 agent/search/workflow 等候选必须服从 `03` 路线和母例需求。 |
| AI 角色：slot synthesis、alignment、repair、review、upgrade planning | 009-018,039-048,059-066,071,074,079 | 已有覆盖 | 主要已在 `09` 覆盖；Agent Project Mode 暂作远期素材，不抢当前 AI Runtime 边界。 |
| Task Envelope / Context Pack / AI Runtime API | 009-018,039-048,067-071,081-082 | 已整合 | `09` 是权威；Context Packet 已收敛为 task envelope 的只读投影，外部 provider evidence 只能进入受控 task envelope。 |
| Zone / allowedPaths / semantic permissions / diff budget | 019-028,059-066,071-073 | 已有覆盖 | `05/09` 已有核心规则；受限递归和统一逻辑容器只作为后续 schema 演进素材。 |
| Workspace 四根布局 / Control Plane / Opaque Module / Workbench mutation | 071-082 | 已整合 | `05/06/10/11` 已覆盖 source/project/control/.pjc、opaque module 边界、private block 回收、override/drift、Workbench structured mutation。 |
| Verification / Acceptance / Policy / Provenance | 001-008,029-038,059-071,074,079 | 已有覆盖 | `08/09` 是权威；本台账保留动机和类比。 |
| Graph / explainability / semantic diff / Engineering IR | 019-038,067-071,076-082 | 已整合 | `08/11` 已覆盖 graph 类型、Engineering Semantic Diff、canonical graph 与投影视图；避免把每个视图变成独立事实源。 |
| Tool evidence provider / Repo graph / GitNexus / Graphify | 080-082 | 已有覆盖 | `05/08/11` 已覆盖 jscpd、dependency-cruiser、discover、Graph-It-Live/MCP 的 evidence/overlay 边界；GitNexus/Graphify 只作为待验证 provider 线索。 |
| Issue 分层：Spec / Composition / Slot / Kernel | 029-038,071-073 | 已有覆盖 | 检查 `07/08/09` 是否都有一致术语；不把“不能完备证明任意逻辑”误写成工程不可治理。 |
| MVP 母例与路线：多租户 B2B SaaS / Ticket SaaS | 001-018,039-048,074,079 | 已有覆盖 | `02/03` 为权威；Agent Project 或双 demo 只可作为 later 候选，不扩大 v0.2 主线。 |
| 技术栈取舍：Next.js/TS/Prisma/SQLite，不用 Elysia | 009-018 | 已有覆盖 | `02/03/05` 如已有则不重复；只作为 MVP 决策。 |
| 与 n8n/OpenClaw/Temporal/Camunda 等边界 | 001-008,039-048,072,077-078 | 已有覆盖 | `01/02` 已覆盖产品边界；外部方法论只取局部启发，不引入重型万能模型。 |
| 高性能/底层/Kernel Boundary | 019-028,029-038,072-073 | 已有覆盖 | `05/06/07/08` 已覆盖 kernel boundary 与 AI 禁写内核规则；受限逻辑容器不等于平台证明任意代码正确。 |
| Infra 标准件：Redis/Kafka/RabbitMQ/Elasticsearch/Object Storage | 049-058 | 已有覆盖 | `06` 已初步覆盖；后续可检查是否需 policy/strategy 映射。 |
| Block version / pin / slot contract / migration / rollback | 074,079 | 已有覆盖 | `06/10` 已覆盖版本、升级、override、migration、回滚；新增 `block.lock`/`registry.lock` 前必须先证明 `graph.lock.json.resolvedBlocks[]` 不足。 |
| 开源生态、社区 block、供应链信任 | 001-008,059-071,074,079 | 已有覆盖 | `06` 已覆盖 trustLevel、official/private/community 准入和发布元数据；`10` 已覆盖升级、override 与回滚治理。 |
| Benchmark 与证据：Spec-to-System / Harnessed Engineering | 029-038,059-066,081-082 | 已整合 | `03` 已补 Spec-to-System benchmark 评分口径；工具 evidence 应服务 dogfood、review 和复杂度收敛，而不是堆工具清单。 |
| Project Knowledge Graph / 信息宇宙 / 多投影视图 | 076-080 | 已有覆盖 | `08/11` 是权威；稳定结论是统一 canonical graph/Engineering IR 派生多视图，不维护多套互相漂移的图。 |
| 自举 / 自维护 | 039-048,079 | 仅素材保留 | 作为长期方向，暂不进入 v0.2 主线规格。 |
| Agent Project Compiler / Agent Project Mode | 074,079 | 仅素材保留 | 可作为 v1+ 或第二 demo 候选；当前只保留“agent 项目也应进入 block/pin/slot/acceptance 治理”的方向。 |
| 组织编译器 / Venture Compiler / Reality Compiler | 049-058,067-071,079 | 仅素材保留 | 属远期愿景，适合 `01` 愿景区或保留台账。 |
| 外部竞品与相邻产品判断 | 078,080,082 | 仅素材保留 | 进入权威文档前必须独立验证能力、license、schema、稳定性和集成入口，不把对话判断当事实。 |
| 用户/开发者接受度与学习成本 | 039-048,067-071 | 已有覆盖 | `01/02` 已覆盖采用前提、学习成本、逃逸能力和早期 adoption 路线；`03` 保留阶段推进口径。 |
| 源码地位：可见、可接管、非唯一权威 | 009-018,019-038,067-071,074-082 | 已有覆盖 | `01/02` 已覆盖源码地位；`05/08/09/11` 保持 source fact、generated artifact、evidence overlay 和 projection 的边界一致。 |

## 权威文档映射表

> 初始映射：只说明信息应归属哪里，不表示已写入。后续应逐条对照现有 `00-11`，把状态改为 `已整合` / `已有覆盖` / `待整合` / `仅素材保留` / `冲突待决`。

| 权威文档 | 应承载的信息 | 当前初判 |
| --- | --- | --- |
| `docs/00-*` 文档治理 | 原始对话录、抽取台账、编号文档之间的权威层级；“先全量抽取，再判断是否进入权威文档”的流程。 | 已整合 |
| `docs/01-用户能力模块化开发-主题整理稿.md` | 核心概念、术语、长期愿景、软件芯片化类比、源码地位、SpecEngineer / Spec Engineering 命名、Project Harness / Capability Supply Chain、项目熵控制器等高层叙事。 | 已有覆盖 |
| `docs/02-工程编译器-MVP-PRD与架构稿.md` | 产品定位、非目标、与 n8n/OpenClaw/低代码/AI IDE/graph-only tool 的边界、MVP 母例、用户接受度、产品形态。 | 已有覆盖 |
| `docs/03-MVP实施计划与路线图.md` | v0.2 范围、v0.3+ 延期项、benchmark、dogfood、生态阶段、CLI/MCP/IDE/workbench 优先级、Agent Project later 边界。 | 已有覆盖 |
| `docs/04-*` AI 执行蓝图 | AI 作为受控 pass 的协作流程、上下文包生成、repair/review/upgrade planning 的执行蓝图。 | 已有覆盖 |
| `docs/05-编译器核心实现规格.md` | app spec、resolved graph、install plan、IR、四根 workspace、文件区/zone、slot/source map、compiler pass ownership、kernel boundary、opaque module 边界、tool evidence 入口边界。 | 已整合 |
| `docs/06-Registry与Block协议规范.md` | block manifest、pins、capability/strategy/infra/governance packs、registry trust、官方/社区/私有 block、Infra Pack 标准件、block 版本、opaque module 到 private block 的回收路径与语义合约演进。 | 已整合 |
| `docs/07-*` Pass / Error / Recovery | Spec/Composition/Slot/Kernel Issue 分层、失败归因、恢复策略、不得扩大 envelope 的规则。 | 已有覆盖 |
| `docs/08-Verification、Provenance与Graph规范.md` | acceptance、policy、verification report、provenance、graph 类型、coverage、Engineering Semantic Diff、context-to-graph、evidence provider、graph overlay、canonical graph / Engineering IR 边界。 | 已整合 |
| `docs/09-AI Runtime、任务信封与治理规范.md` | AI Runtime API、task envelope、Context Packet 投影、allowedPaths、semantic permissions、diff budget、context priority、agent role boundaries、外部 evidence 进入 task envelope 的受控路径。 | 已整合 |
| `docs/10-*` Upgrade / Override / Migration | block 升级、override、迁移、registry 版本、破坏性变更、回滚、人工确认、slot contract migration、override conflict、compatibility matrix。 | 已有覆盖 |
| `docs/11-Workbench与可视化规范.md` | Workbench mutation、review/graph/source/slot-rule 视图、Mermaid/DOT/export、Engineering Semantic Diff 展示、evidence collection、canonical graph projection、Graph-It-Live/MCP 边界、语义合约型 block 与 Engineering IR 演进原则。 | 已整合 |

## 权威统合记录

- `docs/archive/用户能力模块化开发-信息抽取台账.md`：补入 071 续写与 072-082 的分段抽取、主题归类和权威映射；外部产品判断只作为待验证线索。
- `docs/00-文档索引与一致性规则.md`：补入抽取台账的非权威层级，明确长对话录应先进入台账做覆盖、去重、归类和映射，再迁移稳定结论；同步编号范围到 `00-11` / `05-11`。
- `docs/01-用户能力模块化开发-主题整理稿.md`、`docs/02-工程编译器-MVP-PRD与架构稿.md`、`docs/03-MVP实施计划与路线图.md`：同步实现级协议范围到 `05-11`，并修正路线图中的分层规格栈范围为 `00-11`。
- `docs/05-编译器核心实现规格.md`：补入 `source/code/opaque/**/module.yaml` 开放实现边界，明确 opaque module 不是第二事实源，AI 只能在 task envelope 授权下改具体路径。
- `docs/06-Registry与Block协议规范.md`：补入 opaque module 回收为 private block 的条件，避免临时开放区绕过 registry trust、version、upgrade 和 rollback。
- `docs/08-Verification、Provenance与Graph规范.md`：补入 Engineering Semantic Diff 的 review 合同，明确它从 lock、IR/graph、provenance、verification、coverage、policy、repair/upgrade 派生，不新增第二套 review schema。
- `docs/09-AI Runtime、任务信封与治理规范.md`：补入 Context Packet 投影，明确它是 task envelope 执行时的只读上下文投影，不是新事实源或独立状态机。
- `docs/11-Workbench与可视化规范.md`：补入 Review View 展示 Engineering Semantic Diff，但仍只从 review summary 和现有治理产物派生。
- `docs/03-MVP实施计划与路线图.md`：补入 Spec-to-System benchmark 评分口径，明确优先衡量规格到可运行系统的工程闭环，而不是裸模型代码题能力。

## 后续处理队列

- 072-082 已完成分段抽取与主题归类；下一步只复核少量可能存在措辞缺口的权威文档，不重复写已覆盖主题。
- 对 `00-11` 编号文档做最终一致性核对，避免把同一规则重复写入多个权威位置。
- 只把稳定、可执行、对当前产品和实现有约束力的信息写入权威文档；愿景、类比、远期想法和未经验证的外部产品判断保留在本台账。
- 后续重点检查术语口径是否漂移：SpecEngineer / Spec Engineering、Repo Harness、Context Packet、semantic diff、Engineering IR、kernel boundary、benchmark。
