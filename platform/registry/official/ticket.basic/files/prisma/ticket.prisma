model Ticket {
  id          Int    @id @default(autoincrement())
  tenantId    String
  title       String
  description String
  status      String
  assigneeId  String
  createdBy   String
  updatedAt   String
}
