#!/usr/bin/env sh
set -eu

export SEC_GIT_HOOK_ACTIVE=1

if [ -f ./package.json ]; then
  bun run postinstall
fi

if { [ "${3-0}" = "1" ] || [ ! -e node_modules ]; } && [ -f ./package.json ]; then
  bun run deps:ensure
fi
